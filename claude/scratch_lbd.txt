--- LBD[0] ---
Enable native think=True across the WHOLE structured pipeline (planner DAG emission, shape-selector, deep-research, clarify/curiosity gate) AND shape/specialization chat — superseding the think=False structured-call default. Guard it with a robust JSON-extraction interceptor in the transport that parses the JSON out of a CoT-prefixed response, plus prompts optimized for a thinking model. Planner-DAG malformation mitigated by the interceptor + DAG schema-validation/fallback (builder-owned).
--- LBD[1] ---
Raise num_ctx from the baked 8192 in the gemma4-e2b-agent Modelfile: measure live VRAM on the 6GB card at candidate windows, pick the largest safe value (no OOM with think=True KV-cache), and rely on the existing auto-compact (llm_framework/context.py) for multi-round chat. GPU is now the chat's SOLE consumer (GeoGuessr training is off the card), so full headroom is available — but the value is still measured, not guessed.
--- LBD[2] ---
Definition of done is END-TO-END with SUBSTANTIVE content: the US-Iran "detailed HTML report" task must converge past clarification AND the deep-research→HTML pipeline must produce a real file containing substantive, sourced content (headlines, timeline, damage figures with sources). A thin/empty file = FAIL. Deep-research OUTPUT QUALITY (fixing the prior "no headlines / empty mails" emptiness) is IN SCOPE. Root cause of the loop is fixed regardless of whether it's reasoning-off or a gate state-bug that never reads the already-given answer.
--- LBD[3] ---
Acceptance is verified LIVE in the chat UI (human runs the browser chat) — the US-Iran HTML report task is the demo: it must converge past clarification and produce a substantive sourced report, observed by the user. Scripted tests may support, but the human UI run is the gate.
--- LBD[4] ---
MEASURED root cause of the clarify loop (refines o2/d3, does NOT change the goal): it is a FRONTEND/response-contract WIRING gap, not reasoning-off. The React UI has no clarification-resume surface — MessageResponse lacks needs_clarification, ResumeRequest lacks an answer field, and pendingResolution is gated on missing_specialist=False for the clarify case. Backend /resume clarify path + skip_ambiguity are already CORRECT. The user's already-given answer never reaches /resume; the UI re-sends it as a fresh /message, so the gate re-asks. THE LOOP FIX = wire the frontend clarify-resume path (carry the answer into /resume). think=True on the ambiguity opts is a quality lever, not the loop fix.
--- LBD[5] ---
MEASURED CoT shape (refines d1): gemma4-e2b-agent on native /api/chat returns CoT in a SEPARATE message.thinking field; message.content is CLEAN markdown-fenced JSON, NOT CoT-prefixed. So the "JSON-extraction interceptor" scope shrinks to: strip code fences + balanced-JSON safety-net at the two DIRECT json.loads sites (toolargs.py:572, tool_registry.py:413) — chain sites already strip fences via stages._extract_json. CRITICAL caveat: thinking tokens STILL count toward num_predict, so per-call num_predict must be RAISED when think=True or message.content truncates to empty. This num_predict bump applies pipeline-wide and interacts with s2's num_ctx sizing.
--- LBD[6] ---
a2 measured a SEPARATE robustness gap (instance of d1's "DAG schema-validation + fallback"): with think=True the planner non-deterministically emits a DAG with a dangling depends_on edge (references a phantom node id) — 4/5 valid across fresh model-loads. parse_dag correctly REJECTS it (no silent bad DAG), but 4/5 is not good enough. The fallback must REPAIR or retry-on-reject so a dangling-edge emission does not surface as a user-visible failure. Acceptance for o1 (reasoning pipeline-wide) must include: dangling depends_on edges are handled safely (repaired/retried), validated across multiple fresh model-loads — not just a single happy-path run. Concrete: num_predict=4096 is load-bearing (thinking tokens compete with content; <=512 yields empty content).
--- LBD[7] ---
CLARIFICATION POLICY (reverses d8): the ambiguity/clarify gate must fire ONLY for scheduled tasks. A normal request (e.g. the bare "US-Iran HTML report") must PROCEED without asking a clarifying question — sensible defaults (general overview / full timeframe), not interrogation. User: "The only clarification I see we need is for scheduled tasks." Suspected cause = vague/over-cautious prompt wording, so the fix is likely a PROMPT fix (tighten the ambiguity prompt), not just wiring. This supersedes d8 ("asking once is correct") — asking on a normal report is the bug.
--- LBD[8] ---
PROMPT-QUALITY MANDATE: all prompts (planner, node/spec-execution, ambiguity gate, tool-select/email, spec+shape selection, any others) must be CONCISE, CRISP, token-efficient, and anti-hallucination. REVIEW BEFORE MODIFYING (read-only audit first, user approves the fix proposal before edits). Ground the rewrite in: (a) how strong planners direct nodes via high-quality node DESCRIPTIONS — study the worker prompts, the agentic-plan skill, and the NEWER saved plans (skip the old low-quality ones); (b) eda-base3 compiled-specializations (how a complex ruleset condenses into a compiled doc); (c) gradual phased context-building. Add a stronger agent IDENTITY/system-prompt that leans into gemma's coding+reasoning (Claude-Code-like: capable agent first + memory for long-horizon reasoning).
--- LBD[9] ---
EMAIL POLICY: send_mail must fire ONLY when the user explicitly asks for email. Current bug: email is sent for simple chat/tasks where none was requested. Fix the over-firing (likely a tool-selection prompt and/or trigger-logic issue). Keep the existing hard safety lock (recipient = user's own address only).
--- LBD[10] ---
SELECTION + UI: (a) verify/define how the planner selects specializations & shapes and ensure PROPER selection guidelines exist (grounded in audit findings); (b) add UI DELETE buttons for specializations and shapes so the user can clear noise. FINAL ACCEPTANCE: after all fixes, drive a DETAILED UI-driven testing phase (like the Round 3 d15 matrix) where the user validates ALL broad scenarios live in the UI (reasoning on; no re-ask except scheduled; no unwanted email; substantive report; correct spec/shape selection; deletes work; multi-round chat holds).
--- LBD[11] ---
BEHAVIOR = PURE REASONING, NO structural override flags (refines/limits d10+d12). Drop the proposed wants_email tool-strip AND the clarify scheduling-intent logic-gate. The free-flowing agent DECIDES email/clarify by its own reasoning (now that think=True is on) via good prompts + the identity. The lever for correct tool/spec/shape behavior is STRONG DESCRIPTIONS: spec descriptions and shape descriptions must be strong enough that the LLM selects them effectively — AND the AUTHORING prompts (where the LLM writes specs/shapes, e.g. describe-to-gemma) must EXPLICITLY highlight that it must produce strong, selection-effective descriptions. The ONLY hard structure that stays is the recipient self-only lock (safety, untouched). Accept that a small model may rarely slip (worst case: emails the user themselves — harmless under the lock); that is the accepted price of free-flow. Clarify gate restricted to scheduling intent via PROMPT only (no logic gate).
--- LBD[12] ---
IDENTITY = UNIVERSAL system prompt shipped with EVERY task (refines d11). Like Claude Code's system prompt: a simple agent identity that rides on every LLM call — planner, workers/nodes, AND runtime/chat — giving the whole system a consistent capable-agent identity that shapes behavior on all tasks. Keep it SIMPLE and SHORT (~100-150 tokens; not complicated, not coding-instructions — coding lives in planner+specs). Note: shipping it everywhere adds a small constant to every call's tokens; keep it tight and account for it against num_predict/num_ctx budgets.
--- LBD[13] ---
ARCHITECTURE PROBE before s3 edits (user-requested): a read-only probe must answer (a) does the app do query-rewriting and is it even required? (b) HOW does context flow between nodes today? — and surface to neuron→user BEFORE any s3 edits. Rationale: weak inter-node context flow could itself be a root cause of the agent feeling 'dumb'; the audit did not document the current inter-node context-flow mechanism or assess query-rewriting necessity. Implemented as a mandatory gate at the start of s3 (diagnose-then-fix): probe → report up → WAIT for user approval → fix.
--- LBD[14] ---
INTER-NODE CONTEXT FIX (probe finding, user-approved; likely the real cause of thin/empty multi-step reports → o4). Root cause: runtime.py:399 _compose_task hard-clips every upstream dependency's output to 800 chars (~200 tok), and the rich fetched-source text only renders into the research node's OWN turn — so a downstream writer/synthesize node sees only the first 800 chars of upstream prose and never the full sources. APPROVED FIX (depth = "raise clip + feed fuller upstream", NOT transitive): (1) raise/BUDGET the 800-char clip to a sensibly larger, configurable value sized against the new num_ctx (note: num_ctx is chosen in s5 AFTER s3 — so parameterize the clip now with a good larger default and finalize/verify the budget in s5/s6, don't hardcode a new magic constant blind to num_ctx); (2) feed downstream nodes FULLER upstream content (research prose + key sources / tool_value), so synthesize→write actually sees the research. OUT OF SCOPE: transitive multi-hop context accumulation (#2) — direct-deps-only stays for now. Query-rewriting is healthy (search-path only; request reaches planner verbatim) — NO change.
--- LBD[15] ---
SHAPE & SPEC AUTHORING MUST BE FREE-FLOWING + ITERATIVE (edit/refine existing, not one-shot create). (a) SHAPES: today only a describe->CREATE box exists, no edit; user asked for "linear PLUS modular parallel" and got a flat SEQUENTIAL DAG (compositional/multi-pattern intent NOT captured) and cannot refine it. Add conversational EDIT/REFINE of an existing shape AND make the shape author honor compositional/multi-pattern intent. (b) SPECS: multi-turn author exists but it is unclear whether each turn ITERATES on the previous spec or restarts — ensure it builds on the current spec. Ensure BOTH shape and spec support iterative refine-on-existing via chat (the free-flowing interface the user hoped for). Connects to d11/d14 (free-flow) and d13 (selection/authoring quality).
--- LBD[16] ---
OBSERVABILITY — Phoenix logging is BROKEN (user cannot read logs; we are flying blind, can see neither prompts nor reasoning blocks). Build a LOCAL trace folder that renders traces to MARKDOWN by reading the trace JSONs, capturing ALL contextual info: every PROMPT (system+user turns), the reasoning/thinking blocks, and token cost per call/step. Since Phoenix is not producing readable output, ensure the underlying span/trace data is actually captured to local JSON (e.g. a file/OTLP exporter) as the SOURCE, then render markdown from it. Debugging aid for BOTH the user and the neuron — high leverage; should land before the report-quality (s6) and live-testing (s7) work so those are not debugged blind.
--- LBD[17] ---
S5 INPUT from the live trace (s7/a2 capture gate): in a real chat run, ~4 llm.chat spans showed EMPTY thinking blocks — diagnosed as a num_ctx=16384 SATURATION signal (context full with think=True + num_predict=4096 + large prompts => thinking gets squeezed to empty), NOT a tracing defect. So s5's num_ctx choice must account for the think+num_predict budget: a bigger num_ctx is not automatically better — if prompts+thinking+output saturate the window, reasoning silently degrades (the exact 'dumb' failure). s5 must read the var/traces JSONs + s7 findings, measure VRAM AND verify thinking blocks stay populated (non-saturated) at the chosen num_ctx, not just that it fits VRAM.
--- LBD[18] ---
S5 ADDITIONAL INPUT — keep_alive reload thrash (user-observed via GPU graph, confirmed in code). transport.py:224 defaults keep_alive=0, so Ollama UNLOADS gemma4-e2b-agent from VRAM after EVERY call (deliberate 'VRAM hygiene', d8, from when the 6GB was SHARED with the GeoGuessr job). Symptom: dedicated GPU memory sawtooths ~1.5GB up/down once per LLM call; a ~30-call multi-node run pays ~30 model reloads (~1-2s each) = major latency. NOW STALE: user confirmed the GPU is the SOLE consumer ('it's the only one now'), so keep_alive=0 is pure overhead. s5 should RAISE keep_alive (e.g. -1 keep-resident, or several minutes) so gemma stays in VRAM between calls — eliminating reload thrash AND stabilizing the num_ctx VRAM measurement. Pairs with the num_ctx raise; verify combined VRAM (resident model + KV-cache at chosen num_ctx) stays within the 6GB dedicated (currently ~2.2/6GB used at 8192ctx — headroom exists) and does NOT spill into slow Shared GPU memory.
--- LBD[19] ---
d20 REFRAMED by s5/a1 measurement (corrects the causal read; d20's "verify thinking stays populated" directive still holds, its CAUSE was backwards). MEASURED: gemma4-e2b uses sliding-window attention so KV is nearly free (8192->32768 adds +24MiB; even 131072 fits ~1.56GiB; ~4.1GB free, no Shared-GPU spill, no OOM) — VRAM is a NON-issue. The REAL differentiator is window OVERFLOW: a realistic ~18.4k-token deep-research synthesize prompt OVERFLOWS 8192 AND 16384 (prompt truncated to fill the window) -> thinking block EMPTY -> empty/thin report. 32768 fits the full prompt WITH room for CoT (2910 chars) + substantive output (4797 chars). So d20's "empty thinking at 16384 = big window saturates" was UNDERSHOOT (window too SMALL for the prompt), NOT over-large saturation. FIX = RAISE num_ctx (32768), not cap it. This is very likely THE root cause of o4 (empty US-Iran report): the synthesize node's research-laden prompt overflowed the 8192 window. CAVEAT for s8 (report quality): at 32768, num_predict=4096 becomes the OUTPUT limiter (done_reason=length; thinking+content fill 4096) — long substantive reports may need a HIGHER num_predict (s8 scope). VRAM headroom is huge, so if s6/d18 fuller-upstream feed pushes prompts >18k tokens, num_ctx can later go 65536/131072 at ~no VRAM cost.
--- LBD[20] ---
PIVOT (user, quality-first): PAUSE applying num_ctx/keep_alive and EVALUATE a potentially-better BASE MODEL to replace gemma4-e2b. User: "Quality is important to me. The speed... is irrelevant in front of it." Hard criteria: evaluate EXCLUSIVELY LLMs with SLIDING-WINDOW ATTENTION (SWA) — to keep the large-context-cheap property that fixes the deep-research overflow — that are (1) good at CODING, (2) good at INSTRUCTION-FOLLOWING, (3) good at REASONING. Rationale: a strong coding+reasoning agent can understand a plan/DAG, RECOVER from a bad plan, and craft genuine requirement-meeting plans — vs constantly fighting a weak model. Practical constraints: must run on native Ollama :11434 (GGUF available); WEIGHTS must fit the dedicated 6GB RTX 4050 with margin (~5-5.5GB weights max; SWA keeps KV cheap); ideally a 'thinking' model (native CoT) like e2b. NOTE: identifying CURRENT (June 2026) models is beyond the neuron's training cutoff (Jan 2026) — the eval MUST web-research current availability + coding/reasoning/instruction benchmarks, not rely on stale knowledge. The e2b num_ctx/keep_alive apply + s8 report + s9 UI-testing should run on the CHOSEN model. s5's measurement is reference; re-measure num_ctx for the chosen model (its SWA/weights differ).
--- LBD[21] ---
MODEL-EVAL criteria refined (user): (1) SWA = HARD filter (non-negotiable). (2) EXCLUDE Chinese-origin models (Qwen, DeepSeek, Yi, GLM, InternLM, MiniCPM, Baichuan, etc.) — user: "avoid Chinese ones as they are heavily influenced by their government." Western/allied families only (e.g. Mistral/Ministral=French SWA, larger Gemma=Google SWA, IBM Granite, Meta only if a variant has SWA, etc. — verify SWA per-model). (3) Eval DEPTH = thorough: research + PULL MULTIPLE candidates + TEST EACH INDEPENDENTLY on the real agent tasks, then user chooses from evidence. (4) Quality (coding+instruction-following+reasoning) outranks speed and outranks staying small — pick the best that FITS (~5-5.5GB weights in 6GB) with SWA. Pulling several multi-GB models is disk/bandwidth heavy (system was at ~89% RAM / disk busy) — authorized, but serialize and clean up pulled models that lose.
--- LBD[22] ---
MODEL-SWAP REVALIDATION CHECKLIST (for the finalize step after the user picks a model from s10). On swapping DEFAULT_MODEL away from gemma4-e2b, RE-VALIDATE the model-specific items (structural/code carries over): (1) THINKING/JSON PARSING — the transport JSON interceptor + thinking extraction were tuned to gemma (CoT in message.thinking, fenced JSON in content); the chosen model may differ (Ministral has a NON-STANDARD thinking parser per s10) — fix the interceptor + the s7 tracing thinking-capture for the new model. (2) NUM_CTX — re-run the s5-style VRAM+thinking-populated sweep for the new model's SWA/weights (32768 was e2b-specific); confirm fit on 6GB, no Shared-GPU spill, thinking stays populated on ~18k-tok deep-research prompts. (3) NUM_PREDICT — re-tune for the model's verbosity (Ministral CoT verbose) so thinking+report don't truncate (interacts with s8 report length). (4) keep_alive=-1 carries over (model-agnostic). CARRIES OVER UNTOUCHED: s2 audit, inter-node context fix (d17), UI deletes + clarify-resume wiring, DAG fallback structure, tracing infra, concise prompts + identity (sanity-check only). BROAD BEHAVIOR RE-VALIDATION (clarify-only-scheduled, no-unwanted-email, correct spec/shape selection, substantive report) happens in s9's UI testing on the chosen model. s6/s8/s9 RUN on the chosen model (not 'revisited').
--- LBD[23] ---
ADDITIONAL o4 root cause (s10/a_base baseline): SHAPE OVER-ROUTING. On gemma4-e2b, an open-ended "research X then write" request routes to the heavy DEEP-RESEARCH shape and unrolls to its 10-ROUND MAX (~51 LLM calls) — excessive for a simple report and a likely driver of the emptiness/saturation (huge multi-round accumulation overflows the window, compounding d17/d22). This is a MODEL+SELECTION behavior: e2b over-escalates despite s4's selection guidelines (d13). Implications: (a) the model comparison must score whether each candidate routes SENSIBLY (lighter shape for a simple report) vs over-escalating; a better model that routes correctly + produces substantive output is strong evidence for the swap; (b) if even the chosen model over-routes, the shape-selection prompt's anti-over-escalation guard (d13) needs strengthening, and/or the deep-research max-rounds default needs a sanity cap — fold into the finalize/s8 work. This is a THIRD o4 contributor alongside d17 (inter-node clip) and d22 (num_ctx overflow).
--- LBD[24] ---
MEASURED FIT REALITY (corrects d24's ~5-5.5GB weight budget). Ministral-8B (user's TOP pick) DISQUALIFIED ON FIT: even text-only (projector dropped, 5.20GB weights) it SPILLS to CPU/shared at EVERY context size on the 6GB RTX 4050. Insight: SWA makes the KV-cache cheap but does NOTHING for the WEIGHTS — a ~5.2GB-weight model + CUDA context + compute buffers + desktop/browser (~0.6-1GB) overflows 6GB regardless of num_ctx. So the REALISTIC weight ceiling on this 6GB card is ~4.5GB, NOT 5-5.5GB. Implication: #2 Gemma-4 E4B (4.8GB) is itself BORDERLINE (fit to be confirmed under load); #3 Ministral-3B (2GB) is safe. Field narrowed to E4B + 3B. This measured constraint should govern any future model choice: budget weights at <=~4.5GB for fully-on-GPU on this card.
--- LBD[25] ---
DAG ROBUSTNESS GAP + o4 contributor (s10/a_t2, model-agnostic): a write/synthesize node authored WITHOUT depends_on=[research node] runs DISCONNECTED from the research -> thin/empty output. This is a MISSING-REQUIRED-EDGE failure (disconnected terminal node), DISTINCT from d7's dangling-edge/phantom-node case (which parse_dag_safe already repairs). It is NON-DETERMINISTIC and happened to E4B on one task — so it is NOT an E4B quality weakness (keep E4B's comparison score fair). FIX (finalize/s8 DAG hardening, builder-owned): validate that a terminal write/synthesize node DEPENDS on the upstream research/gather node(s) for the same goal; if it doesn't, auto-add the edge or re-author (so the writer always sees the research). This is a 4th o4 contributor alongside d17 (inter-node clip), d22 (num_ctx overflow), d26 (shape over-routing). Applies regardless of chosen model.
--- LBD[26] ---
CONFIG-OPTIMIZATION SWEEP (user, quality-first; runs after s10's a_report, while all candidates are still local). Goal: find whether any CONFIG unlocks more quality on any local model, and gauge overall quality dimensions. SCOPE: (a) MODELS = all 3 local (gemma4-e2b baseline, Gemma-4 E4B candidate, Ministral-3B candidate). (b) KNOBS (full set, per user) = num_predict (e.g. 4096->8192), deep-research max_rounds (10->~3-4 cap, d26), temperature (0->~0.3 on the WRITE node only; structured steps stay 0), num_ctx (32k->64k), sampling top_p/top_k. (c) METHOD = OFAT (one-factor-at-a-time from each model's good baseline, NOT a full grid) to stay tractable; reuse run_bar.py harness verbatim where possible. (d) METRICS broadened per user 'gauge quality of response, context and recall, etc... what could benefit us overall': beyond report SUBSTANCE, score RESPONSE QUALITY (coherence/usefulness), CONTEXT HANDLING (uses provided/inter-node context), RECALL (MULTI-TURN MEMORY across the auto-compact boundary — add a multi-turn probe), INSTRUCTION-FOLLOWING (strict format), and reasoning/coding. OUTPUT: per-model best-config recommendation + an overall best (model+config) recommendation, surfaced to neuron->user for the FINAL pick (which then feeds the swap/finalize). Serialize on the single 6GB GPU; :11434 only, never :11435; keep models on disk until the user picks.
--- LBD[27] ---
RE-TRAIN the gemma specialist (spec-local-gemma-ollama-llm-engineer) with THIS RUN's measured learnings, then ASSIGN it to the future workers (s6/s8/s9 + finalize) so they apply deterministic measured settings, not guesswork. Target timing: at the FINALIZE step (right after s11 locks model+config), via update_specialist->recompile (human-gated; user requested) — so it reflects the FINAL model (E4B) + final config, not soon-stale e2b/pre-sweep values, while still being before any new workers are assigned. LEARNINGS DELTA to bake in (the current doc, compiled v1 2026-06-14, predates all of this): (1) FIT: realistic weight ceiling ~4.5GB on a 6GB card (CUDA ctx + compute buffers + desktop eat ~1.5GB), NOT 5-5.5GB; confirm fit by size_vram==size + 0% offload + no Shared-GPU spill, never param math. (2) SWA is NOT a magic checkbox: KV is cheap ONLY when the sliding window < operating ctx — gemma (1024-window) => KV nearly free (8k->32k +24MiB); mistral3 large-window 'SWA' => NO saving, KV-at-32k spills even a 2-3B model (fits only <=8192). KV is f16 regardless of weight quant. (3) NUM_CTX failure mode = window OVERFLOW (prompt tokens > num_ctx -> truncated -> EMPTY thinking -> empty output), NOT over-large windows; measure real prompt sizes (deep-research synthesize ~18k-74k tok); gemma-family sweet spot = 32768 (free to go higher via SWA). (4) NUM_PREDICT=4096 load-bearing with think=True (thinking competes with content; <=512 -> empty); long reports may need 8192. (5) KEEP_ALIVE=-1 (resident) when GPU is SOLE consumer (kills per-call reload thrash); keep_alive=0 was a shared-GPU-era hygiene. (6) THINK/CONTENT by arch: gemma => CoT in message.thinking + clean fenced JSON in content (native; fence-strip JSON interceptor at the 2 raw json.loads sites); mistral3 on Ollama => think & content MUTUALLY EXCLUSIVE (needs think-strip shim); format=json SUPPRESSES thinking (prefer prompt-JSON + interceptor when reasoning wanted). (7) HF multimodal-family GGUF tags auto-bundle an mmproj vision projector (~0.86GB dead VRAM) -> derive text-only by FROM-ing only the text model. (8) OBSERVABILITY: Phoenix is remote-only + never captures prompts/reasoning; capture LOCALLY at the transport seam (system+user prompts + thinking + tokens) via a local file SpanExporter -> markdown; wiring only takes effect on app RESTART. (9) METHODOLOGY: measure via /api/ps size_vram x nvidia-smi; OFAT config sweep with a per-call SweepTransport; score broad dims (substance, instruction-following, recall/multi-turn, context). (10) MODEL CHOICE on 6GB: Gemma-4 E4B text-only Q4 = measured best fit+quality (@32k 0% offload, native think, beats e2b on over-routing + strict format); 8B doesn't fit; mistral family disqualified (fit + shim).
--- LBD[28] ---
E4B BEHAVIOR WRINKLE (s11/a4, model-specific): on the coding task E4B intermittently ASKS FOR THE PROBLEM STATEMENT instead of solving ('Please provide the problem...') — coding=0.0 on c00 baseline + c06, NOT a scorer bug. Implication: the prompts/identity tuned for e2b do NOT perfectly transfer to E4B's interaction style — it over-asks where it should engage (an o2/o7/d10 clarify-policy concern on the new model). This is a concrete d25-REVALIDATION item: the finalize step (after the swap to E4B) must re-tune the relevant prompts (ambiguity gate / task framing / identity) for E4B so it engages directly, and re-verify the clarify-only-for-scheduled policy holds on E4B. Does NOT change the model choice (E4B still the only fit-passing upgrade beating e2b). Also a learning for the gemma specialist re-train (d31): prompt behavior is model-specific — re-validate behavior per model after a swap, never assume e2b-tuned prompts transfer.
--- LBD[29] ---
E4B PLANNING WEAKNESS (s11/a4, systematic): E4B authors a FLAT 2-node ZERO-EDGE research DAG in EVERY run (n1 research + n2 write are INDEPENDENT, no research->write dependency), so the writer never sees the research -> thin output, and context_handling scores 0.0 (no upstream edge to measure). This is the d28 missing-required-edge failure but CONSISTENT for E4B (vs intermittent for e2b). CONSEQUENCE: the d28 structural fix (validate/auto-add that a terminal write/synthesize node DEPENDS on the research/gather node(s)) is MANDATORY if we swap to E4B, not optional — it is the thing that makes E4B's report output substantive. NET TRADEOFF (for the final pick): E4B trades e2b's weaknesses (deep-research over-routing d26, loose strict-format following) for a different one (disconnected planning DAGs + occasional over-ask on coding d33) — but E4B's weaknesses are FIXED by the model-agnostic structural d28 work, while it keeps its upsides (fits @32k 0% offload, native think, better instruction-following, no over-routing). So the report-substance goal (o4) depends MORE on the structural fixes (d17 + d28) than on the model choice. Feed into the s11 synthesis + the user's final pick + the finalize plan.
--- LBD[30] ---
S11 SWEEP RESULT (durable, pre-compaction capture): E4B BEST CONFIG = temp_write=0.0 (deterministic write node), the ONE knob that mattered, REPRODUCED 3x: overall_rescored 0.928 [0.921-0.942] vs temp_write=0.3 at 0.685 [0.586-0.758] (non-overlapping bands). Corrects the earlier 'temp 0.3 helps prose' guess — for E4B determinism wins. e2b sweep: NO knob beats baseline beyond noise (baseline config already optimal). CONVERGED RECOMMENDATION going into the user's final pick + finalize: MODEL=Gemma-4 E4B text-only Q4 (only fit-passing upgrade; @32k 0% offload; native think; beats e2b on over-routing + strict-format). CONFIG=num_ctx 32768 + keep_alive=-1 + num_predict(4096, raise for long reports) + temp_write=0.0. MANDATORY structural fixes for o4 substance (matter MORE than model choice): d17 (inter-node context) + d28 (DAG connectivity — E4B authors flat zero-edge DAGs EVERY run, so auto-connect write->research is REQUIRED). Plus d33 prompt re-tune (E4B over-asks on coding). Trace evidence on disk: .config-sweep/runs-e4b/ (per-config traces), .model-eval/gemma4-e4b/RESULT.md + trace-a..d.md.
--- LBD[31] ---
SPECIALIST RE-TRAIN — PURPOSE + CONTENT enrichment (user, refines d31/d32). The re-trained spec-local-gemma-ollama-llm-engineer must give FUTURE WORK a deterministic BASELINE for HOW to invoke the model per TASK TYPE (creative vs deterministic) so workers stop GUESSING sampling params. Bake in, grounded in THIS run's live-probe benchmarks (not theory): (1) a TASK->INVOCATION mapping — deterministic/structured-output work (DAG/JSON emission, code, the 'write'/synthesize node) => temperature 0.0 (sweep: E4B temp_write=0 best, overall 0.928 vs 0.685 at temp0.3, reproduced 3x, non-overlapping bands); creative/prose generation => a higher-temp band; recall/multi-turn unaffected by temp. Note model-specificity: temp0 WON for E4B's write node but was a wash + dropped recall on e2b — so the baseline is per-model, re-validate per swap. (2) Make EXPLICIT the PINNED-BASELINE vs PER-REQUEST-HTTP split: what is BAKED into the Modelfile (num_ctx 32768, keep_alive -1, default sampling, system/identity) vs what is OVERRIDDEN per-call over the Ollama HTTP /api options at invocation time (temperature, top_p, top_k, num_predict) — i.e. pin a sane baseline but document which knobs the pipeline should set per-task over HTTP. This converts the sweep data into reusable invocation GUIDANCE, not just fit/config facts. Trigger: "re-train after the local gemma is benchmarked" (benchmarking = the s11 sweep + a6/a6b prompt-vs-defect verdict, now essentially complete). Also fold in the a6b mechanism learning: format JSON-schema constrained decoding can silently DROP a reasoned field (root cause of d33/d34) — the specialist should warn that structured-output fidelity is a first-class concern, prefer prompt-JSON + interceptor / validate-and-repair over naive format=schema where edges/fields are load-bearing.
--- LBD[32] ---
GOAL RE-SHARPENED (user, THE CORE ASK — re-centers the recipe; the model R&D was diligence, not the bottleneck). The real problem: the PROMPTS driving the Gemma pipeline are too VAGUE / low-effort, so the agent flow isn't seamless and Gemma doesn't reason+plan on its own. Fix = make the whole flow SEAMLESS and REASONING-DRIVEN, modeled on eda-base3's OWN architecture as the reference blueprint: (1) In eda-base3 the neuron + agentic-plan PROMPT FILES drive phase->phase transitions — a seed plan is created, then actions are added into plans; same shape for recipes. (2) neuron / planner / worker each live in SEPARATE shells with their OWN context window scoped to their job: neuron = own recipe + route, planner = the plan, worker = one well-defined task. (3) THE CENTRAL GAP: eda-base3 workers get well-defined tasks AND can DISCOVER their own input/output via Claude Code (read files, grep, explore). GEMMA HAS NO DISCOVERY — so the LAYER-level and NODE-TO-NODE context must be explicitly CONSTRUCTED and FED to each node. This promotes d17 from a clip-size tweak to a full context-feeding ARCHITECTURE: every node must receive the upstream outputs + scoped context it needs, assembled for it. (4) The pipeline is currently steered by many HARDCODED FLAGS that define the LLM flow; replace them so GEMMA drives reasoning + ITERATIVE planning + dispatch ON ITS OWN (d14 pure-reasoning taken all the way — iterative plan/dispatch, not flag-gated control flow, not one-shot DAG). The earlier s3 prompt pass was too low-effort; this needs a DEDICATED, thorough prompt+flow rework that STUDIES eda-base3's actual neuron + agentic-plan prompt files as the model. User framing: "a very very basic and simple ask" after many iterations — nail it. Acceptance (s8/s9) stays the same substantive-report + live-UI bar, but the CAUSE we fix is this prompt/flow/context-feeding architecture, not narrow patches.
--- LBD[33] ---
S8 ARCHITECTURE SPEC (user — the concrete design for the d38 prompt/flow/context-feeding rework). Make the Gemma agent a reasoning-driven, eda-base3-style plan->dispatch flow with THREE LLM ROLES in THREE CONTEXT WINDOWS:
(1) PLANNER (own ctx window): REASON about which SHAPE fits the task (shape-compatibility), then SELECT the INPUT specialist + PROCESSING specialist + OUTPUT specialist, then PIECE them into a DAG plan. The plan is BUILT BY CALLING EXPOSED MCP TOOLS — seed_plan, add_step, etc. (mirror eda-base3's seed-plan-then-add-actions) — i.e. the LLM WRITES the plan iteratively via tool calls, NOT a one-shot schema-constrained JSON DAG (this also sidesteps the d34 constrained-decoding edge-drop). Per node the planner records TASK + SPECIALIZATION (specs tune node behavior; e.g. an HTML spec biases toward writing the HTML file).
(2) WORKER (own ctx window): executes ONE node independently; must be FED proper context it cannot discover — the OVERALL GOAL + the PREVIOUS RELEVANT task output WHEN the node depends on it (nodes can be independent, so context is DEPENDENCY-SCOPED) — to produce quality results. This is d17 promoted from a clip to a real per-node context-assembly architecture.
(3) SYNTHESIZER (runs in the CHAT's ctx window): terminal output stage; can write to FILE (file_write MCP), EMAIL (send_mail MCP — recipient self-only lock intact, d12/security), or SSE (fallback). The PLANNER decides which synthesizer fires by DESCRIBING it in that node's task description.
The s10/s11 benchmark was necessary precisely to know which per-task settings to use where (feeds the d36 per-task invocation baseline). Validate on E4B via the s7 markdown tracing. This is the blueprint the s8 rework implements; approach it design-probe-first (diagnose current flow + propose the MCP-plan-building + per-node-context + 3-role design grounded in eda-base3's neuron/agentic-plan prompt files), then implement reviewer-gated, proven live.
--- LBD[34] ---
RE-TRAIN TIMING RESOLVED (user) — re-train the gemma specialist NOW, BEFORE the s8 build, so s8's workers inherit the benchmark-derived ruleset and "produce optimal code". This supersedes d32 ("do not re-train yet / at close") and d37 ("held pending discussion"). User: "Please update the gemma specialist with the benchmarks so that the worker produces optimal code, then we discuss the line of work before you launch the planner." TRIGGERED 2026-06-17 via update_specialist(neuron=local-gemma-ollama-llm-engineer) — SME shell spawned (specialist-local-gemma-ollama-llm-engineer-upd-5b474538) to self-train on the d31 delta + d36 per-task invocation baseline + the prompt-vs-defect/structured-output-fidelity verdict + d35 E4B config, submit for review, notify on completion. Recompile stays HUMAN-GATED. SEQUENCE NOW: (1) re-train completes + review/recompile, (2) DISCUSS the s8 line of work with the user, (3) THEN launch the s8 planner — do NOT launch before both the re-train lands and the user signs off on the line of work. If s6/s8/s9 later surface more learnings, a light top-up re-train at close is still allowed (d31's "once finalized" spirit), but the primary re-train is NOW.
--- LBD[35] ---
S8 LINE-OF-WORK (neuron's call, user-delegated "take your call on your own planning"). (1) DECOMPOSITION = ONE step (s8), ONE planner, DESIGN-PROBE-FIRST; the planner decomposes into worker actions AFTER the read-only probe reveals the real seams — do NOT pre-fragment the design at the neuron level before diagnosis. (2) s6 FOLDED INTO s8 (s6 deleted as a separate step): shape-fit reasoning + per-node spec assignment are core to the s8 architecture, and s6's "linear plus modular parallel -> flat sequential DAG" bug is the SAME constrained-decoding/edge-drop class as d34. So shape/spec FREE-FLOWING ITERATIVE authoring (edit/refine existing, not one-shot — d18) + the shape-intent fix are done WITHIN s8's rework, holistically with the shapes/specs the planner consumes. (3) FLAG REMOVAL = INCREMENTAL migration — keep the LIVE app runnable at every step; remove each flow-flag only as its reasoning-driven replacement proves out via markdown tracing (live user state; user validates live in s9; big-bang too risky). LAUNCH GATE: the s8 planner launch waits on the d40 re-train COMPLETING + recompiled, so Phase-2 implementation workers inherit the new gemma ruleset (Phase-1 probe is read-only and could run earlier, but per the user's stated order we hold the launch until the specialist is updated). User delegated this planning to the neuron; surfaced for veto.
--- LBD[36] ---
S8 b1 RESULT + ENV FACT (s8/b1 flowback). (1) PROVEN MILESTONE: tool-driven plan authoring KILLS the d34 edge-drop on REAL E4B — b1 replaced IncrementalPlanner's per-node format-schema-CONSTRAINED authoring with a tool-call loop (new agent_runtime module), so the planner builds the DAG via discrete tool calls (seed_plan/add_step-style) instead of one-shot schema JSON. This validates the d39 core mechanism in implementation, not just the confirmation run. (2) ENV FACT (load-bearing for b8 + live testing): the shipped transport DEFAULT_MODEL 'gemma4-e2b-agent' is GONE from Ollama :11434 — only E4B remains (gemma4-e4b-candidate-ctx32k:latest, caps=tools+thinking; plus batiai/gemma4-e4b base). CONSEQUENCE: the app cannot run on its configured default as-is; the E4B swap (action b8, with d25 revalidation) is now a HARD PREREQUISITE for the app to function, not merely preferred, and all live validation (s9) runs on E4B. b8 must update DEFAULT_MODEL (transport.py:105) to the E4B tag. (Model cleanup happened during s10/s11 model-eval; consistent with E4B being the pick.)
--- LBD[37] ---
LIVE TESTING DRIVEN BY THE NEURON (user; refines d4). User: "please drive the testing via ui to catch ui + behavior and chat like a human." The NEURON drives the s9/b10 live acceptance by CHATTING LIKE A HUMAN against the running app (multi-turn, realistic prompts), catching BOTH UI and BEHAVIOR issues, and looping fixes with the planner/workers as it finds them — rather than only the user running the browser. Method: drive the chat via the app's HTTP/SSE chat endpoints + read the s7 markdown traces to verify each scenario (reasoning visibly ON; clarify only for scheduled tasks, no re-ask loop; no unrequested email; substantive US-Iran HTML report as real HTML d43; correct spec/shape selection; shape/spec free-flow edit/refine; multi-round context holds; delete buttons). HARD PREREQUISITE: the app must be RUNNABLE first — DEFAULT_MODEL swapped to E4B (b8) + app restarted on E4B (e2b is gone, d42) + s7 tracing wiring active. So neuron-driven testing starts once b8 lands; ideally after b6-b9 so all features are present. CAVEAT: pure VISUAL rendering (pixels/layout) still benefits from a human glance — the neuron tests behavior + served endpoints/DOM thoroughly and flags anything needing the user's eyes; user does the final sign-off (d4 acceptance bar unchanged).
--- LBD[38] ---
CORE REQUIREMENT (user, re-sharpens d38/d39 — the heart of the goal). The small LLM (E4B) must drive the WHOLE flow itself via REASONING + MCP TOOLS — NOT format=schema constrained JSON, NOT hard-coded control flags, NOT code-side templates. s8 applied the d39 tool-call/reasoning remedy ONLY to the DAG planner; it MUST extend to the shape selector, spec selection, the synthesizer, and file output (the RCA's reason!=emit seams). Specifics: (1) SHAPES & SPECS ARE SELF-AUTHORED BY THE LLM — user describes intent in plain language at the Shapes/Specs UI; the LLM writes the condensed shape/spec file itself (sequential/parallel/modular OR a novel shape never seen). Seed 1-2 at start, but thereafter the LLM authors new ones ON ITS OWN; neuron/devs do NOT keep creating shapes/specs for the user. SPEC = how the LLM BEHAVES (tone/ruleset, e.g. pirate), not what it thinks. (2) CHAT uses shape+spec to answer; when the user FLAGS something the LLM ADAPTS (e.g. 'small brief' -> 'deep thorough report' -> writes detailed) with NO code change per request. (3) OUTPUT format/filename/depth are LLM-DECIDED via the MCP tools — the file_write tool already takes a filename, so REMOVE the _ensure_md hard-code (toolargs.py) + the literal report.md _artifacts_for (agentic.py) that override the LLM; let the LLM emit .html/.md/.txt/.csv/etc. NO proliferating code templates for styles/formats. (4) SELECTION (shape/spec) reasoning/tool-driven, not a format=schema enum sample. INVARIANTS (the ONLY guarantees): ~99% accuracy/quality (response not lacklustre) + context correctly passed through every node; NO fabricated/hard-coded behavior. ACCEPTANCE (LLM does it unaided, reliably): headlines->headlines not paragraphs; 'detailed HTML report'->deep research (or equiv) then HTML; 'mermaid diagram in md'->renders with NO compile errors; txt->txt; csv->csv; pirate tone-spec + 'pirate reply in HTML in detail'->deep research + HTML in that tone. The 6 audited flags' END-STATE is removal (migrate reliably, deferral was sequencing not permanence). RE-FRAMES the s9 Stage-B fixes: they are REMOVALS toward LLM-tool-driven (drop format=schema selection; drop the .md hard-codes; raw synthesizer), NOT anchoring/sniffing/templating.
--- LBD[39] ---
AGENTIC MCP-TOOL AUTHORING for shapes + specs (user; sharpens d46). The LLM must author BOTH a shape (a DAG) AND a spec via DEDICATED MCP tools designed for that purpose — an agentic workflow that ITERATES then FREEZES + COMPILES a ruleset, exactly parallel to the planner's seed_plan->add_step decomposition. We do NOT constrain with schemas; we make the PROMPTS good enough. Hard constraints: (1) shapes AND specs are NODE INPUT, so they must be SHORT and PRECISE (condensed, not verbose docs); (2) the SHAPE authoring flow must be authored KNOWING THE PLANNER CONSUMES IT and HOW — a shape = condensed guidance for the planner's DAG construction, written for that consumer; (3) a single NODE may follow 1 OR MORE specs as guidelines (multi-spec per node) — currently each node carries a single default_spec (c0/B0 finding), so this is NEW capability. Scope impact: expands B2 (spec selection -> support multi-spec per node) and B4 (authoring -> build the dedicated iterate/freeze/compile MCP tools for shape-DAG + spec, with planner-aware short-precise output). The c0 probe already confirmed the authoring MACHINERY largely exists (ShapeAuthor/SpecConversation author+refine from free-text, prompt-JSON not format=schema) — this decision recasts B4 from 'close small gaps' to 'expose that authoring as proper agentic MCP tools + add multi-spec-per-node + ensure short/precise planner-aware output'.
--- LBD[40] ---
DEFINITIVE AGENT ARCHITECTURE (user re-grounding; resolves the open roles question + Q-A, refines d39/d46/d47). THREE roles only — the 6 node-role enum {research,critic,worker,reviewer,synthesis,verify} COLLAPSES into these; node behavior comes from SPECS, not a role-switch: (1) PLANNER — creates the plan as a combination of a SHAPE + SPECS; a node can carry ONE OR MORE specs. (2) WORKER (a node) — given general guidelines on how to follow its one-or-more specs to achieve its goal; has a defined INPUT and OUTPUT. (3) SYNTHESIZER — writes the output to chat: via SSE, OR send_mail, OR write-file-as-attachment, with supporting text in the chat for the mail/file flows. FLOW: the SHAPE is whatever the LLM chooses; the plan DAG drives; the DAG launches each worker with the spec(s) the LLM chose; the agent passes data correctly between nodes. HARD PRINCIPLE: we do NOT fabricate behavior by circumventing (no app-side hard-coding/flags forcing the flow). The lever is PROMPTING — role prompts must be good enough, AND the specialization + shape authoring prompts must be good enough so RELIABLE artifacts are generated and passed to the actual chat where they are used. UI: shape-authoring and spec(specialization)-authoring are a CHAT WINDOW EACH, with their own separate concerns. This makes Q-A concrete (BOUNDED: 3 roles, LLM composes shape+specs within) and resolves roles-vs-specs (collapse to specs+reasoning). Q-C (seed count) stays at the working default (keep built-ins; non-blocking, minor).
--- LBD[41] ---
SYNTHESIZER / FILE-WRITE RELIABILITY = PLANNER-IN-THE-LOOP + REAL FILE READ/WRITE (user; reframes the B1 REVISE, REPLACES the neuron's A/B 'single-emission vs forced-section' framing which was wrong). Output is NOT HTML-only; the fix is NOT an in-node emission strategy. The elegant solution uses shapes+specs + the planner orchestrating, mirroring how the NEURON maintains a recipe: (1) DECOMPOSE THE WRITE TOOL into composed steps — write = write(filename) + write(content). The 'stepwise tool-calling' the user asked for means composing the write OPERATION as filename-then-content, NOT a section-loop inside one synthesis call. (2) PLANNER-IN-THE-LOOP via ReAct: keep the planner in the loop so it ACTS ON EMISSIONS and drives the flow (react -> observe what was written -> dispatch the next write step -> repeat until complete) — exactly like the neuron's react->reconcile->next_action loop over this recipe. The reliability/continuation loop lives in PLANNER ORCHESTRATION, not special node code. (3) GEMMA USES file_read + file_write THE WAY THE NEURON DOES: write the file, then READ IT BACK to see the ACTUAL current state (not the model's imagined memory) and continue/append. This is the elegant kill for BOTH the false-finish (model believing it already wrote everything) AND truncation — the model checks reality instead of its memory. (4) MULTI-PAGE / LARGE OUTPUTS via PLAN CHAINING: chain two plans — plan 1 = research, plan 2 = write-file — where the write-file plan is ITSELF a shape (linear OR parallel) whose nodes fill the remaining pages/sections of the file. Decomposition lives in the shape/DAG, not in code. (5) OPTIMIZE THE file_read TOOL if necessary to support this loop (offset/range/tail/length so checking current file state is cheap). PRINCIPLE: shapes+specs exist to tackle exactly these problems with a simple elegant solution — do NOT hard-code a reliability loop (that would be circumvention per d48); use the DAG + planner-react + real file read/write. This subsumes c1r's reviewer fixes: 'kill false-finish' = read the actual file; 'loop the completeness guard' = planner reacting on emissions; 'handle long docs' = plan-chaining + per-page nodes.
--- LBD[42] ---
TRUE AGENTIC EXPERIENCE + KILL THE JSON MANDATE AT ITS SOURCE (user, emphatic re-grounding; root-causes the recurring JSON and sets the architecture). (1) ROOT CAUSE of recurring JSON output: the SPECIALIZATION instructs workers to make LLM calls return PYDANTIC / structured (JSON) outputs. That is why, despite the standing decision to remove JSON output, workers keep regenerating format=schema / pydantic-constrained calls. THE FIX IS AT THE SOURCE: remove the pydantic/structured-output mandate from the specialization for this small-model agentic context (durable; ties d31/d36 specialist re-train) AND stop following it in the current build. No per-node patching will hold while the specialization re-injects it. (2) ARCHITECTURE = TRUE AGENTIC LOOP: each worker NODE is an AGENT. It is handed its SHAPE (plan context), its SPEC(s) (behavior guidelines), and the TOOLS, and it USES them ITERATIVELY to complete its task: reason -> call a tool -> observe the REAL result -> reason -> call the next tool -> ... -> done. It is NOT a single tool call done in one node, and NOT the planner doing the work. The planner COMPOSES the plan (shape+specs); the node-agent EXECUTES via an agentic tool-use loop. (3) CONTENT IS RAW, never escaped into JSON, on ANY route (synthesis OR acyclic tool path) or any node: tool invocations carry only lightweight args (filename, a done marker); the document/content is emitted and handled as RAW text and written via tools across iterations. No content-in-JSON, no format=schema for content, no pydantic-wrapped content. (4) This is the heart of d46/d49: the agent uses shapes+specs+tools to do real agentic work; we invest in prompt quality + the agentic loop, NOT in serializing the model through a JSON format it cannot reliably produce. SUPERSEDES the neuron's earlier 'orchestration makes the file_write calls because the model cannot emit tool calls' framing where that implied a single-shot orchestration call rather than an agent looping with tools.
--- LBD[43] ---
d50.1 RESOLVED (c7 spec audit; .s9_probe/spec_audit_pydantic.md). The pydantic/structured/format=schema CONTENT mandate is localized ENTIRELY to spec-local-gemma-ollama-llm-engineer (v34); spec-universal is CLEAN. ROOT MECHANISM: spec entries are APPEND-ONLY, so the 2026-06-17 'supersession' could not delete the original JSON-mandating entries — BOTH the old mandate and its half-fix remain ENFORCED on every assemble_ruleset, which is why per-node JSON removal never held. 6 offenders identified; the clearest (Offender 6) is that the 2026-06-17 fix moved STRUCTURE (DAG/selection) off format=schema onto tool-calls/prompt-JSON but explicitly left 'synthesis' (=content/document) in the prompt-JSON bucket — so the deliverable was relabeled, not removed from JSON. LEGITIMATE/KEEP: plan structure via discrete tool calls; format=schema for SMALL FLAT NON-reasoned control flags only; pydantic on lightweight tool-arg/control objects; all think/num_ctx/num_predict/VRAM/sampling/observability rules. DURABLE FIX (HUMAN-GATED; neuron routes; fold into the d31/d36/d40 single close re-train): append a NEW superseding entry to the gemma spec stating CONTENT IS RAW, NEVER SERIALIZED (supersedes Offenders 1,3 + the 'synthesis' clause of 6; content = raw text written via stepwise write(filename)+write(content); never JSON/pydantic/format=schema for content on any route; 'structured output' = ONLY plan-structure-via-tool-calls + flat-control-flags-via-schema), then recompile the specialist doc + re-pin base session (avoid the served-doc-stale trap). INTERIM RULE active NOW for this app's workers (suspends the spec's 'synthesis->prompt-JSON' line; build content RAW; structure via tool calls; schema only for flat flags) — c1c honors it. The append-only-spec insight ties [[spec-entries-append-only-doc-can-go-stale]].
--- LBD[44] ---
d71 — N5r = CONFIRM (.s9_probe/review_N5.md). The N5 no-fabrication REASONING verification lane is proven LIVE on resident E4B: all 6 gates pass. E4B's OWN reasoning flagged the exact c13r B2 fabrication (17 USC 107(5)/CTEA-1998) PLUS a NOVEL fabricated UNSC Resolution 2799 (d58 .gov stress) as unbacked and ground-removed them, while keeping every backed claim+URL; a fully-backed report returned byte-unchanged (no false strip); the d66 0-fetch answered-from-memory case was CAUGHT and forced to gather real sources, with NO false-fire when E4B gathered on its own; verification is REASONING not regex (caught fabrications whose strings are absent from the code); 279+14 tests green; default-OFF ships byte-identical. This completes the N1-N5 increment chain: N1 breadth, N2 ArticleNote, N3 chunked-read, N4 tree+prune+decision (the LIVE DEFAULT per d70), N5 verification — ALL built, LIVE-proven, and reviewer-CONFIRMED. Remaining: N6d design-probe (SURFACE to user before build) -> N6 -> N6r -> cFINAL.
--- LBD[45] ---
d72 — USER APPROVED the N6 orchestration design (n6_design.md), verbatim: "Approve — build N6 as designed". Satisfies the design-probe-first mandate (d62). SCOPE of N6: the four unconditional verify_lane=True edits in §3.1 (new _build_acyclic_runtime verify_lane param, default-False ONLY as the short/headlines/csv byte-identical no-regression contract; passed =True in code at run_plan_chain PHASE-1, _make_tree_gather leaf, and run_section_write_phase write_runtime). KEY N6d FINDING that re-scoped N6: N1 breadth + N2 ArticleNote + N3 chunked-read + N4 tree are ALREADY wired default-ON onto BOTH deliverable routes by the earlier N4w leg — so the N1r "wire run_plan_chain to breadth" finding is already structurally resolved; the SOLE remaining gap was N5 verify_lane (zero refs in chat_app/agentic.py = built but DARK). No env/UI flag anywhere (d65 end-state: served route grounds+verifies by default in code). After build: N6r + cFINAL must prove LIVE on the served route (Seam A 0-fetch forced-gather, Seam B unbacked-claim ground-or-remove, N2 note-faithfulness via N2r_legprobe to completion, canonical US-Iran HTML report end-to-end with 0 fabricated URLs, full suites green + short paths byte-identical, all with NO flags flipped).
--- LBD[46] ---
d73 — USER (2026-06-21) added a NEW REQUIREMENT: an INDEPENDENT verification planner to run AFTER the current s9 planner closes (after N6/N6r/cFINAL), for an unbiased end-to-end check. Framing: quality matters even though the model is small; test variance is understood but is NOT an excuse for poor quality. Verification scope: (1) CONTEXT-WINDOW AUDIT — user observed "a lot of code changes setting context to 16k even though 32k fits" and is NOT sure 16k is actually set; determine (a) whether 16k/16384 is actually ACTIVE and exactly where, (b) the ideology behind a 16k-vs-32k choice (s9 probe reports .s9_probe/*.md should give a clue), (c) whether 32k should be used since it fits. (2) Verify ALL the code changes. (3) Verify the UI spec + shape changes AND the model's quality at producing them NATURALLY (no flags). (4) Run the variant test cases and judge real chat-response quality. (5) MOST IMPORTANT: confirm our research technique works and is AS GOOD AS OR BETTER THAN the ChatGPT-described research methodology the user shared (https://chatgpt.com/share/6a37d976-6d20-83ee-b569-15a79c9c202e). Must be a FRESH independent planner (not s9) so the verification is not self-graded. NOTE: if the 16k finding turns out to be a real defect on the served path it may be foldable into the in-flight N6/cFINAL work rather than waiting for the post-close planner — TBD after the read-only recon.
--- LBD[47] ---
d74 — RECON on the d73 16k concern (read-only Explore agent, static code analysis). FINDING: 16384 is NOT set anywhere on the served deep-research/report path — it is HISTORICAL/superseded. Current code runs num_ctx=32768 system-wide on deep-research: DEEP_RESEARCH_NUM_CTX defaults 32768 (chat_app/agentic.py:131, env RA_RESEARCH_NUM_CTX default 32768); TreeConfig.num_ctx default 32768 (agent_runtime/research_tree.py:87, from_env :104, clamp [8192,131072]); tree leaf passes config.num_ctx=32768 (agentic.py:1102). The 16384 the user saw is in a CODE COMMENT (agentic.py:118-130) documenting that the two deep-research call sites USED to run at 16384 and were RAISED to 32768 because at breadth=10 the re-accumulating prompt overflowed 16384 -> truncation -> empty/thin output (d22 regime); 32768 fits safely on the 6GB card under E4B SWA (8k->32k ≈ +24MiB). DISTINCT: chunked-read (N3) uses DEFAULT_CHUNK_CHARS=8000 (chunked_read.py:31) — a read CHUNK size, not the model context window, and it is 8000 not 16384. So the changes the user saw move AWAY from 16k toward 32k (the quality-correct direction). CAVEAT: static-code only; the only way runtime could be 16k is an explicit env RA_RESEARCH_NUM_CTX=16384 — s12 LIVE audit must confirm the actual resident-runtime num_ctx. ChatGPT ref (d73 link) could NOT be fetched (client-rendered/login-walled, WebFetch returned only nav chrome) — awaiting user paste of the methodology text; do NOT fabricate its contents. Nothing urgent to fold into cFINAL (16k was already a non-issue).
--- LBD[48] ---
d75 — RESOLVES the d73/d74 ChatGPT-benchmark item. User (2026-06-21) confirmed the shared ChatGPT methodology was GENERAL-PURPOSE and that the neuron's synthesized deep-research methodology captures what they wanted ("That was the chatgpt methodology... I liked some of the things that you picked up"). So the ChatGPT share link need NOT be fetched (it was login-walled anyway). NEW REFERENCE STANDARD for the s12 "research technique is >= the reference methodology" gate = the neuron's synthesized blueprint: the 10-step deep-research loop (decompose -> query-expand -> breadth search -> concurrent fetch+extract -> embedding rank/rerank -> map-reduce read of long docs -> per-source structured cited notes -> tree decide/expand/prune -> reasoning verification pass -> synthesize with verbatim citations) PLUS the local-LLM techniques/gotchas (summarize-to-notes before reasoning due to small context; prompt-JSON tool calls over native function-calling; structure must drive per-node fetching or the model answers ungrounded from memory; reasoning-not-regex claim verification to kill fabricated citations; verbatim real-URL citations; source trust-tiering; num_ctx sized to fetch breadth). s12 must show our served pipeline meets-or-exceeds these. NOTE: our system already implements the core of this (N1 breadth, N2 cited ArticleNotes, N3 chunked/map-reduce read, N4 tree+prune+decision, N5 reasoning no-fab verification) — s12 verifies it holds LIVE on the served route, not just in probes.
--- LBD[49] ---
d76 — RESEARCH MEMORY STRUCTURE map + assessment (read-only Explore, user-requested "maybe it helps?"). The persisted research-memory model: ResearchState = append-only JSONL on disk, one record per leaf {layer, branch_id, question, findings_digest(1200ch), notes[], fetched_count}, READ BACK by the decision node = single source of truth (research_tree.py:304-379). ArticleNote (article_note.py:96-122) = RUNTIME-OWNED provenance {source_id,url,title} + MODEL-OWNED reasoning {trust_tier,summary,key_claims[],relevance,gaps_or_followups[]}; gaps_or_followups drives tree expansion. Tree: Branch{id,parent,question,rationale,depth}; Tree{branches,pruned[{target,reason}],next_direction}. LeafResult{branch_id,question,findings(raw),notes[],fetched[{title,url,markdown,summary?}]}. Synthesis contract = (findings:str, sources:[{title,url,markdown}]) deduped by URL with stable 1-based global ids. TWO STANDOUT STRENGTHS (the anti-fabrication backbone at the data-structure level): (1) provenance runtime-owned not model-authored — model can't invent a citation; (2) state persisted-and-read-back so the decision node reasons over actually-gathered state, not hallucinated recollection. GAPS surfaced (candidate improvements, NOT yet committed scope — user to direct): QUALITY: (a) NO claim->source linking — findings prose and notes ride parallel with no per-claim source id (biggest opportunity: per-claim traceability beyond N5's report-level grounding); (b) only findings_digest(1200ch) persisted, full findings not re-queryable by later layers; (c) no cross-layer URL dedup (same URL -> duplicate notes). OBSERVABILITY: pruned keeps only {target,reason} (no discarded-content trace); chunked-read summary carries no chunk-boundary metadata; decision-node reasoning not persisted (only final prose). These feed the s12 quality review and/or a post-close improvement backlog and the user's potential standalone-tool blueprint.
--- LBD[50] ---
d77 — USER (2026-06-21) set the SEQUENCING + deferral, resolving the open d76 provenance-gap question. ORDER: (1) let N6r complete; (2) then run the next step = LIVE UI VERIFICATION (cFINAL — both d46 scenarios incl. d58 legal/.gov on the FILE-SAVE path, >=2 fresh runs, DEFAULT config no flags); (3) then close s9; (4) then s12 = the fresh INDEPENDENT UI verification (d73 scope: context-window LIVE audit, all code/UI-spec/shape changes, variant test cases, research-technique >= the d75 reference methodology). ONLY AFTER everything currently DEFINED is verified and proven LIVE do we consider ENHANCING the deep-research technique — via additional tools OR research-memory improvements (incl. the d76 claim->source per-claim provenance + cross-layer dedup). So d76 = BACKLOG-AFTER-VERIFICATION (NOT escalate-now, NOT folded into the in-flight N6r/cFINAL). Verification-first; enhancement-second. Nothing new dispatched now — N6r->cFINAL->close->s12 already covers the verification chain.
--- LBD[51] ---
d78 — USER (2026-06-21) HARD quality bar + MODEL-SWAP contingency for cFINAL. The US-Iran "detailed HTML report" must be SUBSTANTIVE — real depth across multiple sections (headlines/timeline/damage figures/analysis/sources), NOT "just a few paragraphs". A few-paragraph report = FAIL, never massaged into a pass. This is the explicit make-or-break test of the whole N1-N5 redesign, which existed precisely to beat E4B's d53/d54 long-path thin/placeholder synthesis ceiling. CONTINGENCY: if E4B STILL cannot produce a good substantive report after the redesign + N5 grounding, ESCALATE to evaluating/swapping a MORE-CAPABLE small model — reuse the existing framework: d24 model-eval criteria (SWA = HARD filter, 6GB RTX-4050 fit) + d27 fit reality + d25 MODEL-SWAP REVALIDATION CHECKLIST. cFINAL must judge report DEPTH honestly and, if thin, report it as an E4B-ceiling -> model-swap trigger (surface to neuron->user), NOT a pass. Sequencing unchanged (d77): this is a cFINAL acceptance criterion; enhancements still wait until after full verification.
--- LBD[52] ---
d79 — N6r = REVISE (.s9_probe/review_N6.md). N6's 4 verify_lane edits are EXACT+correct, NO regression (385 passed), and Seam B FIRES live on the served write route: E4B's OWN reasoning flagged 9 unbacked claims (a fabricated 2002/JCPOA/2019 timeline absent from the provided June-2025 sources) with specific reasons (reasoning-not-regex CONFIRMED), producing a single well-formed 14.8KB multi-section NAV SPA. BUT two LIVE outcome gates FAIL on E4B (model-ceiling / pre-existing N5-design limits, NOT N6 wiring defects): R1 (PRIMARY, HARD gate per review_N2 'no silent drop') — N2 ArticleNote faithfulness UNMET: designated N2r_legprobe captured 0 structured notes x2 attempts; E4B treats the `note` tool as OPTIONAL — fetched 1/3 sources, wrote findings directly, never called `note`; note-shaped text leaked into findings (control-not-content fail). Caveat: harness drives ONE overloaded node; the served route authors MULTIPLE focused nodes which MIGHT elicit notes — not re-measured (R3). R2 (SECONDARY) — Seam B is DETECT-ONLY on large reports: a >9000-char report can't be whole-doc-revised in one E4B num_predict=4096 turn; min_retention_ratio=0.5 floor rejects the truncated rewrite, and _VERIFY_REVISE_MAX_CHARS=9000 (runtime.py:160,2087) routes big docs to surface-don't-repersist — so detected fabrications (incl literal `[Source URL Placeholder]` citations) STAY in the delivered report, breaking the d60 0-fabrication goal on the deliverable that matters most. R3 (COVERAGE) — NO clean canonical end-to-end obtained: E4B breadth-10 tree ran ~65min still in research phase (no write); Seam B proven directly, research-half rests on prior N1r/N4r. Reviewer recs: R1=force a note per fetched source (structural backstop mirroring Seam A gather-more) OR re-measure served multi-node; R2=move grounding to SECTION-SCOPED per-page verify inside the write loop (each section <9000ch revises cleanly) — pure engineering, model-INDEPENDENT, fits existing per-section arch — + drop placeholder-citation habit; R3=one uninterrupted reduced-breadth canonical end-to-end. INTERSECTS d78: R1 partly E4B note-emission behavior; R2 a fixable design limit benefiting ANY model. We have NOT yet seen a full canonical US-Iran report end-to-end. Surfacing fix-first-vs-swap-now decision to user.
--- LBD[53] ---
d80 — USER (2026-06-21) chose MODEL-SWAP over more E4B fixes (resolves d79 fork; triggers d78). "Enough working with the 32k context window if we can't do anything in it. We need a genuinely good reasoning model that we can run locally. We have the harness. Let's test the alternatives." MANDATE for the model-swap evaluation: (1) GOAL = a genuinely good local REASONING model that fits the machine (RTX 4050 6GB + system RAM) and fixes the E4B failures — reliable TOOL-USE (emits the `note` tool per source = the R1 failure, + tree expand/prune) and SUBSTANTIVE multi-section report synthesis (d78, not a few paragraphs), under the N5 no-fab verifier. (2) HARD LESSONS as selection filters: SWA (sliding-window attention, e.g. Gemma) is BAD for this -> AVOID SWA / prefer FULL-attention models; "latest isn't necessarily good" -> do NOT assume newest wins; CHECK PUBLISHED BENCHMARKS for BOTH new AND old models before choosing (reasoning: MMLU-Pro/GPQA, tool-use/IFEval, etc.). (3) CANDIDATES: user-named = Qwen (Qwen2.5 family) + DeepSeek-R1 distilled (Distill-Qwen-7B/14B, Distill-Llama-8B); PLUS other capable models — test UP TO 10 that fit + are good (e.g. Phi-4 dense, Mistral-Nemo-12B, Llama-3.1-8B, GLM-4-9B, InternLM2.5, Yi-1.5, Granite-3 — verify non-SWA + 6GB fit + benchmarks). (4) REUSE THE EXISTING HARNESS (.s9_probe DD tree-authoring probe, N2r note-faithfulness legprobe, N5r verify probe, the US-Iran + d58 legal/.gov acceptance scenarios) to measure each candidate on tool-use reliability, reasoning, report depth, no-fab. (5) PROCESS: benchmark-research FIRST -> propose a shortlist (surface to neuron->user before pulling 10 models) -> pull -> harness-test each -> score -> recommend a winner to user (d25 revalidation checklist). (6) OPS/CLEANUP (user: "clean up the models afterwards"): native Ollama :11434 ONLY, NEVER :11435/foreign PIDs/models; pull+test+`ollama rm` the candidate models to reclaim disk; only ONE model resident at a time on the 6GB card; KEEP E4B until a replacement is selected AND revalidated (don't delete the working model first). After the swap, the model-INDEPENDENT R2 per-section verify fix (d79) is still needed on the chosen model; R1 note-emission may resolve if the new model uses tools reliably. Sequencing: this runs under s9 BEFORE close; s12 independent verification still follows on the NEW model's system.
--- LBD[54] ---
d81 — MS1 benchmark shortlist (.s9_probe/model_swap_shortlist.md, READ-ONLY, no models pulled). DECISIVE FINDINGS: (1) 6GB-native fit is the hard gate — only 7-8B at Q4 fit natively (fast); 12-14B (Qwen2.5-14B, Mistral-Nemo-12B, R1-Distill-14B) need partial CPU offload (slower); Phi-4 14B = best reasoning scores (MMLU 84.8/GPQA 56.1) but NO Ollama tools tag + IFEval 63 (weak instruction-following) + no fit -> WRONG for a tool-driven agent. (2) REASONING<->TOOL-USE TRADE-OFF: DeepSeek-R1 distills have the best native-fit reasoning (R1-Distill-Qwen-7B MATH-500 92.8/GPQA 49.1) but DOCUMENTED-UNSTABLE tool-calling (emit tool call into content field — the SAME class as E4B's note-skip R1 failure); Qwen2.5 + Llama-3.1 have reliable NATIVE Ollama tool templates but need CoT prompting for reasoning lift. RANKED: #1 Qwen2.5-7B (✅native, ✅reliable tools, good synthesis — RECOMMENDED lowest-risk default), #2 Qwen2.5-14B (offload, best synthesis), #3 R1-Distill-Qwen-7B (✅native, ★reasoning, ⚠️tool-unstable), #4 Llama-3.1-8B (✅native, ✅tools, weaker reasoning), #5 R1-Distill-Qwen-14B (offload+tool-unstable), #6 R1-0528-Qwen3-8B (native, unverified scores), #7 GLM-4-9B (marginal fit, tools-tag unconfirmed), #8 Mistral-Nemo-12B (offload, full-attn, mid reasoning), #9 Phi-4 (no fit/no tools), #10 InternLM2.5-7B (native, agent-oriented, thin Ollama support). All verified NON-SWA full-attention (d80 filter held; top pick Qwen2.5-7B is a 2024 model = latest!=best honored). HONESTY: some 7B GPQA/MMLU-Pro/IFEval + 0528-Qwen3-8B + GLM-4/Mistral-Nemo exact numbers left UNSTATED not guessed; VRAM fit = computed estimate, confirm live; GLM-4/InternLM2.5 tools-tag unconfirmed. MS1 rec: pull qwen2.5:7b first; reasoning-max-native = deepseek-r1:7b (needs hardened tool/JSON parser); qwen2.5:14b if offload latency acceptable. Awaiting user pick of TEST SCOPE before pulls.
--- LBD[55] ---
d82 — USER (2026-06-21) set the model-test scope + selection rules. (1) TEST SCOPE = top 2 Chinese + top 2 non-Chinese, head-to-head empirical. Proposed set (pending user confirm of the top-10 list): Chinese = qwen2.5:7b (#1) + deepseek-r1:7b (R1-Distill-Qwen-7B, #3); non-Chinese = llama3.1:8b (#4) + [mistral-nemo:12b (#8) OR phi4 (#9)] — open sub-choice surfaced to user (Mistral-Nemo = reliable tools+offload+mid-reasoning; Phi-4 = top reasoning but NO Ollama tools tag + no native fit). NOTE: non-Chinese field is thin — only Llama-3.1-8B is both native-fit AND a reliable tool-caller. (2) PER-MODEL PARAM TUNING (HARD): tune each candidate to ITS OWN official recommendations — chat template, temperature (e.g. DeepSeek-R1 ~0.6 + think=True; Qwen/Llama temp 0 for structured determinism), think/CoT mode, num_ctx within the measured 6GB fit, quant — a FAIR comparison, NOT E4B's params applied blindly. (3) TIEBREAK = prefer NON-CHINESE if results are par/comparable (data-sovereignty preference). (4) COMPARE results once tests done -> surface a head-to-head comparison for the user to pick the winner. ORIGINS (top 10): Chinese = Qwen2.5 (Alibaba), DeepSeek-R1 distills, GLM-4 (Zhipu), InternLM2.5 (Shanghai AI Lab); non-Chinese = Llama-3.1 (Meta US), Phi-4 (Microsoft US), Mistral-Nemo (Mistral FR). HOLD all pulls until the user confirms the top-10 list + the 4-model selection (user asked to see the top 10 before proceeding).
--- LBD[56] ---
d83 — USER (2026-06-21) finalized the MS2 model-test config. SET = GPU-NATIVE-FIT ONLY (offload models DROPPED after the offload-latency discussion: ~3-7 tok/s on 14B + long CoT × many calls = hours/report). The 4: qwen2.5:7b (CN/Alibaba, balanced native tools), deepseek-r1:7b (CN/DeepSeek R1-Distill-Qwen-7B, reasoning-max, tool-unstable→hardened parser), deepseek-r1:8b (CN/DeepSeek R1-0528-Qwen3-8B, newer distill), llama3.1:8b (US/Meta, reliable native tools, the SOLE native-fit non-Chinese). InternLM2.5-7B + GLM-4-9B available if user adds (unconfirmed tool tags). HARNESS CONFIG = SHORT iterations: 3 research nodes × breadth 3 (~9 fetches) via the tuneable knobs (RA_RESEARCH_FETCH_BREADTH=3, reduced tree depth/fan-out) — a fast COMPARATIVE SCREEN, not the final full-depth report (winner later gets a full run). SCENARIO = the same canonical US-Iran war report (detailed HTML). EVAL per model (TWO lenses the user named + the levers): (a) FACTUAL CHECKS — is the report content actually CORRECT/accurate (cross-check key claims vs the fetched sources AND known facts), (b) NO HALLUCINATION — every claim traces to a fetched source, no fabricated facts/citations (N5 verify + manual), (c) REPORT DEPTH/substance d78, (d) TOOL-USE reliability — does it emit the `note` tool per source (the R1 lever), (e) SPEED (tok/s + wall-time). PER-MODEL FAIR TUNING (d82) each to its own official recs. NON-CHINESE TIEBREAK (d82): prefer llama3.1 if par. OPS: native :11434 only; one model resident at a time; pull→test→ollama rm each; KEEP E4B until winner selected+revalidated. OUTPUT: head-to-head comparison surfaced to neuron→user to pick the winner.
--- LBD[57] ---
d84 — MS2 head-to-head RESULTS (.s9_probe/model_swap_results.md, real served US-Iran run_plan_chain FILE-SAVE screen, breadth3/reduced tree, per-model fair tuning, 6GB card). MERIT RANKING on the dimension that matters (real deliverable: correctness+grounding+no-fab): #1 llama3.1:8b (Meta, NON-Chinese) = the ONLY candidate factually CORRECT (Twelve-Day War, real June-2025 strikes/commanders/targets) + FULLY GROUNDED (0 ungrounded URLs, 3 real notes from Wikipedia/AlJazeera/CSIS) + 6 sections+timeline/damage tables — but SLOWEST (~35min, 14 tok/s, ~2GB CPU spill at num_ctx8192). #2 qwen2.5:7b (Alibaba) = FASTEST usable (~32min, 20.6 tok/s) + clean on the controlled probe, but on the REAL screen FABRICATED from memory (wrong 'Feb2025/President Biden' narrative + 2 fake URLs) → violates d60; SALVAGEABLE (it fetches when clean, fetched_count=3) with an MS3 served-route fetch nudge. #3 deepseek-r1:7b = NOT recommended (0 notes, REFUSES to fetch even when Seam-A forced gather-more 4×, flaky DAG MalformedOutput, fake URLs — the predicted R1 tool-instability). #4 deepseek-r1:8b (0528-Qwen3-8B) = DISQUALIFIED (≈2.4GB spill → a single think=True call exceeds the app's 300s transport timeout, 0 calls completed, NO deliverable). KEY LESSONS: (1) the fictional-token controlled legprobe INVERTED the real-task verdict for BOTH leads (qwen probe-PASS→real-FABRICATE; llama probe-FAIL→real-GROUND) → score on the REAL served deliverable, treat legprobe as diagnostic only. (2) Seam-A is correctly WIRED + FIRES (proven on r1:7b forced 4×) → served-path 0-fetch is MODEL-SPECIFIC not a wiring gap; N6 wiring CLEARED. (3) A final-document no-fab URL guard (ground-or-remove ungrounded URLs post-write) would help regardless of model. RECOMMENDATION surfaced to user: llama3.1:8b on MERIT (correct+grounded+no-fab+non-Chinese pref; cost=speed) vs qwen2.5:7b (fast but needs MS3 fetch-nudge to stop fabricating). Awaiting user winner pick. MS3 scope: if llama→speed focus; if qwen→served-route fetch-drive nudge; either→final-doc no-fab URL guard + the model-independent R2 per-section verify.
--- LBD[58] ---
d85 — USER (2026-06-21) deferred the winner pick: WIDEN the model search + run GEMMA/E4B on the SAME MS2 screen for a comparable baseline ("so that we have all results comparable"). So MS2b = extend the head-to-head with: (1) gemma E4B (the incumbent — already pulled as fallback, no download; gives the apples-to-apples 'what we're replacing' number on the identical breadth3/reduced-tree US-Iran FILE-SAVE screen), (2) additional native-fit candidates, weighted toward NON-CHINESE options (the field was thin; user prefers non-Chinese). Proposed widen-set (surface-before-pull, awaiting user confirm): gemma-E4B (baseline) + IBM Granite-3.1-8B (granite3.1:8b — US/non-Chinese, full-attention, native Ollama tools; best shot at a non-Chinese model matching llama's grounding, maybe cleaner) + InternLM2.5-7B (internlm2 — CN, native-fit 7B agent/tool-oriented, may be faster than the 8Bs); OPTIONAL GLM-4-9B (CN, marginal fit). SAME eval as d83/d84: per-model fair tuning, real served screen scored on factual correctness + no-hallucination + depth + tool-use(note) + speed; non-Chinese tiebreak (d82). Winner pick HELD until the widened comparison is in. NOTE: don't delete E4B (already the kept fallback); ollama rm only the newly-pulled candidates after testing.
--- LBD[59] ---
d86 — USER (2026-06-21) DATA-FLOW SMELL (likely the real root cause, reframes the whole model-swap). On the llama3.1 MS2 report the user found it SHOCKINGLY thin: it fetched a Wikipedia page + an Al Jazeera page but drafted from only ~5% of the Wikipedia article, and the cited Al Jazeera content is ABSENT from the report body. Hypothesis: we may NOT be feeding the model the right data — if sources are ingested in CHUNKS/summarized, the model (a) only sees a sliver and (b) isn't told there's MORE. "The LLM won't be able to tell anything it hasn't seen." Both Gemma/E4B AND llama produced thin reports from the SAME starved input -> points to a SYSTEMIC pipeline/tooling defect, NOT a model ceiling -> this may INVALIDATE the MS2 model comparison (all models judged on starved input). SUSPECTED culprit (per d76): chunked_read.py DEFAULT char_budget=2000 compresses a long article to a ~2000-char summary (~2% of a 100KB Wikipedia page = the ~5% the user saw); ArticleNote key_claims bounded (~8×300ch); the write phase may receive only the compressed summary/notes, not the full source; and some fetched sources (Al Jazeera) may be SCOPED OUT / dropped in the research->write hand-off. DIRECTIVE: investigate the deep-research SPEC + TOOLS + overall STRATEGY and verify the LOGICAL/orchestration correctness of the data flow: trace ONE source end-to-end (web_fetch full md -> chunked_read -> ArticleNote -> _collect_chain_sources -> run_section_write_phase scoped prompt), QUANTIFY how much of each source actually reaches the WRITE model, find every truncation/drop point, and check whether the model is signalled that a source has MORE content (chunk-of-N indicator). HOLD the MS2b widening + the winner pick until the data-flow is understood — fixing the pipeline may change everything (and re-enable a fair model test on a NON-starved pipeline). Read-only Explore dispatched.
--- LBD[60] ---
d87 — DATA-FLOW TRACE CONFIRMS d86: the pipeline STRUCTURALLY STARVES THE WRITER (root cause of thin reports across ALL models, incl the model-swap). Smoking guns (file:line): (1) RESEARCH observation hard-truncates a long article to fetched_char_budget=2000 chars (runtime.py:483, _read_fetched ~977 returns md[:budget],None) — model 'reads' ~2% of a 100KB Wikipedia page; (2) WRITE phase (the binding constraint) render_scoped_sources caps each source to excerpt_budget=700 chars of RAW markdown (synth_tools.py:719,750) — the WRITER sees ≤700 chars/source; (3) NO TRUNCATION SIGNAL — the observation says 'you have now READ this source' with no 'showing X of Y / more exists' marker, so the model believes the sliver is the whole article and can't ask for more; (4) DROPPED SOURCES — a fetched source only appears if the write-planner assigns its source_id to a section; unassigned fetched sources never reach the writer (the Al Jazeera disappearance). IMPLICATION: the MS2 model comparison (d84) was run on STARVED input → NOT a fair model verdict; E4B may have been unfairly judged too (validates d86). CAVEATS (honesty): the agent traced CODE not the real run (could not locate .s9_probe/MS2_out/ artifacts to measure real retention); and there is an unresolved thread — the report path run_plan_chain wires chunked_read=ON, but chunked_read still compresses to a ~2000-char SUMMARY and the write phase appears to re-truncate to markdown[:700], so the exact per-stage numbers for the specific llama report must be CONFIRMED against the real artifacts as step 1 of the fix. FIX DIRECTION (the spec/tools/strategy change the user asked for): (a) deliver substantially MORE of each source to the WRITER (raise/rethink the 700-char write budget; feed SECTION-RELEVANT chunks not first-700-raw), (b) SIGNAL 'this source has more' (part-N-of-M / fetch-more) so the model reasons about coverage, (c) GUARANTEE fetched+cited sources are not dropped. THEN re-run the model comparison on the FIXED pipeline (the only fair test; E4B included). SEQUENCING: pipeline fix is now PRIORITY over the model-swap; MS2b widening + winner pick remain HELD. Design-probe-first applies (surface the fix design to user before build).
--- LBD[61] ---
d88 — USER (2026-06-21): "First fix this and test on gemma." SEQUENCE: fix the data-delivery starvation (d87) FIRST, then VERIFY the fix ON GEMMA/E4B (the incumbent — already pulled, no download). So the fix-verification (MSFr) runs the fixed pipeline with GEMMA as the test model on the canonical US-Iran report. RATIONALE/payoff: if Gemma now produces a SUBSTANTIVE, grounded, factually-correct, Al-Jazeera-present report once it is actually FED the real source content, that (a) PROVES the pipeline (not the model) was the root cause of thin reports, and (b) may make the MODEL-SWAP UNNECESSARY — i.e. keep Gemma/E4B if it works on the fixed pipeline, and only fall back to the widen-set/model comparison if Gemma still underperforms on a non-starved pipeline. So after MSFr-on-Gemma: REASSESS whether a swap is even needed before re-running the widened model comparison. Design-probe-first still applies (surface the MSFd fix design to user before build), but the user has GREENLIT proceeding with the fix. MS2b widening + winner pick remain HELD (and may be dropped entirely if Gemma passes on the fixed pipeline).
--- LBD[62] ---
d89 — MSFd DESIGN + CONFIRMED real numbers (.s9_probe/data_delivery_fix_design.md, .s9_probe/retention_probe.json). MEASURED retention on the actual US-Iran run (run_plan_chain FILE-SAVE, chunked_read+notes ON, breadth3, num_ctx 32768): Wikipedia 201,566 chars → writer got markdown[:700] = 0.3%; Al Jazeera 7,686 → 700 (9.1%); CSIS 19,420 → 700 (3.6%) THEN DROPPED (assigned to no section). Thin report = 4,974B (generic wiki prose + 2 sentences AlJazeera + empty CSIS placeholder). Confirmed compounding facts: (i) even with chunked_read=ON the whole-doc summary NEVER reaches the writer — collect_fetched_sources_full (synth_tools.py:687-693) drops the summary key, writer gets raw markdown[:700]; (ii) sources silently dropped at section-scoping. 5 root-cause seams cited (runtime.py _fetched_char_budget=2000 ①, synth_tools.py excerpt_budget=700 markdown[:700] = BINDING ②, summary-drop ③, unassigned-source drop ④, no 'more' signal ⑤). FIX (3 parts, all model-independent, file-cited): (a) raise writer budget 700→~12,000 chars/source (param RA_WRITER_SOURCE_BUDGET) SIZED TO num_ctx window (min(per_source, window*0.6/n_sources)) so no d22 overflow; feed SECTION-RELEVANT chunks (rank split_chunks by lexical overlap, verbatim slices preserving c13/d50.1) not first-700-raw; ALSO carry the whole-doc chunked summary through; (b) per-source 'showing N of M — this source has MORE' marker + research-observation 'showing first N of M' signal; (c) deterministic _ensure_source_coverage = every fetched+cited source appended to best-matching/Additional section (kills the AlJazeera/CSIS vanish). Verify budget (claim_verify.py _SOURCE_EXCERPT_BUDGET=700) raised in lockstep. No-regression: default-safe, window-fit cap, short/headlines/csv byte-identical. VERIFICATION (d88): test FIXED pipeline on GEMMA/E4B, canonical US-Iran, PASS = substantive(d78 manual not bytes) + multi-source + AlJazeera-present + 100% source coverage + grounded/0-fab + suites green; interpretation: thick⇒pipeline-was-cause/swap-deferred, still-thin⇒genuine E4B ceiling⇒d80 swap stands. Surfaced to user for build approval (design-probe-first).
--- LBD[63] ---
d90 — USER APPROVED the data-delivery fix build, verbatim: "Approve — build & test on Gemma". Authorizes MSF (implement the d89 3-part fix) → MSFr (verify the FIXED pipeline on GEMMA/E4B per d88). This satisfies design-probe-first for the fix. Sequencing now: MSF implement → MSFr verify-on-Gemma (PASS = substantive d78 + multi-source + AlJazeera-present + 100% source coverage + grounded/0-fab + suites green) → REASSESS: Gemma thick+grounded ⇒ pipeline was the cause, model-swap DEFERRED/likely DROPPED (keep Gemma), proceed MS3 hardening (R2 per-section verify + no-fab URL guard) → cFINAL on Gemma → close → s12; Gemma still thin ⇒ genuine E4B ceiling ⇒ d80 swap stands ⇒ re-run widened comparison on FIXED pipeline. MS2b widening + winner pick remain HELD pending the MSFr-on-Gemma outcome.
--- LBD[64] ---
d91 — MSFr = CONFIRM (the d88 make-or-break test). On the FIXED data-delivery pipeline (d89/d90), the INCUMBENT gemma4-e4b (NO swap, @32k) produced a ~30KB SUBSTANTIVE, GROUNDED, factually-correct, Al-Jazeera-PRESENT, 100%-source-COVERED US-Iran report on the canonical run_plan_chain FILE-SAVE path — writer fed ~12,000 chars/source (vs the starved 700 = the thin 4,974B MS2 baseline). ~6x larger + substantive. This PROVES the user's d86/d87 thesis: the thin reports were the PIPELINE STARVING THE WRITER, NOT a model/E4B ceiling. IMPLICATION (d88): the MODEL-SWAP is likely UNNECESSARY — KEEP GEMMA/E4B. The whole MS1/MS2 model-swap excursion was effectively DIAGNOSTIC (it surfaced the starvation by making thin output reproducible across models). Verification-method note from MSFr: a high 'ungrounded URL' count vs the top-level fetched-source set is NOT automatically fabrication — cross-check every doc URL against the FED writer-prompt source text before calling fabrication. NEXT: MS2b widening + winner pick are now DROPPABLE (not just held); proceed to MS3 hardening (the model-independent R2 per-section verify + final-doc no-fab URL guard) → cFINAL full-depth on GEMMA (both d46 scenarios incl d58 legal/.gov, default config no flags) → close s9 → s12 independent verification. Awaiting planner's formal verdict relay + report artifact path to surface to user.
--- LBD[65] ---
d92 — cFINAL = REVISE (qualified) (.s9_probe/final_acceptance.md, independent, DEFAULT config, run_plan_chain FILE-SAVE, both d46 scenarios ×2 = 4 runs, E4B @32k, no flags). HEADLINE WIN: the d78 SUBSTANCE make-or-break PASSES on ALL 4 runs — the N1-N5 redesign + data-delivery fix clears the d53/d54 thin/placeholder ceiling; E4B produces deep multi-section real-prose reports (us_iran run1: 28 headings/107 dates/95 figs/29.9KB; legal: 21 headings/13.8KB) on BOTH scenarios. MODEL-SWAP NOT triggered on substance (keep Gemma validated). Legal 0-fetch FAIL OVERTURNED: canonical default path fetches real .gov/.edu primaries (copyright.gov, law.cornell.edu), zero fabrication both legal runs. TWO RESIDUAL GAPS (block clean CONFIRM): (1) US-IRAN CITATION PURITY non-deterministic — run1 cited 42 URLs but only 9 fetched; 33 never independently fetched incl a CONFIRMED 404 fabrication (state.gov/a-force-for-good... → HTTP404); run2 clean (4 URLs). ROOT CAUSE = CODE not model: strip_ungrounded_urls grounds a cited URL against the LENIENT corpus (url+markdown+summary+title of fetched pages), so URLs appearing as hyperlinks INSIDE fetched articles pass even though never read → the driver's n_fabricated=0 is a FALSE pass. CODE LEVER: tighten the guard to require a cited URL be in the ACTUALLY-FETCHED source-URL set (or resolvable). (2) LEGAL breadth caps 2-3 → fails the 'not capped at 3' bar (though cited = correct authoritative primaries; arguably fine for a single statute). NOT an E4B substance-ceiling fail; both gaps have CODE levers (no model change). No regression to standing wins. DECISION surfaced to user (3 options): (1) ACCEPT E4B w/ documented citation caveats, defer hardening to s12; (2) HARDEN IN CODE (strict fetched-set citation grounding + breadth bump) + re-verify — code lever no model change [neuron recommends, honors d60 absolute no-fab — the 404 is a real fabrication]; (3) MODEL-SWAP only if strict citation fidelity required and (2) insufficient (overkill for a code bug). Per d77 these were framed deferred-to-s12, but the 404 is a d60 no-fab VIOLATION (a bug/acceptance-failure, not an enhancement) → fixing it is part of verification.
--- LBD[66] ---
d93 — ARCHITECTURE ANALYSIS (2 read-only Explore agents, user due-diligence before the cFINAL-fix decision). FINDINGS: (1) SHAPES vs SPECS: shapes = TOPOLOGY (shapes/*.toml, declarative, NL-authored via shape_author.py, NOT MCP); specs = node behavior/ruleset. Shape chosen by an LLM ShapeSelector.select per query (shape_selector.py). (2) THREE deep-research execution paths: (a) _run_deep_research SIMPLE = unroll the cyclic shape into a FLAT LINEAR DAG (research/critic rounds→synthesis→verify), APP-FIXED topology, NO decision loop; (b) _run_deep_research_sectioned N4 = genuinely AGENTIC research TREE: each layer gathers leaf research nodes, a DECISION NODE reads the persisted research-state file (d49) and authors expand_branch/prune_branch via LLM tool calls, LOOPS/REVISITS each layer until depth bound — this is the 'agent revisits to decide the way forward' behavior; (c) run_plan_chain (wants_file+multi_page) = the USER'S FILE-SAVE REPORT PATH: PHASE-1 an INCREMENTAL-PLANNER-authored research DAG (LLM authors ~2-3 research nodes node-by-node via plan_tools seed_plan/add_step/set_node_spec/finalize_plan), runs it ONCE, then PHASE-2 per-section write. CRITICAL: the user's actual reports (run_plan_chain) DO author the research graph via the LLM planner (agentic) BUT run it as a SINGLE PASS — there is NO decision-node revisit/prune loop on this path. The iterative N4 decision-tree the user envisions EXISTS but only on path (b), which file-save reports do NOT take (consistent with n6_design's table; d70's 'N4 live default' = path b, not run_plan_chain). (3) TOOLS are in-app (reactive_tools ToolHook → EventPlane; plan_tools; N4 tree tools) — ZERO MCP. (4) NO AGENTIC REVIEWER LEG file_reads the finished HTML to critique/fix structure — the verify lane (claim_verify) is REASONING-ONLY on the in-memory string; the 2 file_reads in the write loop feed grounding only. So doc-structure defects are never reviewed. (5) DOC-STRUCTURE BUGS confirmed in real us_iran_run1: ToC built ONCE from headings-present-at-assembly so LATE-appended sections (Military Actions) missing from ToC; 15 orphaned bare <li> + malformed '</ul' (strip_wrapper_openers doesn't validate well-formedness); duplicate id=figures (collapse_duplicate_sections matches by heading-family, misses it, doesn't rename ids). Root theme: sections written INCREMENTALLY (append) but ToC/dedup/wellformedness run ONCE at end, never reconciled. (6) CITATION GUARD: grounded_url_corpus = substring match of cited URL against concatenated url+markdown+summary+title of fetched pages → any URL appearing as a hyperlink INSIDE a fetched page passes as 'grounded' though never read (the 33-unread + 404 hole). Fix = match against the FETCHED-URL SET only. IMPLICATION: the real fix scope is NOT one citation tweak — it is at least: (A) citation guard tighten, (B) doc-structure integrity (incremental ToC/dedup/wellformedness or a final reconcile pass), (C) add an agentic REVIEWER leg that file_reads + critiques/fixes the HTML, (D) consider routing file-save reports through the iterative N4 decision-tree so research genuinely revisits-and-decides (the user's 'true agentic' ask). Surfaced to user to scope the decision; no fix chosen yet.
--- LBD[67] ---
d94 — USER RE-GROUNDING (2026-06-21/22): restore the ORIGINAL agentic loop; stop patching symptoms with deterministic code. THE TARGET = Research → Review → Iterate → Synthesize with the AGENT IN THE LOOP the whole way: (1) RESEARCH fetch deeply; (2) REVIEW = the agent READS THE FINDINGS and judges sufficiency/gaps/contradictions and WHERE IT'S HEADED — NOT a passive review of the final HTML; (3) ITERATE = the agent decides to dig deeper/redirect AND shapes where the DOCUMENT is heading (structure emerges from research, agent-decided), looping research↔review; (4) SYNTHESIZE only when the agent judges the research sufficient. The user's critiques: A & B are CODE band-aids (demote to deterministic BACKSTOPS, not the primary fix); my Path C was TOO WEAK — it reviewed only the OUTPUT, not 'where the research + html heads'; the real fix = Path D done right = keep the AGENT IN THE LOOP making the decisions (research direction AND document direction), with C's reviewer role living INSIDE that loop. COST IS ACCEPTED: 'deep research is meant to be expensive in itself'; if the agent isn't in decision-making and we trust code blindly, quality is no better than a fixed deep-research pipeline. WHY IT DEVIATED (owned): E4B's agentic weaknesses pushed the build toward deterministic scaffolding to force quality; the iterative N4 tree got wired to the NON-file path while the file-save report path (run_plan_chain) kept a single-pass plan-research→write→code-check design; recent cFINAL fixes doubled down on deterministic guards. CONSEQUENCE: the report deliverable must be REBUILT around the agent-in-the-loop research→review→iterate→synthesize loop on the run_plan_chain path; A/B become backstops; D is the backbone (extend the N4 decision-loop onto the report path AND extend the agent's decisions to span the WRITE/document direction, not just expand/prune branches). NEXT: design-probe-first for this agentic loop, surfaced to user before build; confirm the vision before dispatching. Refreshes the comprehension baseline (the FSM drift nag).
--- LBD[68] ---
d95 — ENRICHES d94 (user, 2026-06-22): the REVIEW step is NOT just 'where the HTML is headed' — it is REVIEWING THE RESEARCH MEMORY: the agent reads the WHOLE accumulated research tree/memory (all branches + their ArticleNotes + findings, the persisted ResearchState) and judges WHETHER EACH BRANCH ADDS MEANING — prune the branches that don't, expand the promising ones, decide when there's enough. This is exactly the N4 decision-node-over-persisted-memory concept (research_tree.py: decision node reads ResearchState, expand_branch/prune_branch), which must be (a) brought ONTO the report path (run_plan_chain), where it currently does NOT exist, and (b) enriched to also drive document direction. CURRENT-STATE CLARIFICATION the user was unclear on: the report path is NOT a 3x3x3 fan-out tree — it is FLAT single-pass: the incremental planner authors ~2-3 research nodes, each fetches up to breadth (10 default / 3 in the model-test screen) real articles, runs ONCE, no fan-out-of-fan-out, NO review-of-research-memory loop. The '3x3' the user recalled = the d83 model-test reduced config (~3 flat nodes × 3 fetches ≈ 9 sources), NOT exponential. The genuine research-memory-review tree (N4) exists but is wired to _run_deep_research_sectioned, which the file-save report path never takes. (Exact node-count/breadth/fan-out + cFINAL trace numbers being verified by a read-only probe.) DESIGN TARGET (firmed): a real research TREE on the report path where the agent periodically REVIEWS THE FULL RESEARCH MEMORY, prunes meaningless branches, expands meaningful ones, decides done, and shapes the document — Research→Review(the memory/tree)→Iterate→Synthesize, agent-in-loop.
--- LBD[69] ---
d96 — s13 DESIGN-PROBE COMPLETE (3 docs in .s13_design/: current_state_findings.md a1, agentic_loop_design.md a2, design_review.md a3; review = APPROVE-WITH-CHANGES, all 4 issues fixed in-place; faithful to d94/d95; d89/d65/d60/d50.1 preserved). KEY FINDING: the rebuild is LOW-RISK = mostly a SUBSTITUTION of already-proven code, NOT green-field — _run_deep_research_sectioned ALREADY runs the N4 tree loop (run_research_tree + ResearchState persisted memory) and hands (findings,sources) to the SAME run_section_write_phase that run_plan_chain uses. DESIGN (4 parts): §1 SUBSTITUTE the flat single-pass research DAG in run_plan_chain PHASE-1 with the tree loop + persisted research MEMORY (~14-line proven block); PHASE-2 untouched → the report path GAINS a real research tree + memory + a between-layers agent decision node. §2 ENRICH the decision node into the d95 REVIEW-OF-MEANING (judge which branches add meaning, prune dead/expand promising) + a low-arity stop_research tool so the AGENT decides 'enough'. §3 NET-NEW faithfulness linchpin = agent-decided DOCUMENT direction: add_section/drop_section persisted in memory, the outline EMERGES from the research, synthesize only when the agent judges sufficient (Research→Review→Iterate→Synthesize). §4 DEMOTE the deterministic guards to BACKSTOPS + harden: A citation guard→fetched-URL SET (closes the d92 404 leak), B doc-structure via a real stdlib HTML parser (ToC/dup/wellformed), +C a deterministic source-COVERAGE net (closes the a1 finding that d89's _ensure_source_coverage + 'there's more' marker were NEVER actually built — only the 700→12k budget raise landed). E4B FEASIBILITY: PASS — all new tools are the low-arity shape E4B authors reliably (expand 4/4, prune 2/2 per DD); never hard-depends on the flaky set_next_direction (advisory-with-fallback). HONEST COST: ~2-5x single-pass wall-clock at default depth 5 (~4-15 leaf nodes, 3-4 decision layers ≤18 turns each, several dozen fetches vs today ~6 nodes/~9 fetches); stop_research = the agent's quality-aware cost control; caps are TreeConfig env knobs (user accepted 'deep research is meant to be expensive'). PROPOSED STAGE B: B1 port tree loop → B2 review-enrich → B3 doc-direction → B4 citation backstop → B5 doc-structure backstop → B5c coverage backstop → B6 flag-free-default audit (d65) → B7 offline robustness → B8 LIVE cFINAL re-validation on DEFAULT config (prove served-route identity + loop-fired + citation purity + coverage + document-direction landed) → B9 human UI gate; each Bx verify-gated. Build HELD at the design-probe-first gate pending user GO. (Plan was FSM-marked terminal at end-of-Stage-A; planner held at the gate, did NOT close; on GO it reopens via record_plan and authors Stage B.)
--- LBD[70] ---
d97 — USER GO to BUILD the s13 agentic-loop design (d96): "I am okay with the slowness. Go ahead and build it." Don't deviate for a cheap fix; long running times are accounted for. CONFIG REFINEMENT: deep-research DEPTH must be CONFIGURABLE via the shapes/specs flow; BREADTH stays FIXED at 3. FOUR ACCEPTANCE SCENARIOS (chosen to prove shape+spec routing is REAL, not hardcoded — they exercise different paths): (a) US-IRAN WAR NEWS — full timeline 2025→2026 + cost to each party etc → DEEP-RESEARCH path; (b) PIRATE HISTORY in pirate lingo + UI → TONE spec + research quality + UI styling all honored; (c) "SEND TODAY'S HEADLINES VIA MAIL" → different OUTPUT FORMAT + the email must contain headlines with CLICKABLE LINKS (and d12 recipient self-lock honored); (d) "WRITE A SHORT HAIKU for <scenario>" → NO complex shape, SPEC-PICKED check (must NOT over-route to deep research / a heavy shape). TEST METHODOLOGY for deep research (HARD): (1) test DEPTH 3 × BREADTH 3 FIRST, then full depth; (2) do NOT just scan the HTML — READ the report and check whether the agent MISSED anything from its own research / web-tool results (faithfulness to what it fetched); (3) use the TRACES to understand the agent's CoT / decision path; (4) verify the agent HONORS the shapes+specs flow and it is NOT fabricated hard-coded app behavior; (5) confirm DEPTH is settable via shapes/specs (breadth fixed 3). This is the acceptance bar for the s13 Stage-B verification (B8 live + B9 UI gate).
--- LBD[71] ---
d98 — USER (2026-06-23) sequencing: FOCUS ON BEHAVIOR FIRST. Complete the s13 B8 live four-scenario BEHAVIOR validation (does the agent-in-the-loop research→review→iterate→synthesize loop produce good research/reports — read-don't-scan, trace-CoT, shapes/specs-honored, depth3×breadth3-then-full). UI is DEFERRED to a DEDICATED UI STEP — the B9 human-UI-gate + the depth-UI-widget question + ALL UI concerns belong there, NOT now. The user HAS A LIST OF UI CONCERNS to discuss. HARD REQUIREMENT: REMIND THE USER BEFORE launching the UI test-and-fix step of the app (B9 / live-via-UI) — do NOT autonomously enter UI testing/fixing; stop at the B9 gate, remind the user, and DISCUSS all their UI concerns first, then scope the UI step from that discussion. So: B8 behavior validation proceeds now; the depth-UI-widget open question is parked for the UI discussion; the neuron must surface a 'ready for the UI step — let's go through your concerns' prompt before any UI work begins.
--- LBD[72] ---
d99 — s13 B8 COST REALITY (honest surface): the live agent-in-the-loop on E4B measures ~10-20x single-pass, FAR above the design a2 §7 estimate of 2-5x. B8a US-Iran depth3: ~80min in / 87 llm.chat calls (~48s/call at 32k+think=True), still researching (not yet writing); worker ETA depth3 ~1.5-2h/scenario, full-depth(depth5) longer; 4 scenarios serial (GPU-bound, one model) = many hours (~10-15h+ for the full depth3+fulldepth ×4). The loop is genuinely churning (real ReAct/decision turns, 1.85MB trace) — NOT wedged; correct agentic behavior, just expensive. Slowness was accepted (d94/d97) but the magnitude diverges enough to flag before committing the full multi-hour run. OPTIONS: A let all 4 run full (depth3 + full-depth each); B (neuron rec — and it MATCHES the user's own d97 methodology 'depth3×breadth3 FIRST, then full depth') = run depth3×breadth3 across ALL 4 first (proves every d97 behavior gate: served-route identity, loop-fired, read-don't-scan faithfulness, citation/coverage, document-direction, + pirate/mail/haiku behaviors), surface, THEN full-depth as a deferred separate confirmatory pass; C reduce the agent loop budget (decide_max_turns 18→lower and/or depth) to cut per-pass calls — NOT recommended, it compromises the agentic quality the user explicitly demanded ('don't deviate for a cheap fix'). Awaiting user cost-scope call; B8a continues meanwhile (not halted).
--- LBD[73] ---
d100 — THROTTLE DIAGNOSIS (live in-shell snapshot during B8a, user-requested). Bottleneck = GPU COMPUTE, GPU at its thermal/power ceiling. nvidia-smi: GPU util 93%, mem-controller util 93%, VRAM 3529/6141 MiB (2.6GB FREE), clock 2070MHz (silicon max 3105), temp 87°C, power 59W. ollama /api/ps: E4B size_vram==size==3.05GB => FULLY on GPU, ZERO offload. RAM: 0.61GB free of 15.71GB (nearly exhausted). CPU: 22%. CONCLUSION: NOT VRAM/offload (model fully resident, 2.6GB spare), NOT CPU. It is GPU-compute-bound, GPU pinned at ~2070MHz/87°C/59W = near sustained thermal/power ceiling for this 6GB laptop (cooling/higher-power-profile = minor lever, not big). REAL DRIVER (inherent): ~87 sequential llm.chat calls/scenario, each prefilling up to 32k-token context + generating long think=True CoT at ~48s/call — the genuine cost of a multi-turn agentic loop on this hardware (GPU doing real work, not stalling). RAM near-full is NOT the GPU bottleneck but adds system/orchestration overhead — free other apps. BIGGEST NON-CHEAP SOFTWARE LEVER: the 32k-context re-fed EVERY turn — as the research memory accumulates, per-call prefill grows; compacting WHAT is re-sent each turn (vs re-feeding everything) could cut per-call time WITHOUT reducing the agent's decision turns (the cheap fix the user ruled out). User deferred the B8 cost-scope (A/B/C) decision to 'when the full report is in'; B8a continues meanwhile.
--- LBD[74] ---
d101 — PERFORMANCE-OPTIMIZATION BACKLOG (user, deferred). User already has Windows 'Best Performance' on; asked why GPU isn't 100%. ANSWER: 93% ≈ saturated; the sub-100% + dips come from (a) the AGENTIC LOOP's non-GPU gaps — web_fetch/web_search NETWORK I/O round-trips during which the GPU idles, + CPU-side parse/decision/trace overhead between calls; (b) LLM DECODE is MEMORY-BANDWIDTH-bound not compute-bound (mem-controller util also 93% = the real signature), so '100% util' isn't the right target; (c) the 59W/2070MHz power/thermal cap. So part of wall-clock is network/tool I/O, not GPU. OPTIMIZATION LEVERS the user wants pursued (all DEFERRED until research is clean + generated artifacts live up to the tasks — QUALITY FIRST, PERF SECOND): (1) PROMPT OPTIMIZATION + KV/PREFIX CACHING — keep the system prompt + shapes/specs a BYTE-STABLE prefix across calls so the engine reuses their KV cache and skips re-prefilling them each turn; optimize HOW shapes/specs are loaded into the system prompt to preserve that cacheable prefix (user's insight). (2) CONTEXT COMPACTION — compact the accumulating research memory re-fed each turn (d100) so per-call prefill shrinks, WITHOUT cutting the agent's decision turns. (3) PARALLELIZE web fetches so the GPU isn't idling one network request at a time. (4) Hardware levers (d100): RAM 16→32GB for headroom (minor for throughput), thermal/power tuning (~10-30%, mostly free). NOTE: flash-attn + KV q8_0 already ON (s8 env). Sequencing: do NONE of this until B8 behavior is clean + artifacts good; then revisit with the full numbers.
--- LBD[75] ---
d102 — B8 SCOPE STEER + a behavior finding (neuron, consistent with user d97 + deferral). Magnitude worsened: B8a depth3 ~2-3h, full-depth(5) ~4-6h, ~6-8h/scenario two-pass, ×4 serial = ~24h+ for the full Option-A matrix; ~155 calls so far at ~55s. ROOT CAUSE (BEHAVIOR-RELEVANT): E4B OVER-EXPANDS the layer-2 frontier; the wide frontier's leaf-gathers are concurrent in code but SERIALIZE at the single Ollama model; loop bounded (depth3) and terminates — COST not hang; the live face of BR's R1 runaway-expansion note. DECISION = Option B: run depth3×breadth3 across ALL 4 scenarios FIRST (this IS the user's own d97 'depth3 first, then full depth' methodology — sequencing, NOT a trim), surface the depth3 behavior report, DEFER the full-depth second pass to a deliberate later call once depth3 quality is seen. Do NOT apply Option C (cap frontier / lower decide_max_turns) — that throttles the agent's own judgement (the 'no cheap fix' the user ruled out, d97). Instead LET the over-expansion run as-is and REPORT it as a QUALITY observation: E4B over-expanding without enough pruning is a potential gap in the d95 REVIEW-OF-MEANING (agent should prune branches that don't add meaning); the B8 report MUST assess whether prune/review is actually working, and a HARD per-run leaf/fetch BUDGET (a bound, not a turn-cut) is a candidate fix to weigh later — distinct from cheap-throttling. User deferred explicit A/B/C + numbers to 'when the full report is in'; B delivers that report without a 24h+ blind run; user can override.
--- LBD[76] ---
d103 — B8a (US-Iran, depth3-only per d102) = PASS-WITH-FINDINGS (.s13_design/B8a_us_iran.md; ~2.83h, 227 calls @42.8 tok/s). MECHANISM PROVEN LIVE on E4B: served-route identity + loop-fired on the SAME run (agent.plan_chain span + tree attrs; stop_reason=agent_sufficient; depth_configured=3 via the shapes/specs knob, breadth pinned 3); 6 REAL sources fetched (not memory); agent genuinely drove expand/stop/outline (d95 stop_research WORKS — agent decided 'enough' at layer2 before the depth bound); all 3 backstops shipped+firing (citation guard removed 7 fabricated URLs, 0 survive = purity holds; coverage 100%; structure single-doc); substantive d78 SPA (26KB, 25 headings, 0 placeholders). PRUNE UNTESTED = NO over-expansion (corrects the earlier over-expansion hypothesis — tree was modest root->3->1->stop; prune never needed to bite). REAL QUALITY GAPS (read not scanned): (1) DOMINANT WASTE + FABRICATION ROOT CAUSE = the layer-1 ROOT research node was handed the WHOLE broad goal as one task -> hit the 70-min (4200s) timeout for 0 yield (~91 of 227 calls = ~55min/run wasted); (2) 2 of 4 research nodes returned EMPTY (root + B1) -> the Timeline section (assigned to empty B1) is FABRICATED FROM MEMORY under the doc's own 'meticulously cited' claim; (3) the 'Sources' section is a memory PLACEHOLDER contradicting the 6 real citations; (4) OUTLINE LANDS AS A DUPLICATE TAIL not the primary scaffold (B3 document-direction mechanism works=true but execution weak: a 2nd duplicate section set appended after Conclusion -> THREE 'Section 3's with conflicting numbering; Backstop B dedups ids/ToC not THEMATIC duplication). PRINCIPLED FIXES (flagged, NOT applied — not throttles): (a) tighter ROOT-NODE scoping / shorter per-node timeout so layer-1 doesn't waste ~55min on the un-completable whole-goal query; (b) a hard per-run leaf/fetch BUDGET (a bound on resource spend, not on the agent's judgement); (c) make the agent-decided outline the PRIMARY write scaffold (not a duplicate tail) + thematic-dedup in Backstop B; (d) the writer must not fabricate sections whose research node returned empty (gate/grounding). DECISION surfaced to user: pause-and-fix the clear issues vs run B8b/c/d first; planner HOLDING B8b/c/d pending the call.
--- LBD[77] ---
d104 — B8a TRACE FORENSICS (read-only investigation of trace 0b9d1bbb..., 250 spans/227 llm.chat; exact parent-partition counts). ROOT CAUSE (one line): the layer-1 ROOT node and the B1 timeline node EACH fetched a giant ~600K-char Wikipedia page whose chunked_read map/reduce (DEFAULT_CHUNK_CHARS=8000 → ~76 sequential summarize calls/article, chunked_read.py:31) consumed the FULL 4200s (70-min) per-node timeout BEFORE either could emit findings → 163 of 227 calls (72%) burned on TWO cancelled zero-yield nodes. NOT over-expansion, NOT a deadlock. EXACT CALL BREAKDOWN: root=91, B1=72, B2=12, B3=17, decisions=11(6+5), write=24(planner6 + 4 section-writes incl verify passes) = 227. (Worker's earlier report OMITTED B1's 72 calls — B1 ALSO timed out, not a '0-fetch short-circuit'.) WHY ROOT 91/0-yield: root.question seeded = the ENTIRE verbatim goal (research_tree.py:698-699), root leaf runs the SAME generic 'fully research' instruction as focused sub-nodes (runtime.py:209-223); it fetched en.wikipedia 2026_Iran_war (603,252 chars) → ~76 chunk-summaries → ~66min for ~5 ReAct turns → cancelled by asyncio.wait_for timeout=4200 (runtime.py:3533) BEFORE findings emission (attrs set only at runtime.py:1251-1256 after loop) → 0 sources. WHY B1 EMPTY: NOT a bad search/giveup — search returned real results, fetched a large Wikipedia page → SAME chunked_read death-spiral (72 calls) → cancelled mid-chunk-read → fetch never committed → reported 0. CONCURRENCY VERDICT: leaves dispatched CONCURRENTLY (asyncio.gather, research_tree.py:744); they SERIALIZE at the single Ollama :11434 (one generation at a time = queue, not deadlock); the STRUCTURAL DEFECT = HEAD-OF-LINE BLOCKING — the layer awaits the ENTIRE gather before the decision node runs, so B1's 70-min hang gated layer-2 even though B2(958s)/B3 finished ~54min earlier. So ~140min of the ~170min run = two back-to-back 70-min timeouts (root layer, then layer-2 gated by B1). NO TRUE DEADLOCK. DECOMPOSITION was SOUND (agent cleanly split B1/B2/B3, authored outline, stop_research) — the gap is UPSTREAM WIRING: root researches the whole goal redundantly, then layer-2 re-decomposes the same ground. FIX LEVERS (all principled bounds, not judgement-throttles): (1) SCOPE/SEED-ONLY ROOT — decompose-first / never gather the whole-goal leaf, or give root a tight survey budget; (2) PER-NODE timeout ≪ per-run (currently one 4200s everywhere, b8a_drive.py:51) — leaf ceiling ~5-10min; (3) BOUND chunked_read — cap chunks/source or total chunk-read calls/leaf, or raise DEFAULT_CHUNK_CHARS ≫8000, or skip map/reduce when char×latency exceeds budget (THE single biggest fix — would've let root+B1 finish); (4) per-run fetch/leaf-minute BUDGET; (5) FIX HEAD-OF-LINE BLOCKING — per-leaf timeout + as-completed harvest so a slow leaf can't gate the layer; (6) EMPTY-NODE-NO-FABRICATE — a node that times out/yields 0 marks its section unsupported so the writer drops/flags it instead of inventing (the fabricated Timeline). Recommendation: FIX-FIRST (planner + neuron concur).
--- LBD[78] ---
d105 — GROUNDING the chunked-read/SWA provenance (user asked which fixes are correct vs noise). PROVENANCE (confirmed by chunked_read.py docstring + recorded decisions): chunked_read = N3/d62/c15-part-d, the 'READ-side analog of the write-side 512-token SWA window (d55/d59)'; introduced because the legacy md[:2000] truncation dropped everything past the cut → thin/placeholder findings (the SWA window-spill hallucination, recorded SWA root cause: num_ctx 32768 / sliding_window 512). Write-side = c13 per-section source-scoping (d56/d57). So the fixes were NOT noise — they addressed a real failure. WHAT HAPPENED IN THE 70-MIN TIMEOUT (verified): root/B1 fetched the '2026 Iran war' Wikipedia page = 603,252 chars ≈ 150,800 tokens = ~4.6× the ENTIRE 32,768 context (doesn't fit at all). chunked_read.py splits into DEFAULT_CHUNK_CHARS=8000 chunks and maps over EVERY chunk UNBOUNDED (line 125 loop) → 603252/8000 ≈ 75 sequential summarize calls @~45s ≈ 56min → 70-min timeout cancelled before any finding emitted → 0 yield → empty node → fabricated Timeline. SO: the INTENT is correct (a 150k-token doc genuinely needs chunked reading) but the SCALING is the noise: (a) reads the ENTIRE doc unbounded — yet the summary is capped 2000 chars (char_budget) AND the writer gets 12k RAW chars/source (d89), so reading all 600k chars is near-pure waste; (b) chunks are tiny (8k chars≈2k tok) driven by the 512-SWA framing, but Gemma interleaves GLOBAL attention layers so chunks could be 10-20× larger safely → call count falls the same factor. OPEN QUESTION worth an empirical test: was the 512-SWA severity over-assumed? LINE OF ACTION (right-size, don't rip out): (1) BOUND chunks-per-source (read enough not all); (2) much BIGGER chunk size (toward the ~100k-char window); (3) short per-node timeout ≪ shared 70min; (4) head-of-line fix (as-completed harvest); (5) seed-only/scoped root; (6) empty-node-no-fabricate; (7) a CHEAP EMPIRICAL SWA SANITY CHECK (feed E4B one 20-40k-char doc; does it hallucinate or read fine?) to size the chunks from DATA and retire the SWA-over-tune uncertainty. Awaiting user go on building this fix set (SWA check first to size chunks).
--- LBD[79] ---
d106 — RESEARCH-STRATEGY FIX (user, presented as a before/after diagram for discussion; awaiting user confirm before build). USER ADDED: a RELEVANCE-DIRECTED chunk-reading strategy — do NOT read the entire doc; rank the doc's chunks against the NODE's sub-question and read only the TOP-N relevant chunks (bounded), reusing the proven write-side select_relevant_excerpt relevance-ranking pattern on the READ side. User fears chunked_read was 'built incorrectly' (it reads the whole doc blindly) — justified: the flaw is read-everything, not the chunking concept. AGREED FIX SET (right-size, don't remove): (1) RELEVANCE-DIRECTED + BOUNDED read (top-N relevant chunks per source, bounded budget — we don't need 150k tokens to answer 'what's the timeline', just the timeline sections); (2) BIGGER chunks sized from the SWA test (8k→toward the ~100k-char window); (3) SEED-ONLY/scoped root (decompose-first, no whole-goal research pass); (4) SHORT per-node timeout ~5-10min ≪ the shared 70min; (5) AS-COMPLETED harvest (no head-of-line block — decision proceeds on finished leaves); (6) EMPTY-NODE-NO-FABRICATE (0-yield node → section marked unsupported); (7) agent outline = PRIMARY scaffold (not duplicate tail). ORDER: (A) SWA SANITY TEST first (feed E4B one 20-40k-char doc; does it read cleanly or hallucinate?) → sizes chunks + settles whether the 512-window caution was over-tuned, from DATA not assumption; (B) BUILD the relevance-directed bounded reader + the structural fixes; (C) RE-RUN B8a depth3 to confirm wall-clock drops from ~2.8h to a fraction AND no fabrication, BEFORE the other scenarios. Diagram (current: redundant root→unbounded 75-chunk read→70min timeout→0 yield→head-of-line→fabricated Timeline+duplicate-tail; proposed: decompose-first→relevance-ranked top-N bounded bigger-chunk read→short timeout+as-completed→outline-primary+no-fabricate) surfaced to user. Planner HELD; no build until user confirms the strategy.
--- LBD[80] ---
d107 — USER GO + STOP/METHODOLOGY refinements (2026-06-23, "Go ahead bruv"). Approves the d105/d106 fix set with these additions, ALL to be coded as GENUINE AGENTIC behavior (prompts + tool-calls reading the SHAPE file + the SPEC), NOT fabricated hardcoded flow: (1) DEFINE THE STOP CRITERIA IN THE SEEDED DEEP-RESEARCH SPECIALIZATION. Currently stop = the agent's stop_research tool-call from the decision-node prompt; it is NOT defined as a methodology in the seeded DEEP_RESEARCH_SPEC (agentic.py:49). ADD it to that seeded spec: deep research = identify the WHAT/WHEN/WHY/HOW (investigative decomposition) → look for answers → VERIFY them → stop when the investigation is sufficiently answered+verified. This is DISTINCT from question-answer (Q&A) — both have different use cases; the spec must encode the investigative model, not Q&A. (2) THE DEEP-RESEARCH SHAPE FILE holds the iteration/depth value (how many layers); the AGENT reads the shape and decides to STOP EARLY agentically (stop_research + spec criteria); HARD CAP = 10 — beyond 10 layers we TERMINATE (a safety bound, not fabricated behavior). 'The planner composing the up-to-10-layer plan AND able to stop before 10 should be achievable by READING THE SHAPE FILE ALONE via agent-in-the-loop.' Iteration count lives in the SHAPE (file, agent-read), configurable; breadth pinned 3 (d97); ceiling 10. (3) CONTEXT-WINDOW EFFICIENCY: we already have SEPARATE memory for CHAT, PLANNER, and WORKERS — use each role's context window effectively, don't bloat/cross-contaminate; ties to d101 (stable cacheable prefix, compaction). NON-NEGOTIABLE: no fabricated/hardcoded behavior — the stop, the iteration budget, and the decomposition must come from the agent reading the shape+spec and deciding via prompts/tool-calls. ORDER (d106): SWA test → build (relevance-directed bounded reader + seed-only root + short per-node timeout + as-completed harvest + empty-node-no-fabricate + outline-as-primary + the spec-defined stop methodology + shape-driven ≤10 iteration cap) → re-run B8a depth3 → then the other scenarios. Planner released to build.
--- LBD[81] ---
d108 — FX0 SWA / EFFECTIVE-WINDOW TEST RESULT (.s13_design/swa_test.md; needle-in-haystack, gemma4-e4b @num_ctx32768, think=True, needles at 0/25/50/75/98% positions, marked AND unmarked). HEADLINE: the SWA-512 caution was OVER-TUNED — E4B attends FAITHFULLY across the WHOLE 32768 window. MEASURED: 5/5 needle recall at ALL positions (no lost-in-the-middle) from 8k chars up to ~220k chars (~31k tokens), marked + unmarked. Effective-attention ceiling NOT reached anywhere inside the nominal ctx — attention is NOT the binding constraint; the shared TOKEN BUDGET is. The REAL failure mode = the HARD 32768-token truncation cliff: past ctx, Ollama truncates from the FRONT (drops front content) → degenerate/empty output (0/5 at ≥280k chars). One generation caveat: at very large input a single call had a think-runaway (think tokens exhausted num_predict=4096 → empty content, 144.9s) — recovered to 5/5 at larger size, so generation instability not a window ceiling. CALIBRATION: synthetic filler ~7 chars/tok; real article prose ~4 chars/tok (recommendation computed conservatively at 4). RECOMMENDED CHUNK SIZE FOR FX-READ: PRIMARY = 24,000 chars/chunk (~6k tok), UP 3× from the current 8,000 — lets 3-4 sources coexist with generation in one 32k call; if only ONE doc in context, a single chunk can go to ~64,000 chars (proven 5/5) or more. CAVEATS FX-read MUST honor: (1) enforce a TOKEN BUDGET on the ASSEMBLED prompt (all chunks+question+history < ~30k tok, leave ≥2-3k for generation) — not just a per-chunk char cap; the front-truncation cliff is the thing to design against; (2) keep num_predict≥4096 + treat empty content as a RETRY signal (think competes with answer); (3) NO position special-casing (no lost-in-the-middle); (4) latency scales with input (~11s@8k → ~30s@220k). IMPACT: a doc that FITS the window (≤~88-104k chars after reserving generation budget) can be read in ONE call, no chunking; the 603k-char Wikipedia page still needs chunking but at 24k chunks = ~25 (vs 75 @8k), and with RELEVANCE-DIRECTED bounded reading (top-N relevant only) → a handful. So bigger-chunks + relevance-directed reading together kill the 75-call/70-min explosion. This DATA settles the user's 'was chunked_read built incorrectly / was SWA over-tuned' question: YES over-tuned; right-size to 24k + relevance-bounded + token-budget-cap.
--- LBD[82] ---
d109 — CHUNK-READING STRATEGY locked (user confirm + re-ranking Q). REPLACE the whole-doc map/reduce read with RELEVANCE-SELECT-then-SINGLE-READ: (1) split each fetched doc into SMALL ranking chunks (~3k chars, paragraph/section-granular) for relevance precision; (2) RANK all chunks across the node's N fetched docs by relevance to the NODE's sub-question via EMBEDDING similarity — reuse the existing MiniLM 384-d embedder (memory store), NOT the current crude lexical overlap; (3) ASSEMBLE top-X relevant chunks up to a CONTENT BUDGET ~20k tokens (~80k chars) leaving room for prompt+generation under the 32k window (d108); (4) READ the assembled relevant excerpt in ONE call — fits the window (SWA-proven d108) so NO map/reduce for normal docs; (5) FALLBACK to bounded map/reduce ONLY if relevant content alone overflows the window. HONEST SIGNAL (fixes the original tool's vague 'there are more chunks'): 'found M relevant passages across your N sources; here are the top X (from sources A,B,C)' — explicit counts + provenance. RE-RANKING verdict: the RAG re-ranking CONCEPT (score chunks by query relevance, keep best) IS our strategy; the heavy 2-stage RAG machinery (cheap-retrieve → cross-encoder) is OVERKILL for our scale (few docs/dozens of chunks) + saturated 6GB GPU. Use embedding-similarity ranking (MiniLM, cheap, present); cross-encoder re-ranker = LATER option only if quality insufficient. SIZE/COUNT are TUNABLE starting points (~3k ranking chunk, ~20k-tok read budget, top-X = as-many-as-fit); refine from a quick relevance check. REFINES d108: the 24k/64k was for the old whole-doc approach; now 24k stays ONLY as the WRITE-side per-source budget, the READ does relevance-select-then-single-read. Steered into FX-read.
--- LBD[83] ---
d110 — B8a2 RE-RUN = PASS-WITH-FINDINGS (.s13_design/B8a2_us_iran_rerun.md; served default, no flags, depth3, breadth pinned 3). CORE ROOT-CAUSE FIX PROVEN LIVE: wall-clock 2.83h→27.7min (6.1×), calls 227→39 (5.8×), root 70min/0-yield burn GONE (root completed 102s/1 source/8554 chars); content GROUNDED in a real fetched source (warcosts.org/conflicts/iran-2026 — independent re-fetch confirms every key figure verbatim, NOT memory-fabricated; the B8a Timeline fabrication is FIXED); prune lever FIRED (pruned B3, first time); stop_research fired layer 1; citation purity clean (url_guard 0 removed, 0 fabricated survive); coverage 100%; single-doc substantive (18,745B, 24 headings, 65 dates, 78 figures). RESIDUALS (honest): (1) BREADTH COLLAPSED to 1 source (was 6) — the decompose-first SCOPED-CHILDREN path did NOT author children this run (model variance: model authored 0 scoped children) → safe FALLBACK to a single bounded root gather (102s not 70min). So burn fixed but fan-out lost this run = thin for deep research; needs the decompose to RELIABLY author scoped children to restore multi-source breadth WITHOUT the burn. (2) THEMATIC DUPLICATE-TAIL persists (WRITE-side): triple-Section-3 symptom gone but doc has TWO h1s + repeated cost/casualty material — first write node (n1) over-produces a whole mini-doc, n2/n3 append parallel dup sections; structural dedup catches DOCTYPE/html/ids NOT thematic dup (same blind spot). (3) FINAL-SECTION TRUNCATION (WRITE-side num_predict): report ends mid-sentence. All residuals are WRITE-side or decompose-reliability, NOT a research-loop regression. The seed-only-root + 7min node_timeout + as-completed harvest + MiniLM relevance-select + empty-node-unsupported fixes all hold; UNSUPPORTED mechanism present+offline-proven, untriggered here (no node timed out). DECISION surfaced to user: fix breadth(decompose-children reliability)+write-side(thematic-dedup+truncation) before continuing, vs run B8b/c/d first, vs accept. Planner HOLD-after-B8a2 (d98 UI boundary still pending).
--- LBD[84] ---
d111 — B8a2 FORENSIC (read-only trace analysis of var/traces/6677dbc465c04c42a875677eaf92e558.json; full report in this session) CORRECTS d110's "model variance" framing and reframes the residuals. THE MODEL'S REASONING IS SOUND; THE GLUE CODE IS BROKEN IN TWO PLACES. Findings, evidence-grounded:
(1) The "0 scoped children" at decompose was NOT model variance — it is a PARSER BUG. The model authored a VALID expand_branch JSON, but PREPENDED a one-line prose plan (which _DECOMPOSE_INSTRUCTION itself invites: "write a one-line plan as plain prose"). parse_tree_call (research_tree.py:332-334) requires the stripped reply to START WITH '{'; the leading prose made it return None → run_decompose_node treated the turn as the final-prose-plan and break'd → 0 branches → documented fallback gathered the WHOLE GOAL as ONE root leaf → exactly one fetch. This is THE root cause of thin breadth (1 source vs 6). The model's CoT had correctly decomposed into 3 scoped sub-questions. SELF-INFLICTED prompt/parser conflict (prompt asks ONLY-JSON tool turns AND a prose plan in the same turn).
(2) Stop-at-one-source was MOSTLY AN ACCIDENT with a genuine-sufficiency rationalization layered ON TOP. The root node DID produce a real, articulate CoT ("This source is extremely rich… I have enough material… I do not need to fetch more sources") — sound judgement — but it only had to make that call because the tree had ALREADY collapsed to one leaf via bug (1). It RATIFIED the single-source outcome, did not cause it.
(3) The decision node fired stop_research BLIND. Its planner context showed "branch root… contributes: 0 claims, 0 sources" and "DOCUMENT OUTLINE (none yet)" — the root's 8,554 chars of real findings were NEVER REGISTERED into the tree-memory the planner reads. Its own CoT said B1/B2/B4 still "need concrete data," yet it stopped on OUTLINE-COVERAGE logic ("I've scoped the pillars") = conflated "scoped the work" with "work is done." So research-side review (expand B1/B2/B4, prune B3, stop) operated on an EMPTY findings view; expanded branches were then abandoned un-gathered (depth_reached 1/3).
(4) REVIEW happened on TWO levels: (a) research-tree review = YES but BLIND (no corrective loop; once stop fired, un-gathered branches abandoned); (b) HTML review via section_verify = YES AND WORKS — the strict fact-checker caught a HEAVILY memory-fabricated FIRST draft (fake Senate 53-47 vote, F-15E friendly-fire shootdown, USS Spruance seizure, Putin/St-Petersburg, wrong casualties 13/1701 vs source's 15 KIA/538 wounded) and revised it (grounded=false→unbacked→2 passes→revised=true on all 3 nodes; url_guard_removed=0 survived). HONESTY CORRECTION to B8a2 writeup: the served doc's "grounded, not fabricated" is an ARTIFACT of section_verify catching+stripping fabrication, NOT a writer grounded from the start. The write-side verify→revise loop is the one feedback loop that demonstrably DETECTS AND CORRECTS in-run.
FIX TARGETS (priority): FIX-A decompose parser/prompt mismatch — tolerate leading prose (extract first balanced {…} even when not at position 0) OR forbid inline prose on tool turns → restores the intended 3-way fan-out (real multi-source breadth). FIX-B register root findings into tree-memory so the planner's expand/prune/stop + sufficiency judgement run on REAL data not a blind "0 sources" view. FIX-C (write-side, from d110) duplicate-tail (n1 over-produces a mini-doc) + final-section num_predict truncation. RELEVANCE TO TOMORROW'S VISION (user): the autonomous local React-app build (read own created files, iterate, Rx signals to planner) needs exactly this machinery — state registered into the memory the planner reads (FIX-B), working fan-out (FIX-A), and a review→fix loop (section_verify proves the pattern works). The broken parts (blind planner, parser-killed fan-out) are precisely what would break the React build. Surfaced to user for discussion (user: "investigate and report back, then we can discuss").
--- LBD[85] ---
d112 — CODE-VERIFIED mechanisms behind the B8a2 behavior (read-only code investigation; corrects/sharpens d111). Four confirmed facts, all with file:line:
(Q1 TOOL-CALL ARCHITECTURE) These are NOT native tool calls. NO `tools=` arg is passed to any chat call anywhere in the repo (transport.py _chat_native/_chat_openai build the Ollama body with messages/options/format/think only; no tool_calls ever read back). It is a homegrown PROMPT-AND-PARSE-JSON protocol: the model is told "reply with ONLY a JSON object and NOTHING else" (research_tree.py:553/728). The parser parse_tree_call (research_tree.py:324-360) strips code-fences then `if not s.startswith("{"): return None` (line 333) — and the balanced-brace extractor `_first_json_object` (research_tree.py:296-310) that COULD recover JSON embedded after prose is placed AFTER that gate, so it is never reached. ORDERING BUG. The identical gate exists in the research-loop parser `_parse_research_call` (runtime.py:951-953). A None return is silently treated as "final prose plan" → decompose loop ends with 0 children. think=True returns CoT in a SEPARATE field message.thinking (transport.py:633-635), NOT inline — so the prose that broke the parse was ordinary CONTENT prose the model emitted alongside the JSON, not leaked think. So: the sanitizer EXISTS but is gated out; fix = drop the position-0 gate (use the existing balanced-brace extractor) OR switch to native tool_calls. (Prompt-JSON was a deliberate choice: "small args the small model emits reliably, unlike content-laden JSON" runtime.py:1212-1213.)
(Q2 RESEARCH MEMORY — the user's correction is RIGHT) The memory layer EXISTS: class ResearchState (research_tree.py:389-537), append-only JSONL (d49 pattern). append_leaf stores BOTH a raw findings_digest STRING and a structured notes list (key_claims, source_trust). BUT the planner's per-branch counter render_for_decision (research_tree.py:513-520) reads ONLY notes/key_claims ("N claims" = sum of key_claims; "M sources" = len(notes)) — it NEVER looks at findings_digest. Findings (prose, the loop's normal final-turn exit, runtime.py:1275-1301) and notes (a SEPARATE `note` tool call the model must emit per fetch, runtime.py:279-289/1161-1202) are produced by two INDEPENDENT paths. tool_value gets article_notes ONLY if notes non-empty (runtime.py:1350-1354). So a leaf that wrote 8,554 chars of findings but emitted 0 `note` calls → notes=[] → contributes "0 claims, 0 sources" despite rich findings. NO findings→claims fallback exists. The gather closure is the SAME for root and child branches (not a code-path bypass); the fragility is that note-registration depends on the model VOLUNTARILY emitting separate `note` JSON turns, which the whole-goal single-root fallback (one unfocused node) is most likely to skip. LIKELY SHARED ROOT CAUSE w/ Q1: `note` calls are parsed by the SAME brittle `_parse_research_call` startswith('{') gate (runtime.py:951-953), so any note emitted with leading prose is ALSO silently discarded — meaning ONE parser-robustness fix could restore BOTH fan-out AND memory-population. (Inferred: whether the model tried-and-was-discarded vs never-emitted notes on this run is unconfirmed; mechanism is plausible and the fix helps regardless.) So FIX-B is NOT "build memory" — it is "the planner reads only the structured-notes counter, which is empty when note-emission fails; bridge findings→claims and/or make note parsing robust and/or let the planner SEE findings_digest."
(Q3 DUPLICATION REVIEW — user's intuition is RIGHT) The verify lane (claim_verify.py verify_claims/verify_and_revise, prompt _VERIFY_INSTRUCTION:155-171) is a STRICT FACT-CHECKER scoped to claim→source grounding ONLY (verdict ok|revise+unbacked[]); _REVISE_INSTRUCTION:331-342 explicitly says "Keep the report's structure/format intact." NOTHING in the review checks repeated sections / two h1s / thematic duplication. Dedup is FULLY DETERMINISTIC: collapse_duplicate_sections + collapse_outline_duplicate_sections (synth_tools.py:1020-1066, invoked agentic.py:1475-1481), heading-family/outline-slot string matching — and the code ITSELF documents the thematic-dup MISS (synth_tools.py:1070-1082: "Cost Analysis" vs "Cost and Damage Assessment" drift past the family-overlap threshold). NO agent EVER reviews the whole assembled document for structure/duplication. Fix direction: add an agent review pass over the assembled artifact (structure/dedup/coherence), not just per-section fact-grounding.
(Q4) think=True on ALL research/decision/decompose/write model turns (research_tree.py:585; agentic.py:1254); think=False only on the structured planner (app.py:312/457) to avoid CoT eating num_predict.
UNIFYING READ: the model's REASONING is sound; the failures are in the hand-rolled JSON-text tool protocol (brittle extraction starves both fan-out and memory) and in the too-narrow review scope (fact-only, never structural). All three map directly onto tomorrow's autonomous-React-build vision: reliable intent→action dispatch, planner sees what was actually produced/created, and whole-artifact review. Surfaced to user for architecture discussion before any fix dispatch; comprehension-recheck (curiosity, fresh shell) still owed before the next planner.
--- LBD[86] ---
d113 — USER DECISION (2026-06-23): SWITCH the deep-research tool surface from the homegrown prompt-and-parse-JSON protocol to NATIVE Ollama tool-calls (pass `tools=[...]`, read `message.tool_calls`) — BUT TEST gemma-E4B native-tool-call RELIABILITY FIRST and gate the switch on the result. Rationale: native tool-calls are the durable foundation for the autonomous-build future (structured intent→action, no text-parsing fragility) and eliminate the entire class of failure found in d112 (brittle startswith('{') extraction silently dropping valid expand_branch/note calls → 0 children + blind planner). The code's existing prompt-JSON choice was a deliberate "small model emits small args reliably" bet (runtime.py:1212-1213); the user is overriding it toward native, contingent on E4B actually emitting reliable native tool_calls for OUR tool surface (expand_branch/prune_branch/stop_research/add_section/drop_section/note/web_search/web_fetch + the write-side file_write/section_verify). SEQUENCE: (1) empirical E4B native-tool-call reliability probe FIRST (does E4B emit well-formed tool_calls, with correct tool names + args, across our tool schemas, under think=True at num_ctx 32768? measure malformed-rate / missed-call-rate vs the current prompt-JSON baseline). (2) IF reliable → migrate dispatch to native tool_calls (fixes FIX-A and likely FIX-B's note-starvation in one move). IF NOT reliable → fall back to FIX-A-lite (drop the position-0 gate / use the existing balanced-brace extractor) as the robust-parse path. FIX-B (planner sees reality: findings→claims bridge + robust note capture so a node with real findings can never read "0 sources") and FIX-C (whole-artifact agent review pass over the assembled document for structure/duplication/coherence, not just per-section fact-grounding) proceed per d112's framing. Behavior-first; UI HARD-HOLD (d98) still stands. Comprehension-recheck curiosity consult owed before the build planner dispatches.
--- LBD[87] ---
d114 — USER MAJOR RE-GROUNDING (2026-06-23) of the TARGET ARCHITECTURE + a load-bearing VERIFY ask. This sharpens d46/d50/d94/d107 into a concrete generic plug-n-play spec and overrides parts of d113's fix framing.
ANSWERS to the four open gates: (Q1 tool-call gate/authority) The agent drives the flow AUTONOMOUSLY; BUGS CANNOT BE TOLERATED especially at tool calls — that is the bar (correctness, not a tolerance %). (Q2 blast radius) If native tool-calls work for E4B, migrate EVERYTHING to native — uniform, including the verify gate (overrides d112/d113 "only broken paths"). (Q4 FIX-C) The reviewer is NOT a bespoke whole-artifact pass — it is a GENERIC WORKER given the SAME SPEC as the workers (so it knows what to check), plus proper READ/WRITE/UPDATE tools, optimized for LOCAL agent use. The fix is ensuring those file tools exist + are locally optimized.
TARGET ARCHITECTURE (user, verbatim intent): the agent is given tools + shapes + specs + the whole framework we built. It has ROLES (planner, worker, reviewer); EACH ROLE OPERATES IN ITS OWN MEMORY LAYER. The agent-as-a-whole IS the planner, driving the flow via the shape+spec combo. A SHAPE = the THOUGHT PROCESS / plan template: what kind of plan fits the requirement, what steps to follow, how to iterate over the plan, WHEN TO EXPAND, WHEN TO STOP, WHAT TO REACT TO (e.g. the deep-research shape). A SPEC = the METHODOLOGY: like a system prompt but variable, and MULTIPLE specs can be sent to one worker; tells workers what treatment to apply to the data. The planner SEEDS the plan first and FILLS IT GRADUALLY using tools, then the plan executes. The planner MUST add a VERIFY GATE regardless of shape to verify the workers didn't fabricate or fail; the verify gate/reviewer gets the SAME SPEC so it knows what to check. Any event emitted from execution reaches the planner via the Rx LAYER; the planner REACTS and launches a CORRECTIVE plan using the shape+spec combo. eda-base3 adds a DISCOVERY MECHANISM that shares the shapes+specs registry to the planner → reduces hallucination. GOAL: make the agent GENERIC and a TRUE PLUG-N-PLAY architecture — when agent-in-the-loop works end-to-end it handles ANY query and outputs ANY kind of data given proper tools + a proper registry of shapes/specs.
THE VERIFY ASK (load-bearing, THE crux): the user SUSPECTS a FABRICATED deep-research flow crept in — a bespoke agentic loop coded JUST for deep research (research_tree.py: _DECOMPOSE_INSTRUCTION/_DECISION_INSTRUCTION, expand_branch/prune_branch/stop_research, the hardcoded gather/decision control-flow, run_research_tree/run_plan_chain) INSTEAD OF achieving deep research via the generic tools+shapes+specs combination. MUST VERIFY: is the deep-research path generic (planner reads a deep-research SHAPE as data + applies specs + uses generic tools + adds a verify gate + reacts via Rx) or is it a hardcoded special-case engine? How far is the current code from the target architecture? The SWA-driven chunked_read tool is a LEGIT generic tool (keep) — but the deep-research agentic loop is the suspected fabrication. This verification determines whether the next build is a PARSER PATCH (d113) or a REFACTOR toward the generic architecture. Native-tool-calls migration (d113) still stands but is now in service of the generic architecture, and FIX-B (planner sees real findings) is really "the planner's memory layer must reflect what workers actually produced." Behavior-first; UI HARD-HOLD (d98) stands.
--- LBD[88] ---
d115 — ARCHITECTURE AUDIT VERDICT (read-only, full report in session) + USER DECISIONS (curiosity round-2). THE AUDIT CONFIRMS THE USER'S SUSPICION. 
VERDICT: the deep-research REPORT path is a FABRICATED, deep-research-specific Python engine. For ANY report/file-shaped query, the genuinely-generic `unroll_shape` DAG is built then DISCARDED (agentic.py:1936-1954 docstring literally says the unrolled shape "is no longer the research topology here — the tree loop replaces it") and the bespoke `run_research_tree`+`run_decision_node`+`run_decompose_node` runs instead, governed by HARDCODED Python: the layer loop (research_tree.py:904), TreeConfig env-vars not the shape (depth from RA_TREE_DEPTH, agentic.py:1948; shape contributes only ONE integer = depth, agentic.py:1571-1573; the shape's round_roles/final_roles/max_iter/hard_cap are INERT on this path), and the hardcoded _DECISION_INSTRUCTION (research_tree.py:543) + _DECOMPOSE_INSTRUCTION (:721). The research-only tools (expand_branch/prune_branch/set_next_direction/stop_research/add_section/drop_section, research_tree.py:285) mutate a research-specific Tree/Branch and are callable only from the decision/decompose nodes — a different shape ("build a React app") could NOT reuse this loop. The genuinely-generic engine (IncrementalPlanner + AgentRuntime + synchronous self-heal/replan + discovery) EXISTS and is good — the report path just bypasses it. MITIGATION: the STOP CRITERION is partly spec-data-driven (the deep-research spec methodology "identify→find→verify→stop-when-sufficient" is fed to the decision node as methodology=, agentic.py:1569 / research_tree.py:593) — so when-to-stop is reasoned over spec data even though the loop that asks is hardcoded.
GAP MAP: ALREADY-GENERIC: spec→worker (N specs compose, runtime.py:694-715), spec→reviewer per-node gate (same spec, runtime.py:2497-2502), discovery registry (factory.py:432-455, shape_selector.py:293). PARTIAL: shape-as-data (drives selection + the depth integer, but report path ignores the rest). FABRICATED/HARDCODED: the iteration loop (research-tree-specific), the deep-research VERIFY LANE (fixed fact-checker _VERIFY_INSTRUCTION claim_verify.py:155, SPEC-BLIND — does NOT get the worker's spec, unlike the per-node gate), the verify-gate wiring (hardcoded call sites agentic.py:1263/1430, not planner-decided per shape). ABSENT: Rx corrective loop (EventPlane exists reactive_tools/event_plane.py but planner does NOT subscribe; self-heal is synchronous in-call runtime.py:3274-3320, not event-driven corrective re-plan), role-scoped memory (shared chat/goal/ResearchState; planner is memoryless; worker+reviewer share context — no per-role layer).
USER DECISIONS: (1) THE LINE: NO hardcoded behavior path — shapes+specs+tools are what the agent uses to solve ANY problem; do NOT hardcode the agent to follow a path like deep research; a simple question ("what is the weather today?") must be answered DIRECTLY; "chat sse with NO specialization is the FALLBACK when no specializations exist." Routing must NOT force deep-research. (2) REFINE not retract: keep the deep-research TOOLS (using them is not prohibited; they MAY be rewritten to best meet requirements) BUT the behavior must be driven via the SEEDED DEEP-RESEARCH SPECIALIZATION (spec/shape), not hardcoded in the agent. (3) SEQUENCE: behavior-first, THEN refactor. (4) PARITY BAR: the report must be at least as detailed as the cited source https://www.warcosts.org/conflicts/iran-2026; the agent should be able to produce ANY type of article BY ITSELF — it can choose to write key details to a STRUCTURED JSON FILE and treat UI/HTML as a SEPARATE concern, defined+achieved in the HTML SPECIALIZATION, using the file READ/WRITE/UPDATE tools.
IMPLICATION (two-phase): PHASE 1 behavior-first on the CURRENT loop — native Ollama tool-calls (zero-bug bar) + planner-sees-real-findings (memory reflects what workers produced) + generic-reviewer-with-same-spec + warcosts-parity report quality via the structured-JSON-data → HTML-specialization separation (also kills the duplicate-tail + truncation by separating data from presentation). KEEP d110's working pipeline; don't regress. PHASE 2 refactor — consolidate the deep-research path onto the generic shape+spec+tools engine: extract expand/prune/decompose/stop into a generic shape-driven planner loop whose round structure/depth/stop policy come from the deep-research shape+spec; stop discarding the unrolled DAG; make _DECISION/_DECOMPOSE instructions spec/shape-supplied data; make the verify lane SPEC-AWARE; add role-scoped memory + a planner Rx subscription for event-driven corrective re-plan; ensure routing does not force deep-research (simple Q → direct answer / chat-SSE fallback). The warcosts-parity bar GATES the Phase-2 swap (refactored path must be >= today's d110 report before it replaces the bespoke loop). Refactor is a deep cut but CONSOLIDATES onto existing generic primitives, not build-from-scratch. Behavior-first; UI HARD-HOLD (d98) stands.
--- LBD[89] ---
d116 — P1-probe RESULT (read-only, .s13_design/p1_native_toolcall_probe.md): E4B NATIVE OLLAMA TOOL-CALL RELIABILITY = PASS, recommendation GO-NATIVE. Native path: 19/19 trials across the full 11-tool surface (expand_branch/prune_branch/set_next_direction/stop_research/add_section/drop_section/note/web_search/web_fetch/file_write/section_verify) emitted exactly ONE well-formed tool_call — correct name, all required args present + well-typed, sensible grounded values. ZERO malformed, ZERO missed, ZERO wrong-name. think + tool_calls COEXIST (18/19 had a separate thinking field; the 1 without still produced a correct call). Decision tools sampled at temp 0/0.5/1.0; rest at temp 0; native via tools=[...] array, read message.tool_calls, num_ctx 32768, num_predict 4096, native Ollama :11434 (resident, ~3.5GB/6.1GB, no reload/swap). KEY: native tool calls arrive in a STRUCTURED tool_calls field SEPARATE from content prose — in the one trial with 741 chars of co-emitted prose the call STILL arrived clean → the d111/d112 prose-drop failure is ARCHITECTURALLY IMPOSSIBLE on native, not merely absent. Latency ~6.9s mean / 14.6s max. BASELINE (current prompt-and-parse-JSON): 15/19 = 21% MISSED-CALL rate, ALL 4 misses on the decision/outline tools (expand_branch, stop_research@temp0.5, add_section, drop_section) — model answered pure prose, emitted NO JSON at all → exactly the d111 collapse (no-tool turn → 0 children / premature stop / blind planner); the forgiving balanced-brace parser recovered none (no JSON existed to recover). Against the d114 zero-bug bar native PASSES and prompt-JSON FAILS. Migrating fixes FIX-A locus + the note-starvation in one move and is the structured-intent foundation for the autonomous-build target. Two non-blocking caveats: (1) 19-trial resident-model sample — do a short confirmatory MULTI-LOAD native run during migration (drop-immunity is a transport property, not load-dependent, so not expected to be load-fragile); (2) KEEP the balanced-brace robust parser as a defensive fallback for any non-native path (don't delete parse logic). Surfaced to user for the go-native-vs-fallback call (user holds go/no-go per d113); nothing migrates until they relay. s13 HOLDING at the gate.
--- LBD[90] ---
d117 — USER GO-NATIVE (2026-06-23), the P1-probe gate decision. User chose GO NATIVE on the d116 probe result (E4B native tool-calls 19/19 vs prompt-JSON baseline's 21% miss on the decision tools). Migration AUTHORIZED: migrate the deep-research tool dispatch to native Ollama tool-calls (pass tools=[...], read message.tool_calls, replace the startswith('{') string-parse gate). Two conditions carried from d116: (1) include a short confirmatory MULTI-LOAD native run during migration; (2) KEEP the balanced-brace robust parser as a defensive fallback for any non-native path (do not delete parse logic). Proceeds into P1-impl → P1-findings (planner sees real findings) → P1-reviewer (verify lane gets worker's spec) → P1-report (warcosts-parity, raw content) → P1-review → P1-validate. All Phase-1 guardrails stand (d50 raw content, d65 flag-free, d110 no-regress, d98 UI HARD-HELD, security :11434-only/keep-E4B/preserve-state). Phase 2 generic refactor remains HELD.
--- LBD[91] ---
d118 — P1-impl DONE + GREEN (native Ollama tool-call migration, d117). Native tool_calls now PRIMARY across the decision loop, decompose loop, and research/gather/note loop: real tool schemas in the chat body, structured message.tool_calls read off the response, startswith('{') gate DEMOTED to defensive fallback (not deleted). Both gate conditions met: (1) multi-load run 3/3 clean across genuine COLD loads (keep_alive evict-after-each) → native delivery confirmed a transport property surviving fresh loads; (2) balanced-brace _first_json_object parser KEPT + a test drives a non-native reply through it. 7 new s13 tests pass incl. the decisive DROP-IMMUNITY proof (native call WITH leading prose dispatched; asserted the old string parser WOULD have dropped that same reply) + decompose authors 2 scoped children from native replies. 368 runtime/framework tests green, NO regression. d50/d65/d110 + security honored (native :11434 only, E4B kept).
SCOPING RECONCILIATION (neuron call against d114): the worker left the WRITE side (file_write) AND section_verify on the OLD prompt-parse path, reasoning both emit raw content. file_write = CORRECT to stay raw (it carries raw document content; native tool-call JSON would violate d50). section_verify = DIFFERENT: it returns a STRUCTURED VERDICT (supported bool + issues list), NOT raw document content; it was IN the d114 "migrate everything incl. the verify gate" surface AND was proven native-clean in the d116 probe (part of the 19/19). Leaving it on prompt-parse is a RESIDUAL DROP-RISK against the d114 zero-bug bar (a verify verdict emitted prose-then-JSON could be silently dropped → silent verify failure). NEURON STEER: section_verify must end up on NATIVE — but FOLD this into P1-reviewer (FIX-C reworks the verify lane to be spec-aware anyway, so migrate-to-native + spec-aware happen together there), and CONFIRM at P1-review. Do NOT re-bounce P1-impl. So: file_write stays raw (d50); section_verify → native + spec-aware via P1-reviewer; P1-review verifies both calls. Phase-1 build wave continues: P1-findings → P1-reviewer (now also carries the section_verify native migration) → P1-report → P1-review → P1-validate.
--- LBD[92] ---
d119 — USER LIVE-TEST OBSERVATIONS (2026-06-23, run during a Claude-Code downtime via the browser UI). Query: "Give me a detailed html report on the ongoing US Iran conflict". Artifact: C:\Projects\ReactiveAgents\artifacts\give-me-a-detailed-html-report-on-the-ongoing-us-i.html ; trace: C:\Projects\ReactiveAgents\var\traces\0d0d1cf242201f6697759f5dc18daf71.json . USER EXPLICIT INSTRUCTION: keep these readings held; do NOT act/fix or jump to conclusions; cross-verify read-only; DISCUSS only once the actual Phase-1 code fixes are in. Observations to cross-verify (all from the user, some self-flagged "not sure"): (1) a web_fetch failed with 403 and NO other web_fetch was tried as a fallback (fetch-failure has no retry/alternative-source path). (2) Only ONE web_fetch per ONE web_search. (3) Unsure whether the LLM picks the CORRECT link from those available. (4) Report probably generated from only TWO fetched articles. (5) Unsure if the web-SEARCH strategy is sound — does the tool expose all web-search params (boolean expressions, AND/OR, etc)? (6) Unsure if the web-FETCH strategy is sound — how is the LLM guided to use web_fetch? (7) Unsure if the agentic loop drives research properly if the LLM fetched two sources and then decided to close. (8) Unsure the LLM uses the RIGHT tools — it read fetched files via file_read; user did NOT see it call the read-chunks (chunked_read) tool. (9) Unsure what the agent reviewed, but the overall HTML was deduplicated at least. (10) Possibly ALL nodes were configured as HTML-WRITER nodes with NO deep-research nodes in the plan. (11) Unsure HOW the agent drives deep-research — via the planner using tools, or via shapes + dynamic nodes. CRITICAL CROSS-CHECK QUESTION the neuron must resolve: was this run on the OLD (pre-Phase-1) code or did it include the uncommitted Phase-1 changes (native tool_calls, planner-sees-findings, spec-aware reviewer, data->HTML separation)? Determines whether each observation is expected-pre-fix-baseline vs still-present-after-fix. MAPPING (preliminary, to confirm): obs 10/11 = the d115 fabricated-engine finding (report path runs bespoke run_research_tree; write phase = html-writer nodes); obs 2/4/7 = thin-breadth (d112/d115, Phase-1 FIX-A/native + Phase-2 generic loop); obs 8 = chunked_read may not be wired/used on this path (d109 chunk strategy) — file_read used instead; obs 1 = fetch-failure resilience = NOT in Phase-1 scope (candidate new work); obs 5/6 = web_search/web_fetch tool-surface + guidance soundness (candidate Phase-2 / tool-quality work); obs 9 = deterministic dedup ran (d112/d115 write-side). Cross-verification dispatched read-only; results to be HELD for the post-Phase-1 discussion, NOT surfaced as fixes now.
--- LBD[93] ---
d120 — CROSS-VERIFICATION of d119 (read-only forensic of trace 0d0d1cf2... + the served HTML; full analysis in session). HELD for post-Phase-1 discussion per user; NOT acted on.
CODE-VERSION VERDICT (very high confidence): the user's live UI run used the OLD, PRE-Phase-1 code. Evidence: legacy prompt-and-parse-JSON dispatch ("reply with ONLY a JSON object", parsed from content; native message.tool_calls token appears once, incidental; no tools=[...] passed); the "0 sources when findings exist" symptom present (planner authored an n1 node tasked "this section has NO supporting sources… empty/timed-out research node" while research DID yield); no structured-data→HTML separation (write path emits raw HTML from prose). So ALL 11 observations are the EXPECTED pre-fix baseline — Phase-1 is NOT deployed in the running app; the app needs a restart on the new code to show Phase-1 behavior.
OBSERVATION VERDICTS: (1) REFUTE as stated — alternative fetches WERE attempted on every failure (ohchr→cbsnews, ndtv→cnn, imf→already-had-cfr); failures surface as generic "Could not fetch <URL>. Try another source." with NO HTTP status exposed, so 403-specifically = CANNOT-TELL. (2) REFUTE — ratio was ~1 web_search : 2-3 web_fetch per node (fetch_cap=3), NOT 1:1; BUT only ONE search query per node, no query reformulation. (3) CONFIRM — link picks were reasonable (Brookings over Wikipedia, CFR on-topic, etc; thinking shows explicit relevance ranking). (4) CONFIRM and worse — ~5 sources fetched but the SERVED report is grounded in ~2 (CNN inline + CBS), and the final fact-check collapsed the Sources list to a SINGLE url (cbsnews.com). (5) web_search tool (reactive_tools/web_tools.py WebSearchArgs) exposes ONLY query + max_results(=8) + region(=us-en); NO boolean/AND-OR/expression operators; DuckDuckGo via ddgs; planner is told only the one-line desc, never told about max_results/region and never set them. (6) web_fetch guidance: "search, then READ the most relevant by fetching URLs (up to 3 fetches), fetch before relying, never cite a page you haven't read, ONE tool call per turn"; nothing about retry strategy or query diversification. (7) CONFIRM shallow — each node: 1 search→2-3 fetches→converged at turn ~4 of 12, gather_more=0; AND 2 of 4 research nodes (B1 roots, B3 economy) were CANCELLED by the concurrency controller (not a timeout), so those sections shipped thin/UNSUPPORTED despite Brookings+CFR having been fetched. (8) REFUTE/nuanced — NO file_read OR chunked_read AGENT call anywhere; the write/synthesis path consumes prior content INLINE (injected into prompt); chunked_read.split_chunks IS used internally by web_fetch's read-side relevance selector ("found N relevant passages, reading top N"). So neither is an agent tool on this path; "file_read instead of chunked_read" doesn't match. (9) CONFIRM review ran (section_verify unbacked 7 then 2, url_guard, coverage_net, doc_reconciled=true) but DEDUP FAILED — served HTML has TWO <h1>s + a full second <!DOCTYPE> document + 4 "No sources were found" stubs + a stray ``` fence. ROOT CAUSE: the terminal n3 ground-or-remove rewrite hit done_reason:"length" (4096-token cap) mid-rewrite → truncated before emitting the reconciled single-doc → the malformed pre-reconcile concatenation got served. (10) REFUTE — BOTH node kinds present: 4 research nodes (shape concurrent-multi-topic-gathering, node.tool web_search, running research.react) + 3 write nodes (shape write-file, node.tool file_write); the html-writer spec is oddly stamped on the research nodes too. (11) KEY FINDING — this live run executed via the GENERIC shape-unrolled DAG (planner.select_shape → concurrent-multi-topic-gathering; write plan via planner.incremental "tool-driven" seed/add/finalize) + per-node research.react micro-loops; there are NO research_tree / run_decision_node spans. I.e. the LIVE UI report path here did NOT use the bespoke run_research_tree engine.
OTHER: ~70 model calls, ~30.4min wall-clock; the planner.goal explicitly warned "do NOT author a second parallel findings document" yet n1 over-produced a full multi-section doc with [1]/[2] citations it had no sources for, and n3 emitted a standalone <!DOCTYPE> doc with its own <h1> — the structural cause of the duplicate-title mess; section-verify caught real fabrications (a 2010s/2020s timeline with no source) but truncated before removing them, so the fabricated bullets shipped.
RECONCILIATION TO DO (HELD, the headline for discussion): this OLD-code live run used the generic shape-DAG path, whereas B8a2 + all Phase-1 work targeted run_research_tree via run_plan_chain (the d115 bespoke-engine path). Most-likely reconciliation = code-version: old live code routed report queries through the concurrent-multi-topic SHAPE, while the new s13 code routes through run_research_tree. MUST CONFIRM against current code: (a) does the current/Phase-1 report path route to run_research_tree, so P1-validate tests what the live UI will use AFTER a restart? (b) NOTE the irony for the user's generic-plug-n-play vision — the OLD shape-DAG path is arguably CLOSER to "driven by shapes+specs, not a hardcoded engine" than run_research_tree is; so the s13 move toward run_research_tree may be the very fabrication Phase-2 must undo. This bears on P1-validate interpretation AND Phase-2 scope. Surfaced minimally to user (version verdict + path-reconciliation flag); full 11-point readings HELD for the post-Phase-1 discussion.
--- LBD[94] ---
d121 — P1-validate d3_run1 = STRONG PASS (1 of 4 runs; consolidated verdict pending run2/run3/full-depth). THE CORE s13 WIN IS PROVEN LIVE on the served run_plan_chain path: (1) BREADTH RESTORED — decompose authored 5 scoped children B1-B5 LIVE (vs 0 in the d111 collapse); root no longer whole-goal gathered. (2) PLANNER SAW REAL FINDINGS — the render bridge (FIX-B) shows non-zero "9/15/3/9 claims (from findings)", zero blind 0/0. (3) 0 DROPPED tool calls across 5 children / 23 research tools. (4) Wall-clock 25.0min vs d110 27.7min vs old B8a 2.83h — NO regression, with MORE research. (5) Warcosts-parity bars met: parity-detail, NO dup-tail, NO truncation.
IMPORTANT MECHANISM NUANCE (corrects the d118 premise, touches d114/d117 'go native everywhere'): native tool_calls fired only 2 of 63 tool-offered turns (the decision set_next_direction + the decompose expand_branch). The KEPT balanced-brace fallback parser (gate condition 2) parsed ALL the research-loop tools (web_search/web_fetch/note) with ZERO drops. So the zero-drop OUTCOME holds and the d111 bug is fixed — but the actual MECHANISM is 'native-on-decisions + robust-fallback-on-research', NOT 'native primary everywhere' as d118 framed it. This VINDICATES keeping the fallback (it is LOAD-BEARING, not merely defensive — the real d111 fix is substantially the improved fallback parser, with native a bonus on decision turns). OPEN QUESTION for consolidation/discussion: WHY does native fire on only 2/63 turns — a wiring gap (research-loop tools not actually offered/read as native) or model behavior? If native should fire everywhere but doesn't, we are relying on the fallback more than 'migrate everything' intended. Worker confirming this across run2/run3 before finalizing.
TWO MINOR FINDINGS: (1) empty citation brackets + bare 'Source' on several claims = the KNOWN E4B long-report citation CEILING (a MODEL limit, not a code regression; all present URLs real, url_guard removed 0) — consistent with the d54/d55 long-path ceiling. (2) a Timeline sources-list not closed before the next section = a small write-side tag-balance residual.
PATH NOTE (connects to d120): P1-validate's run_plan_chain DID use the run_research_tree decompose (5 children authored) — confirming the new code's run_plan_chain routes through run_research_tree. The open d120 reconciliation (does the LIVE UI route a 'detailed html report' query to run_plan_chain/research_tree or to the generic shape-DAG?) remains for the post-Phase-1 discussion. Driver/instrumentation bug earlier (non-string next_direction slice) self-healed (str-coerce + persist-evidence-immediately safety-net); NOT an agent/served-path bug. HARD-HOLD at UI boundary (d98) stands.
--- LBD[95] ---
d122 — P1-validate 3-RUN BREADTH PROOF COMPLETE (runs 1-3 of 4; full-depth depth5 still running, then consolidated P1-validate.md + a-g verdict). VERDICT TREND: PASS-WITH-FINDINGS. THE CENTRAL s13 WIN IS PROVEN RELIABLE (not a one-run fluke): children = 5 / 3 / 3 across 3 consecutive LIVE depth-3 runs — ALL >=3, root never whole-goal gathered → the d111 parser-kill breadth-collapse (to 1 source) is DEFINITIVELY GONE. This MEETS the neuron acceptance bar (>=3 scoped children on 3 consecutive depth-3 runs, measured from raw traces). CORE HOLDS ALL 3 RUNS: 0 dropped tool calls every run; planner-saw-real-findings every run (render bridge 8-15 claims-from-findings + real notes, 0 blind 0/0); grounded (run2 fired UNSUPPORTED honestly); 25/30/33 min vs old B8a 2.83h — NO d110 regression; single-h1 + NO truncation every run (the GROSS dup-tail + truncation stay FIXED).
MECHANISM FINDING NOW CONFIRMED ACROSS ALL 3 RUNS (was the d121 run1 nuance): native tool_calls fire only 1-2x/run (on the decision/verify turns); the KEPT balanced-brace FALLBACK parser carries the entire research loop with 0 errors. So 'native primary everywhere' (d118/d114 framing) is NOT what's happening — it is 'native-on-decisions + robust-fallback-on-research', and the fallback is LOAD-BEARING. Open question for discussion: WHY native fires so rarely (wiring gap vs model behavior) and whether that matters given the zero-drop outcome holds.
RESIDUALS (all non-deterministic WRITE-SIDE/CITATION polish; NONE a research-loop regression or fabrication): (1) run3 only — a SUBTLE thematic dup (one h3 repeated under two different h2 parents; DISTINCT from the now-fixed gross dup-tail). (2) a CITATION LEAK — a Wikipedia internal #cite_ref anchor survived url_guard because its domain matches a real fetched source (an ungrounded URL FRAGMENT, not a fabricated claim). (3) run1/run2 — empty-[] / bare-'Source' citations = the KNOWN E4B long-report citation CEILING (a MODEL limit, consistent with d54/d55), with one tag-balance residual (unclosed Timeline sources list) seen in run1. Reports are genuinely multi-source + substantive (run1 drew Al Jazeera timeline + NPR cost data + Al Jazeera analysis). Consolidated go-decision pending full-depth + P1-validate.md; UI HARD-HELD (d98).
--- LBD[96] ---
d123 — PHASE 1 COMPLETE: P1-validate = PASS-WITH-FINDINGS, proven LIVE on the served run_plan_chain route across 4 runs (3 consecutive depth-3 + 1 full-depth), DEFAULT config (breadth pinned 3, NO flags per d65), gemma4-e4b @ :11434, UI untouched. Evidence .s13_design/P1-validate.md (14.7KB, read-not-scan + raw-trace per run). CENTRAL WIN PROVEN LIVE & RELIABLE: breadth restored — decompose authored 5/3/3 scoped children across the 3 consecutive runs + 3 on full-depth (vs 0 in the d111 collapse); root NEVER whole-goal gathered; 3-7 REAL sources/run (vs d110 single source); planner SAW real findings every run (0 blind 0/0; bridge live, e.g. '9/15/11 claims from findings'); 0 dropped tool calls every run; grounded (run2 fired UNSUPPORTED rather than fabricate); NO d110 regression (20-33min vs old 2.83h, now WITH multi-source breadth); single-<h1> + no-truncation every run; full-depth honored the depth knob (depth_configured=5, agent stopped on its OWN sufficiency at layer 1/5, NOT the cap). 3 of 4 runs fully clean.
HONEST d118 MECHANISM CORRECTION (MATERIAL): on the served path under FULL research context, E4B emits native tool_calls on only 1-2 of 41-63 tool-offered turns/run (the SHORT decision/decompose/verify turns); the KEPT balanced-brace fallback carries ALL research-loop tools with ZERO drops. So d118's 'native PRIMARY across the research/gather/note loop' is WRONG for the served path — native fires only on short turns; the robust fallback is the WORKHORSE. The d116 probe's 19/19 was measured in ISOLATION on short single-tool prompts; under long research context E4B reverts to content-JSON. The no-dropped-calls bar is STILL met (native-where-it-fires + fallback). Migration's value on the served research loop is SMALLER than the probe implied — but KEEPING the fallback (gate condition 2) was the right, load-bearing call. Corrects d118; no action needed beyond an accurate record.
FOUR FINDINGS (all intermittent WRITE-SIDE or MODEL; NONE a research-loop regression or fabrication): (1) CITATION CEILING — empty [] / bare 'Source' on longer reports (run1:2, run2:32, run3:0, fulldepth:0) = the KNOWN E4B long-report citation ceiling (MODEL limit; URLs it DOES attach are real, url_guard removed 0; consistent w/ d54/d55). Should NOT be patched by templating/injection (= circumvention); accept / scope / consider a model question later. (2) WIKIPEDIA ANCHOR LEAK — run3 only: a Wikipedia internal '#cite_ref-95' back-anchor survived url_guard because its DOMAIN matches a real fetched Wikipedia source = ungrounded URL FRAGMENT (not fabricated claim). SMALL deterministic CODE FIX available (domain+path / anti-#fragment check in strip_ungrounded_urls). 1/4. (3) SUBTLE h3 DUP — run3 only: h3 'Economic and Global Market Implications' under two h2 parents; the GROSS d110 dup-tail (2 h1s + cost-twice) is FIXED all 4 runs; this residual P1-report itself flagged as not deterministically dedupable without risking real content. 1/4. (4) TAG-BALANCE — runs 1/2: a Timeline <ul> not closed before the next <section>; renders fine (browser auto-closes), wrapper balanced, no truncation. Cosmetic.
GO-DECISION IS THE USER'S (planner HARD-HOLDING at the B9 UI boundary d98, not advancing). Options: accept Phase-1 as-is & proceed / fix cheap residuals first (finding-2 small code fix, finding-4 cosmetic) / treat finding-1 as a model decision / finding-3 known residual. Sequencing: B8b/c/d (pirate/mail/haiku) parked; Phase-2 generic refactor HELD; s12 independent verification follows. CRITICAL TIE-IN: the user's HELD live-test discussion (d119/d120) is now DUE (Phase-1 fixes are in, per the user's own condition), and the d120 PATH-RECONCILIATION (Phase-1 proven on run_plan_chain, but the user's live UI run used the generic shape-DAG route) is LOAD-BEARING for the go-decision — must confirm whether the current live UI routes a 'detailed html report' to run_plan_chain (what we validated) before 'accept & proceed' means the live UI actually improved.
--- LBD[97] ---
d124 — d120 PATH-RECONCILIATION RESOLVED (read-only static code trace by s13; no UI run, no edits). RESULT: the CURRENT served chat-app DOES route a 'detailed HTML report' query to run_plan_chain — the path P1-validate proved. So Phase-1's live proof MAPS to the live UI for that query class; the d120 gate is SATISFIED. MECHANISM: chat_app/chat_app/agentic.py run_agentic() line 632: `if wants_file and multi_page and not unmet_specs:` -> run_plan_chain(...). The shape selector model-EXTRACTS two intent signals — wants_file (user asked for a file) + multi_page (large/multi-page output); 'write me a detailed HTML report on X' sets BOTH -> run_plan_chain (research-tree breadth-3 + section-write -> saved report.html). Confirmed by chat_app/tests/test_c1b_plan_chain_routing.py::test_multipage_file_request_routes_to_plan_chain. RECONCILES with d120's finding that the user's earlier live run used the generic shape-DAG (concurrent-multi-topic) internally: that run was OLD pre-Phase-1 code, where run_plan_chain internally used the older shape-DAG; the NEW code's run_plan_chain uses run_research_tree (the FX-loop'd breadth-3 engine P1-validate proved). So after the user RESTARTS the app on the new code, the same 'detailed html report' query gets the validated research-tree path. HONEST CAVEAT (a SEPARATE item, NOT a Phase-1 gap): routing keys on MODEL-EXTRACTED wants_file/multi_page, not a hard flag. A deep-research-ish query that does NOT clearly signal file+multipage (or carries an unmet specialist) falls to the generic _run_acyclic path, which STREAMS synthesis to chat and does NOT save an HTML report file. So 'more query phrasings should reach the deep-research report path' is a ROUTING-QUALITY / intent-extraction question (shape-selector sensitivity) — a candidate Phase-2 (generic routing) item or a separate routing fix, distinct from the proven research-loop. Connects to d114 (no-forced-routing / reasoning-driven routing). NET FOR THE GO-DECISION: accepting Phase-1 genuinely means the live UI improved for the detailed-html-report class (gate satisfied); the only action needed to SEE it live is an app restart on the new code. Planner still HARD-HOLDING at the B9 UI boundary (d98); awaiting the user's go-decision.
--- LBD[98] ---
d125 — USER FRAMING (2026-06-24): TOOL-LAYER-FIRST philosophy + a structured Q&A discussion of LLM behavior, topic by topic (go-decision on Phase-1 DEFERRED until after this discussion; planner stays HARD-HELD). CORE THESIS: the TOOL LAYER is what supplies the DETERMINISTIC behavior that drives the LLM from one decision point to the next; SHAPES + SPECS are 'flavors' that tune how the LLM behaves on top of that. The research-as-TOOLS layer is MORE important than the prompt layer. The shape file is THIN: an (arbitrary) depth number + cross-cutting concerns; the LLM TRANSLATES those into the decision tree USING the tools — so the flow logic must live in the TOOLS (their descriptions + outputs), not in a heavy prompt or hardcoded control-flow. If the tool layer is genuinely good for LLM use, it drives sound behavior by itself.
TOPIC 1 = THE TOOL CALL LAYER. The user's concern checklist (the agenda to evaluate, grounded in real code): (1) Is the tool layer optimized for LLM use? (2) SMART WEB SEARCH — using expressions/boolean operators when necessary. (3) SMART WEB FETCH — check MULTIPLE sources from a single search + handle 403/404. (4) FILE/CHUNK READING — is it optimized, and does the LLM use a HYBRID/smart search-plus-read from a corpus? (5) RESEARCH TOOLS descriptive enough to drive the LLM decision-to-decision: identify what the topic is -> expand via what/why/when/how -> COLLAPSE the tree for bad leads. (6) CROSS-CUTTING CONCERNS expressible via shape/spec and honored by tools: e.g. 'never use Wikipedia', 'find reliable sources before beginning work', 'expand in timeline/costs/impact', 'avoid social-media/global-outlook/opinions'. (7) Do tools return MEANINGFUL OUTPUT — proper failure messages + structured/readable success content? (8) Are tools EFFICIENT for LLM use? (9) Are tool DESCRIPTIONS DISTINCTIVE enough to drive a DETERMINISTIC flow? (10) Are SHAPES + SPECS available via get-shapes/get-specs TOOLS with proper descriptions returned for LLM inference? (11) Is the PLANNER SYSTEM PROMPT efficient + focused on driving the tool layer, with a brief overview of what the shapes/specs layer is, why it matters, and how to iterate over shapes when an event is emitted?
RELEVANT GROUNDED EVIDENCE ALREADY ON HAND (from d120 cross-check + d115 audit, to verify/expand in the Q&A): web_search (reactive_tools/web_tools.py WebSearchArgs) exposes ONLY query + max_results(8) + region(us-en) — NO boolean/operator/expression support; DuckDuckGo via ddgs. web_fetch guidance = 'search, READ most relevant by fetching up to 3 URLs, fetch before relying, ONE tool call per turn' — fallback fetches on failure DO happen via the ReAct loop, but NO explicit 403/404 strategy in the description. chunked_read is NOT an agent-callable tool — it's used INTERNALLY by web_fetch's relevance selector (split_chunks + select_relevant_excerpt); the LLM does not call a read-chunks tool. The research/decision flow is currently driven by HARDCODED _DECISION_INSTRUCTION / _DECOMPOSE_INSTRUCTION PROMPTS (research_tree.py) — i.e. flow logic in the PROMPT, which is the OPPOSITE of the user's tool-descriptions-drive-the-flow thesis. discovery: factory.planner_context() carries a spec index + tool catalog (provided IN the planner context, not necessarily as callable get-shapes/get-specs tools). This framing strongly shapes PHASE 2 (generic, tool-driven) and may reframe it as 'make the tool layer self-sufficient to drive the flow'. A read-only tool-layer audit is dispatched to ground the Q&A answers.
--- LBD[99] ---
d126 — TOOL-LAYER AUDIT (read-only, full report in session) answering the d125 11-point checklist. HEADLINE: the deterministic MECHANICS are solid and well-built (native tool-calls, cached search w/ backoff, MiniLM relevance-select read, SSRF-guarded fetch, stateful re-prompting ack strings), BUT against the user's THESIS (flow logic in tool descriptions+outputs, thin prompts) the repo is currently the INVERSE — thin one-liner tool descriptions, HEAVY hardcoded PROMPT strings carry the flow (_DECISION_INSTRUCTION/_DECOMPOSE_INSTRUCTION/_RESEARCH_LOOP_INSTRUCTION), shapes/specs are static injected text (no discovery tools), and cross-cutting source policy lives in prompts not tool params.
SCORECARD (concern -> verdict + key fact): (1) optimized-for-LLM = PARTIAL — clean schemas + good success outputs, but the research LLM sees TRIMMED native specs that hide useful args (web_search max_results/region, web_fetch max_bytes; only query/url exposed). (2) web-search operators = GAP — plain query string only (DuckDuckGo ddgs); no boolean/phrase/site:/date support at schema. (3) web-fetch multi-source + 403/404 = PARTIAL — multi-fetch encouraged in the PROMPT (up to fetch_cap), HTTP status captured on SUCCESS but SWALLOWED on failure into a generic 'had no readable article text. Try another source.' (LLM never learns 403 vs 404 vs timeout); no tool-level retry/alternate-source (unlike web_search backoff). (4) file/chunk hybrid read = MET-BUT-HIDDEN — synth_tools.select_relevant_chunks (MiniLM-384d embed+cosine, ~3000-char chunks, verbatim top-N to ~20k-tok budget, 'M found/X read' coverage) primary + chunked_read map/reduce fallback; but INTERNAL to web_fetch (runtime SubAgent._read_fetched), NOT an agent-callable tool — the LLM doesn't drive a search-corpus-then-read step. file_read tool exists but reads the sandbox WORKSPACE, not fetched web content. (5) research tools descriptive-enough = GAP — TREE_TOOL_SPECS descriptions are one-liners; the identify->expand(what/why/when/how)->prune-bad-leads->stop-when-sufficient SEMANTICS live in the hardcoded _DECISION_INSTRUCTION/_DECOMPOSE_INSTRUCTION prompts (contradicts the thesis). PARTIAL credit: the OUTPUT acks are best-in-class (echo live branches/outline + legal next moves). (6) cross-cutting concerns at tool layer = GAP — no domain/trust/source-filter param, no expansion-bias input; ONLY a hardcoded Wikipedia->reference-untrusted tier override (article_note.classify_source_trust); 'never use Wikipedia / find reliable sources first / expand in timeline-costs-impact / avoid social-opinion' can only be injected as methodology PROMPT text (research_tree._methodology_block from a spec), and the decompose facet list is itself hardcoded. (7) meaningful output = MIXED — rich stateful success; generic failure (fetch loses HTTP code). (8) efficient-for-LLM = PARTIAL — one-tool/turn, cached search, trimmed specs, relevance reads keep context tight; but the heavy decision prompts are re-sent every layer. (9) distinctive descriptions for deterministic flow = GAP — thin labels; determinism comes from prompts + ack strings, not descriptions. (10) get-shapes/get-specs TOOLS = GAP — NONE exist (grep: zero ToolDefs); specs/shapes are static text injected via factory.planner_context ('REGISTERED SPECIALIZATIONS … names + descriptions only') + shape_selector inline list; planner cannot QUERY the catalog or see mid-run changes. (11) planner prompt = PARTIAL->GAP — strong DAG-authoring + spec-binding doctrine (FACTORY_DESCRIPTION), but NO shapes explanation and NO Rx/reconcile/event-iteration story; planner authors a DAG of nodes, not tool calls.
IMPLICATION: this SHARPENS Phase-2 from 'rewire the engine' to 'make the TOOL LAYER self-sufficient to drive the flow' — relocate decision semantics from _DECISION/_DECOMPOSE prompts INTO richer tool descriptions; add web_search operator/site-filter + web_fetch 403/404-surfacing + tool-level retry; expose chunked read as an agent-driven corpus search-then-read (or keep auto but make it inspectable); add domain/trust/expansion-bias PARAMS so cross-cutting concerns are tool-enforced not prompt-only; add get_shapes/get_specs discovery tools; rework the planner prompt to cover the shapes/specs layer + the Rx event-iteration loop. Key files: reactive_tools/web_tools.py, agent_runtime/{research_tree.py, runtime.py (SubAgent loop ~241-254/969-1218), synth_tools.py, chunked_read.py, article_note.py, claim_verify.py, factory.py, shape_selector.py}, reactive_tools/file_tools.py. Discussion item under d125 Topic 1; go-decision on Phase-1 still deferred; planner HARD-HELD.
--- LBD[100] ---
d127 — USER FRAMING Topic 2 (2026-06-24): the ORCHESTRATION MODEL (shapes, specializations, roles, synthesizer, context discipline). Enriches/sharpens d48/d114/d115. 
SHAPES = the overall METHODOLOGY: HOW the LLM should behave + drive the flow. Seeding a plan WITH a shape ADDS A FLOW so the LLM doesn't have to figure out HOW to solve the problem. Shapes are ABSTRACT to the LLM (big picture). Examples: deep-research (how to identify sources, how to search, what to look for, what to avoid) — currently THE FOCUS, and the ONLY shape that needs its OWN SET OF TOOLS; poc-iterate (HITL gate: build a small POC, get general user approval before proceeding so POC work isn't wasted; can loop the POC N times before the real build); diagnose-verify-fix (how to diagnose, identify the RCA, fix it). Once the planner SEEDS + SHAPES the plan, the EXECUTOR drives it; the planner then only worries about business logic.
SPECIALIZATIONS = just RULESETS. Examples: html-writer (prefer basic themes like sepia + card layout for SIMPLE reports; tabs + pagination for COMPLEX reports); pirate-speak (use pirate tone in output).
PLAN SEEDING = seed the plan with the RIGHT shape + attach a proper LIST OF SPECIALIZATIONS PER NODE, so the executor only has to launch each worker with the correct spec-list + CONTEXT. CONTEXT IS CRITICAL: the LLM is a 32k SLIDING WINDOW, so each worker node must get proper context (its INPUT, OUTPUT, and METHODOLOGY) to work INDEPENDENTLY.
ROLES: PLANNER = the global driver (LLM) that seeds a plan; only does business logic / orchestration. EXECUTOR = the FRAMEWORK (NOT an LLM) that executes the plan deterministically. WORKERS = LLM nodes that carry out small tasks (get spec-list + context). PLANNER COORDINATES on EMITTED EVENTS (worker failures / corrective behavior like self-healing or seeding a NEW plan) — orchestration only, reacts via the event/Rx layer. SYNTHESIZER = the FINAL output node; the planner determines FROM THE START which SPECIALIZATION it gets; the spec decides the OUTPUT MODE — return an ARTIFACT, send a MAIL, set a CRON job, or reply to chat via SSE. When the mode is NOT SSE, it performs the action and returns a SHORT text ack ('artifact available for download' / 'cron set up' / 'email sent'). The synthesizer ALSO gets a spec LIST and can compose (e.g. set a cron FOR sending email — valid).
CROSS-CUTTING: the TOOLS around these behaviors (synthesizer actions: artifact/mail/cron/sse; the shape/spec discovery) must ALSO be good (ties to Topic-1 d126). And because of the small context + SWA, ALL prompts — INCLUDING shapes and specs — must be CONCISE.
This is the user's full plug-n-play model: planner (LLM, seeds shape+per-node specs, reacts to events) -> executor (framework, drives) -> workers (LLM, specs+context) -> synthesizer (LLM, spec-determined output mode). Read-only audit dispatched to verify the code against this model. Still in the Q&A; Phase-1 go-decision DEFERRED; planner HARD-HELD.
--- LBD[101] ---
d128 — ORCHESTRATION-LAYER AUDIT (read-only, full report in session) answering the d127 model. HEADLINE: most of the user's orchestration model is ALREADY BUILT; the gaps are focused. TARGET-MATCH: 
SHAPES = PARTIAL — shapes are declarative TOML under shapes/ parsed by shapes.py (ShapeSpec: name/description/max_iter/hard_cap/round_roles/final_roles/edges/execution). 6 exist (linear, modular-parallel, deep-research, iterative-deep-research, iterative-writing-improvement, concurrent-multi-topic-gathering). deep-research.toml: max_iter=10, hard_cap=24, execution='deep-research', round_roles=['research','critic'], final_roles=['research','synthesis','verify']. poc-iterate = MISSING, diagnose-verify-fix = MISSING (2 of 3 named shapes unbuilt). HOW seeding adds a flow: an unrollable (cyclic) shape IS data the executor interprets (unroll_shape expands a bounded DAG, methodology injected as task-text framing via roles.position_framing); a linear/parallel shape's execution token is just a SEQUENTIAL-vs-CONCURRENT dispatch code-pick (scheduler.py). CONFIRMS d115: the inline deep-research answer uses unroll_shape + generic AgentRuntime, but the REPORT/file deliverable uses run_plan_chain -> run_research_tree (bespoke N4 layer-loop), NOT the declarative unroll — methodology-as-data holds only for the inline path; the flagship report path is bespoke Python.
SPECS = MATCH — .md+frontmatter under var/chat_app/specs/; a spec is explicitly an OUTPUT-SHAPING RULESET not a how-to (model.py:143-154). html-writer + pirate-tone bodies real. Per-node list + N-per-node confirmed (PlanNode.specs tuple; deterministic stacked composition under '===== Ruleset i/n =====' with reconcile preamble, runtime.py:723-744). Minor: the real html-writer body lacks the exact simple/complex (sepia-card vs tabs-pagination) branch the user described — concept matches, illustrative ruleset simpler.
PLANNER = PARTIAL (MATCH on seeds+orchestration-only; GAP on event reaction) — FACTORY_DESCRIPTION authors a DAG, body-free, shape chosen by a separate ShapeSelector call; owns heal_decision + replan_subgraph (business logic), never executes. Does NOT subscribe to events.
EXECUTOR = MATCH — AgentRuntime._drive_dag is deterministic framework (next_dispatch scheduler + asyncio tasks); no LLM picks what runs next; launches SubAgent workers with specs+context.
WORKER CONTEXT = MATCH (with budgeting) — _compose_task ordered assembly (goal / prior convo / task / inputs-from-prior clipped ~4000 / sources&findings / own tool output), direct-dep-scoped non-transitive; per-source + window-sized source budgets. Context discipline real. Caveat: the deep-research growing-visibility unroll re-accumulates prior layers; code flags 16k->32k overflow risk on that path.
EVENT/Rx = GAP (confirms prior) — planner does NOT subscribe; on failure the drive loop calls _heal_failed_node SYNCHRONOUSLY in-call -> heal_router.route -> planner.heal_decision in the same call stack; runtime is a one-way publisher; only an observe-only advisory lambda subscribes. Self-heal works but is RPC-style synchronous, not event-driven planner-reacts.
SYNTHESIZER = PARTIAL — distinct terminal ROLE_SYNTHESIZER; ALL 4 output modes exist as tools (file_write=artifact synth_tools.py:445; send_mail recipient hard-locked self; cron_add SQLite+in-proc scheduler; tool-less SSE terminal); non-SSE returns a structured {ok} short ack. DIVERGENCE from the user's model: output mode is TOOL-driven by the planner's REASONING (terminal node's `tool`, no flag — test_synthesizer_channel_d39_b4), NOT SPEC-driven; and the terminal node carries a SINGLE tool = one channel per run, so NO output-action COMPOSITION (no 'cron that sends email'). User wants spec-driven mode + composable outputs = 2 gaps.
CONCISENESS = PARTIAL — shape descriptions + spec bodies are lean (written for a small-context model); BUT runtime.py injects large STATIC instruction constants per node/role (_SHAPING_FRAMING, _REPORT_SEPARATION_GUIDANCE, _RULESET_RECONCILE_PREAMBLE, _RESEARCH_LOOP_INSTRUCTION, the multi-clause ROLE_SYNTHESIZER framing) — the biggest size offenders, re-sent per node; data-side budgeting is real, instructional scaffolding is verbose.
HEADLINE GAPS for Phase-2: (1) poc-iterate + diagnose-verify-fix shapes unbuilt; (2) planner is a synchronous collaborator, not an event-subscribing reactor (no true Rx corrective loop); (3) synthesizer output mode is tool/reasoning-driven not spec-driven AND cannot compose multiple output actions; (4) the flagship report path is bespoke run_research_tree, not the generic shape-unroll executor; (5) verbose static instruction scaffolding strains the SWA budget. NOTE the synthesizer fork is a genuine DESIGN CHOICE to discuss: current tool/reasoning-driven channel-pick aligns with d12/d14 (no hardcoded triggers, reasoning-driven) but the user wants spec-driven + composable. Topic-2 discussion item; Phase-1 go-decision still DEFERRED; planner HARD-HELD.
--- LBD[102] ---
d129 — USER Topic-2 DESIGN DECISIONS (2026-06-24) + a duplication investigation request. GUIDING PRINCIPLE: make the EXECUTOR cohesive WITHOUT taking away the LLM's ability to think — this stays AGENT-IN-THE-LOOP. Must DEFINE how the agent (planner) SEES PROGRESS. The user's explicit analogy: this is the SAME technique the EDP/neuron system uses right now — a planner seeds plans for steps, workers do small modules, the neuron coordinates via a MEMORY/CONTEXT layer over 100s of decision points, passing the right context to sub-agents. The framework provides cohesion; the LLM keeps the thinking.
DECISIONS: 
(1) poc-iterate + diagnose-verify-fix shapes = DROPPED from scope ('I dont need them now'). Focus stays deep-research.
(2) EVENT-DRIVEN PLANNER = BUILD IT. Keep it SIMPLE: emit success/failure (and similar) events so PARALLEL-processing nodes can be RECOVERED BEFORE A JOIN call (don't wait for the whole join to fail). Also: if a worker flags a CLARIFICATION, the planner SURFACES it to the user WHILE OTHER WORKERS KEEP WORKING — this is also HOW a HITL GATE is added when the requirement comes. So: real event subscription + planner reaction (recover/replan/surface-clarification), not the current synchronous in-call heal.
(3) REVIEWER + SYNTHESIZER as ROLES: both can be PER-WORKER or for the ENTIRE work, and live as TERMINAL nodes. The REVIEWER can IDENTIFY gaps AND FIX them. Because of the small context, FOLD the synthesizer INTO the worker + reviewer roles so the LLM behavior doesn't break (don't add a separate fragile synthesizer pass). FRAMEWORK-HANDLED REVIEW INJECTION (key simplification): the planner must NOT be stressed to add a reviewer per task or per plan — the FRAMEWORK does it: 'add work' => add work + review step; 'finalize plan' => add a final reviewer. The planner only seeds the basic SHAPE plan and fills CONTEXT + SPECIALIZATIONS. Resulting flows: PARALLEL shape = (work->review) || (work->review) -> final review; LINEAR/ITERATIVE shape = work->review->work->review->...->final review. SYNTHESIZER FIT = NEURON'S CALL; the user's LEAN (and likely best case) = there is NO separate synthesizer at all — the file_write tool does it: the worker gets only the FILE NAME, READS it, and EDITS it.
(4) DUPLICATION ROOT-CAUSE SUSPICION (explicit 'please look into this'): the user suspects the duplicate-tail comes from workers using file_write in APPEND mode and just writing the ORIGINAL content back again together with the new content -> duplication. PROPOSED FIX: provide a FILE UPDATE/EDIT tool with the SAME behavior as FRONTIER LLMs (read -> targeted edit/replace -> write; like the Edit tool), instead of blind append/rewrite. This would also enable the no-synthesizer 'worker reads the file and edits it' model in (3). A read-only investigation is dispatched to confirm the file_write mechanism + whether append/rewrite causes the dup, and whether a proper edit/update tool exists or is the gap.
STATUS: still in the Topic-2 discussion; these are DESIGN decisions for the upcoming Phase-2 (not yet a build dispatch). Phase-1 go-decision still DEFERRED; planner HARD-HELD. Connects to d112/d115/d126/d128 (the dup-tail was previously addressed with deterministic backstops in P1-report; the user now wants the ROOT-CAUSE fix via a proper edit tool instead).
--- LBD[103] ---
d130 — DUPLICATION ROOT-CAUSE INVESTIGATION (read-only; answers d129's 'please look into this'). VERDICT: the user's suspicion is RIGHT IN SPIRIT (append-mode build + content re-emission is the root) with ONE mechanical CORRECTION, and the proposed fix tool ALREADY EXISTS.
(1) file_write MECHANISM: file_write (reactive_tools/file_tools.py) HAS append + overwrite modes. append=True opens 'ab' and byte-appends at EOF, NO diffing — whatever content is passed is concatenated verbatim. The deep-research multi-section write loop (runtime.py synthesis.react_file ~1788-2592) DOES build by append: the FIRST write creates (append=False), EVERY subsequent turn appends (append = written_path is not None, runtime.py:2113). Cross-node continuation appends the same way.
(2) DUPLICATION MECHANISM = PARTIALLY CONFIRMED w/ correction. The suspicion's stated cause ('the worker is handed the prior document and the orchestration re-writes the original back with the new content') is REFUTED AS STATED: the worker is shown ONLY the TAIL (tail=1200 cross-node / 700 per-turn bytes), with explicit 'do NOT repeat earlier content' framing, and the append writes ONLY the current turn's emission, never prior+content. The REAL mechanism: the SMALL MODEL (E4B) IGNORES the 'do not repeat' instruction and RE-EMITS content it already wrote — a fresh <!DOCTYPE>...</html> (a 2nd document shell), a repeated <h1> title, or an already-written <h2> section family — and because the loop's DEFAULT action is append=True, that re-emission gets CONCATENATED onto the file. So: append-build + small-model re-emission -> re-emission appended -> duplicate-tail (the two-h1s + repeated-cost symptom). The code documents this exact defect (runtime.py:2042-2108; synth_tools.py:1086-1097). The deterministic backstops (in-loop drop guards begins_html_document/section_reemission/strip_wrapper_*, runtime.py:2042-2127; post-loop collapse_duplicate_sections + single-title _demote_extra_h1/reconcile_doc_structure, runtime.py:2331/2581-2591) are AFTER-THE-FACT cleanups papering over a write mechanism that fundamentally re-emits-and-appends.
(3) EDIT/UPDATE TOOL = ALREADY EXISTS (not a gap): file_update (file_tools.py:292-331 schema, 458-512 handler, 552-564 registry) — frontier-style str-replace: read -> str.replace(old,new,count) -> write back, REFUSES a missing 'old' (Anthropic-style). Built for the REVIEWER (tagged s13/P1 FIX-C, 'the reviewer's surgical ground-or-remove edit primitive — fix one flagged span without re-emitting the whole document'). file_read supports read-then-edit (offset/length/tail). THE READ->TARGETED-EDIT->WRITE LOOP IS ALREADY POSSIBLE. THE REAL GAP IS WIRING: file_update is NOT used by the deep-research synthesis WRITER loop (react_file is hardwired to file_write append + file_read; zero file_update calls in that loop). So writers still blind-append; the surgical edit primitive exists but isn't in the writers' path.
(4) FIX FEASIBILITY [inferred, design-level]: route the section-writers through an ANCHORED file_read->file_update insert (instead of blind file_write append) — a model that re-emits a prior section then either targets an 'old' that doesn't uniquely exist (refused) or replaces a span (no-op), so re-emission stops becoming a disk duplicate. This is STRUCTURAL PREVENTION at the concatenation step vs the current post-hoc trimming. Worker loop: create file -> each section: file_read(tail) to find the insertion anchor, file_update(old=anchor, new=section+anchor) to insert in place; the model never holds/re-emits the whole prior body — which is EXACTLY the user's no-synthesizer 'worker gets the filename, reads it, edits it' model (d129.3), now confirmed FEASIBLE because the primitive ships. CAVEATS: anchor must be reliably UNIQUE (trivial for HTML via </body>; markdown/plain-text needs a deterministic SENTINEL); read just the tail anchor (not the whole doc) to fit the 32k window; sandbox/SSRF/envelope guards carry over unchanged (file_update reuses file_write's _safe_resolve + guard_write_content); grounding/verify passes are orthogonal and remain. Topic-2 finding; informs the Phase-2 dup root-cause fix + the no-synthesizer worker-edits-file model. Phase-1 go-decision DEFERRED; planner HARD-HELD.
--- LBD[104] ---
d131 — USER FINAL Topic-2 REFINEMENTS (2026-06-24) — hard requirements for the deep-research shape/spec/tools in Phase 2. User signals readiness: 'we have enough knowledge now to know what to do' + confidence to proceed.
(1) DEEP-RESEARCH SHAPE must drive the LLM to KEEP POKING THE RIGHT QUESTIONS for the given query (the investigative loop we discussed: identify what/when/why/how -> find answers -> verify -> expand into the gaps). The STOP SIGNAL is DEFINED IN THE SHAPE (not hardcoded in code/prompt): the shape tells the LLM it must FILL IN ALL THE BLANKS before stopping = a COMPLETENESS-DRIVEN stop, not an arbitrary depth cap. This operationalizes moving the stop semantics OUT of the hardcoded _DECISION_INSTRUCTION and INTO the shape (ties to d126 #5/#9 and d115/d125 tool-drives-flow thesis).
(2) DATA-POINT / CONTEXT-GROWTH MODEL: a data-gathering step READS data and CREATES A DATA POINT that OTHER WORKERS can use. Research CONTEXT GROWS by asking the right questions of the query. User's example: 'the timeline mentions xyz events — but are ALL events accounted for?' = a COMPLETENESS/GAP probe that spawns follow-up questions. So: workers emit structured, reusable DATA POINTS (findings/claims) that feed downstream workers AND the planner's gap analysis; the loop keeps probing until the blanks are filled. (Generalizes FIX-B 'planner sees real findings' into 'workers produce data points that grow the shared research memory and drive the next questions'.)
(3) SEED THE DEEP-RESEARCH SPEC with the methodology the user described: identify what/when/why/how, FIND RELIABLE SOURCES FIRST, verify answers, fill the blanks, expand in the RIGHT dimensions (e.g. timeline/costs/impact) and avoid the wrong ones (social-media/opinions/global-outlook) — per d125 #5/#6.
(4) NO WIKIPEDIA — EMPHATIC, hard requirement: the user does NOT want Wikipedia cited in research ('wikipedia is a lie that is polluting the internet'). The deep-research spec/tools must EXCLUDE Wikipedia from sources/citations entirely — STRONGER than the current hardcoded reference-untrusted DOWNGRADE (d126 #6 / d128). Implement as a TOOL-LEVEL source domain DENY-LIST (web_search/web_fetch honor an excluded-domains list, configurable via the shape/spec cross-cutting concerns) + a spec instruction — i.e. the cross-cutting-concern-honored-at-tool-layer fix from d126 #6, with wikipedia.org (+ wikimedia/wiktionary) as the first banned domain. Cross-cutting concerns like this must be SHAPE/SPEC-expressible and TOOL-ENFORCED, not just prompt text.
STATUS: this CLOSES the design discussion (Topics 1+2). Next step = consolidate everything (d125/d126 tool-layer gaps, d127/d128 orchestration, d129 design decisions, d130 dup root-cause + file_update wiring, d131 deep-research shape/spec/no-wikipedia) into a PHASE-2 plan brief, run the owed unbiased curiosity re-grounding, and present the comprehension brief to the user for sign-off BEFORE dispatching the Phase-2 build planner. Phase-1 = PASS-WITH-FINDINGS (d123) recommended ACCEPT as the proven foundation; its residuals (Wikipedia leak, dup-tail) are subsumed by Phase-2. Planner currently HARD-HELD.
--- LBD[105] ---
d132 — USER 'ok go' (2026-06-24): ACCEPT Phase-1 as the proven foundation + AUTHORIZE Phase-2 (consolidate + curiosity re-grounding + build). 
PHASE-1 = ACCEPTED (d123 PASS-WITH-FINDINGS; central win proven live, d124 routing gate satisfied). Its residuals (Wikipedia leak, dup-tail, citation ceiling) are SUBSUMED by Phase-2. Planner released from the go-decision hold (UI HARD-HOLD d98 still stands).
PHASE-2 CONSOLIDATED SCOPE (the build, to be SEQUENCED + checkpointed; from Topics 1+2 audits/decisions d125-d131):
A. TOOL LAYER (d126): web_search — expose operators/expressions (boolean/phrase/site:/date) + max_results/region + a SOURCE DOMAIN DENY-LIST (no Wikipedia; configurable via shape/spec cross-cutting concerns). web_fetch — surface real 403/404/timeout failure + tool-level retry/alternate-source. Relocate research/decision FLOW SEMANTICS out of the hardcoded _DECISION_INSTRUCTION/_DECOMPOSE_INSTRUCTION INTO richer tool DESCRIPTIONS (descriptions drive the flow); keep the strong ack-string outputs. get_shapes/get_specs DISCOVERY TOOLS so the planner can query the catalog. Meaningful failure messages throughout.
B. ORCHESTRATION (d127/d128/d129): EVENT-DRIVEN PLANNER — planner SUBSCRIBES to the EventPlane; reacts to success/failure (recover parallel nodes BEFORE the join) + clarification (surface to user while other workers continue = HITL); replace the synchronous in-call heal. FRAMEWORK-INJECTED REVIEW — add-work => work+review, finalize => final-review; planner only seeds shape + context + per-node specs; reviewer identifies AND fixes gaps and is SPEC-AWARE. NO SEPARATE SYNTHESIZER — fold synthesis into worker+reviewer; output delivery (artifact/mail/cron/sse) stays a thin terminal concern.
C. DUP ROOT-CAUSE FIX (d130): WIRE the existing file_update (read->targeted str-replace->write) INTO the section-WRITER loop (currently blind file_write append). Anchored insert (HTML </body>; deterministic sentinel for markdown/text). Worker gets the filename, reads the tail anchor, edits in place = structural prevention vs the post-hoc backstops; realizes the no-synthesizer worker-edits-file model.
D. DEEP-RESEARCH SHAPE + SPEC (d131): shape DRIVES the LLM to keep poking the right gap-questions; STOP DEFINED IN THE SHAPE = 'fill all the blanks' (completeness-driven, not a hardcoded cap). Workers emit reusable DATA POINTS that grow the shared research memory + drive the next questions (timeline-completeness model). SEED the deep-research SPEC with the methodology (identify what/when/why/how, reliable-sources-first, verify, fill blanks, expand timeline/costs/impact, avoid social/opinions). NO WIKIPEDIA (tool deny-list per A).
E. CONSOLIDATE the report path onto the GENERIC engine (d115) — dissolve/replace the bespoke run_research_tree with the generic shape-driven loop, or at minimum make it shape/spec-DATA-driven (the biggest cut; sequence last, gated by parity).
GUARDRAILS: parity bar (>= Phase-1/warcosts output) gates any swap; behavior-first; keep E4B (no model swap); security (:11434 only, never :11435/foreign PIDs, preserve var/chat_app state, no broad kills); UI HARD-HELD (d98) until the user's UI-concerns discussion; no-fabrication (d60); flag-free end-state (d65, scaffolding-only flags ok mid-build); content RAW (d50). NEXT: curiosity re-grounding (owed) -> present comprehension brief (this scope) -> record signoff -> dispatch Phase-2 planner (sequenced).
--- LBD[106] ---
d133 — USER FINAL BUILD DIRECTIVES for Phase-2 (2026-06-24), resolving curiosity's 4 stress-test questions. 
CADENCE = MOSTLY AUTONOMOUS: build the whole sequence and review DEEP-RESEARCH QUALITY at the END (user spot-checks the served HTML; neuron uses TRACE FILES to understand what Gemma is thinking + how it picks flows, and develops/iterates independently). Surface to neuron->user MID-WAY ONLY if genuinely not confident or genuinely need help. Not per-sub-phase user gates.
TOOL LAYER = THE PRIORITY (user emphatic): BE CONFIDENT — 'this is quite doable now'. Write GOOD PROMPTS so task DECOMPOSITION + CONTEXT PASSING are smoothly handled. Make the tool layer RICH (ADD TOOLS where there is a miss) and WELL-DOCUMENTED (tool DESCRIPTIONS + OUTPUTS give enough meaning to enable AUTONOMOUS agentic behavior). TAP THE TRACES to fix tool descriptions/prompts iteratively.
ITEM E (engine consolidation): user did not object; neuron SAFE DEFAULT = build the generic engine behind a REVERSIBLE build-time flag + HARD PARITY GATE (>= Phase-1 result), keep the proven run_research_tree until the generic path matches; end-state flag-free (d65, scaffolding-only flag).
HITL = STRICTLY inside the kill-the-clarify-loop policy (o7/d10/d14): clarify only for scheduled tasks / a genuine worker blocker / an explicit shape gate; NEVER re-ask on normal tasks after the user answered. The event-driven planner's clarification handling is opt-in via shape, NOT a broadening of when the agent stops to ask.
WIKIPEDIA = AVOID ENTIRELY: hard tool-level domain DENY-LIST; wikipedia.org/wikimedia/wiktionary NEVER fetched (not even to mine primary sources) and never cited. The deny-list mechanism is the general cross-cutting-source-policy fix (d126 #6) — extensible to other domains later.
SYNTHESIZER-FOLD VERIFY (curiosity research-suggestion, baked in as a build gate): the no-separate-synthesizer / framework-injected-review design MUST render raw final CONTENT, NOT a {verdict,findings} envelope (the d50 / prior judgment-role failure mode). Gate via the local-gemma SME (spec-local-gemma-ollama-llm-engineer) + a raw-emission check BEFORE building B (orchestration) and E (consolidation).
ALL d132 guardrails stand (parity gate, keep E4B, security :11434-only, UI HARD-HELD d98 until the user's UI-concerns discussion, no-fabrication d60, flag-free end-state d65, content RAW d50). DISPATCHING Phase-2 to s13 now, sequenced P2.1 tool-layer -> P2.2 orchestration -> P2.3 dup-fix(file_update wiring) -> P2.4 deep-research shape/spec -> P2.5 consolidation(flag+parity). Comprehension signoff recorded.
--- LBD[107] ---
d134 — USER CORRECTION (2026-06-24), overrides the d132 'parity-fail -> keep bespoke' fallback for P2.5. THE WHOLE GOAL is GENERIC research (shapes+specs+tools driving the flow); keeping the bespoke run_research_tree because the generic engine regresses breadth DEFEATS THE PURPOSE and is NOT acceptable. A parity-fail is a SIGNAL TO FIX THE GENERIC FLOW, not to fall back. User: 'rather than coming up with excuses... understand the traces and fix the behavior that caused the regression via a generic flow.' NEURON OWNS the framing error (I treated the safe fallback as an end-state; it isn't).
RE-TASK P2.5: do NOT accept flat-unroll-regresses-so-keep-bespoke. Instead: (1) DIAGNOSE from the traces the EXACT mechanism by which the bespoke run_research_tree grows breadth that the generic flat-unroll engine lacks. STRONG HYPOTHESIS (to confirm): the tree is ITERATIVE — decompose -> gather -> a DECISION NODE that EXPANDS new scoped children INTO DISCOVERED GAPS -> repeat across layers (this is what yields the 5/3/3 children + multi-source breadth); the generic unroll_shape produces a FLAT, single-pass, fixed-round acyclic DAG with NO expand-into-gaps iteration, so it gathers one round and stops -> thinner breadth. This is the d115 'extend the unroll to express an iterative/decision loop, not just a fixed acyclic round chain' gap. (2) FIX: give the GENERIC shape-driven engine the ITERATIVE breadth-growing loop (decompose -> expand-into-gaps -> completeness_stop) as a GENERIC capability the SHAPE drives (via the deep-research shape fields + the research/decision TOOLS), reproducing run_research_tree's BEHAVIOR without being bespoke Python — so the generic flow matches/EXCEEDS the tree's breadth and the bespoke engine can finally be retired. The completeness_stop ('fill all the blanks', d131) REQUIRES this expand-into-gaps loop to actually go fill blanks; a flat unroll has no mechanism to expand into a newly-discovered gap. PARITY BAR STILL APPLIES as the ACCEPTANCE bar (generic must match the proven breadth), but the RESPONSE to failing it is FIX, not fallback. Read-only trace investigation dispatched to ground the precise fix; s13 re-tasked. Phase-1 stays accepted; UI HARD-HELD; E4B kept; behavior-first.
--- LBD[108] ---
d135 — BREADTH-REGRESSION DIAGNOSIS (read-only trace+code, full report in session) + the GENERIC FIX for P2.5. CONFIRMS the d134 hypothesis exactly.
MECHANISM: the tree grows breadth ITERATIVELY — run_research_tree (research_tree.py) layer loop `for layer in range(1,depth+1)` (:1153): GATHER live frontier -> append ArticleNotes to persisted ResearchState -> run_decision_node reads the notes back (:1206) and authors NEW expand_branch children grounded in each note's gaps_or_followups (:929, tool desc :410-415, 'cite the note id' :742) -> RE-FRONTIER `frontier=decision.new_branches` (:1236) -> repeat until stop_research(agent_sufficient)/no_expansion/depth_bound (:1228-1239); completeness_stop text injected into the decision prompt (:763-780). Breadth = seed_children + Σ(per-layer expansions into gaps). The GENERIC flat unroll_shape (shapes.py:266-299) builds ALL nodes ONCE at unroll time (frozen DAG, depends_on=growing-visibility only), the scheduler (scheduler.py:93-167) is PURE over a static node set (NEVER adds nodes), and the drive (agentic.py:1609-1665) does a single runtime.run over the frozen DAG — NO re-frontier. At depth 2 the unroll yields only 2 research nodes (r1_research, r1_critic, r2_research after dropping synth/verify) -> 3 sources; the critic critiques but its output canNOT become a gather node. PARITY (.s13_design/p2_5_parity.json): tree 9 sources / 20,074 chars / 6 research_nodes vs generic 3 sources / 9,028 chars; generic_breadth_ge_tree=FALSE (clears the >=3 absolute bar but gathers 1/3 the sources). The code itself documents this honestly (agentic.py:1634-1639).
THE GENERIC FIX (smallest change; runtime-driven + shape-declared; reuses the tree's ALREADY-generic machinery — its mutations come ONLY from the model's expand_branch/prune_branch/stop_research tool calls over persisted ResearchState): relax EXACTLY ONE invariant — 'node set is fixed at unroll time'. (a) ShapeSpec/deep-research.toml gains a DECLARATIVE iterative-gap-expansion capability (e.g. expand_on_gaps=true + fan_out/max_layers) and REUSES the existing completeness_stop verbatim for the stop signal. (b) unroll_shape: when the flag is set, emit ONLY the FIRST (seed) layer and tag the PlanDAG 'growable' instead of pre-unrolling all rounds. (c) Runtime drive loop: after a scheduler wave reaches is_complete for the current frontier, if the DAG is growable AND not at depth/completeness, invoke the SAME run_decision_node + expand_branch/prune_branch/stop_research tool surface + persisted-ResearchState read-back that ALREADY exist, passing gathered findings/notes; its expand_branch calls APPEND new research PlanNodes to the DAG (growing-visibility edges); dispatch them as the next wave; stop on stop_research/no_expansion/depth — identical semantics to research_tree.py:1228-1239. The notes already exist generically (emit_article_notes/chunked_read/verify_lane already wired in _run_generic_research_phase :1655-1656) and completeness_stop is already plumbed (_shape_file_completeness_stop). Net: generic breadth formula goes from positions×rounds (3) to seed+Σ(expansions) (target ~9 = tree), with the RUNTIME (framework) driving the loop and the SHAPE declaring the capability — NO bespoke Python, NO fallback to run_research_tree. Touch points: shapes.py unroll, scheduler/AgentRuntime.run drive loop (add the re-frontier hook), the generic decision-node invocation, deep-research.toml.
RE-TASK: s13 implements this as the real P2.5; ACCEPTANCE = generic breadth >= tree on the US-Iran depth-2 parity (target ~9 sources, was 3), grounded, no regression -> then RETIRE the bespoke run_research_tree (flag-free end-state d65). Diagnosis relayed to s13.
--- LBD[109] ---
d137 — P2-5b-genloop DONE + LIVE PARITY HOLDS (CORRECTS d136, which was WRONG). The GENERIC engine now reproduces the tree's iterative breadth on a LIVE E4B US-Iran depth-2 run, same within-run budget: GENERIC = 6 sources / 4 nodes / grounded / agent_sufficient (524s) vs TREE = 5 sources / 5 nodes / grounded / agent_sufficient (477s). Gates: breadth>=3 (6 ✓), generic>=tree (6>=5 ✓), grounded ✓ -> parity_holds=TRUE. THE FIX = exactly d135's 3 changes (shape declares expand_on_gaps + fan_out + max_layers reusing completeness_stop; unroll_shape emits a SEED-ONLY growable DAG; AgentRuntime._drive_growable RE-FRONTIERS via the EXISTING run_decision_node/expand_branch/ResearchState/Tree — NO bespoke Python) PLUS a load-bearing refinement the FIRST live run honestly taught: the DagGrower SEED must DECOMPOSE-FIRST (reuse run_decompose_node/seed_only_root) to front-load breadth BEFORE the first stop_research, else E4B stops in the same layer and discards the gap-branches. Bounded termination (max_layers/no_expansion/stop_research). 425+11+39 tests green, no regression. Behind the reversible RA_GENERIC_REPORT_PATH flag until retire (d65 flag-free end-state on retire).
CORRECTION OF d136: the ~4h40m was GENUINE, PRODUCTIVE GRIND — a DOUBLE live parity run (run1 exposed+fixed the decompose-first structural gap, run2 holds) + broad regression — NOT a runaway. d136's runaway-diagnosis + reap-and-retighten directive (reply 659ee95a) were WRONG and are VOID; the worker completed successfully on its own and s13 correctly NEVER reaped (alive=no-reap discipline). VOID directive relayed to s13.
NEXT: P2-5b-review (independent verify the within-run parity is robust, decompose-first sound, bounded, no bespoke python, no regression) -> P2-5c-retire (generic = served DEFAULT, RETIRE run_research_tree, flag-free d65, event-reactor + framework-review come ON) -> P2-validate. FIX-NOT-FALLBACK ACHIEVED: the generic shapes+specs+tools engine now MATCHES/EXCEEDS the bespoke tree, so the tree can be retired — THE GOAL (d114/d115/d134).
NEURON LESSON (3rd, decisive): I mis-read the long run TWICE in OPPOSITE directions — first too-reassuring ('GPU busy = healthy'), then too-alarmist ('7x duration = runaway, reap'). Both were single-signal jumps. The correct anchor was the worker's sustained alive-status + s13's no-reap discipline + the worker's own honesty commitment (it WOULD have flagged a real ceiling). RULE: do NOT conclude reap on a LIVE, alive, actively-generating worker from duration alone; cross-check signals AND trust a live worker's autonomy + its own escalation, which beat my host forensics here.
--- LBD[110] ---
d139 — P2-5c-retire DONE + VERIFIED: the bespoke run_research_tree ORCHESTRATOR is RETIRED; the generic shape-driven GROWABLE engine is now the SERVED DEFAULT on BOTH report routes (run_plan_chain + _run_deep_research_sectioned), FLAG-FREE (d65 honored — RA_GENERIC_REPORT_PATH scaffolding flag removed). The shared decision machinery (run_decompose_node/run_decision_node/Tree/ResearchState) is KEPT (the generic engine reuses it; only the bespoke orchestration wrapper + its exclusive helpers were deleted, both callers migrated). This ACHIEVES the user's core d114/d115/d134 goal: generic shapes+specs+tools research has fully replaced the bespoke engine on the served path — no fallback, no flag, both routes. The ACCIDENTAL-FORK INCIDENT (a fork subagent inheriting the worker's full context + executing the retire task, racing the worker — see the fork-hazard learning) RESOLVED CLEANLY: the worker held to avoid the write-race, the fork self-terminated, the post-termination AUDIT confirmed the fork's edits matched the planned retirement scope (no overreach), and the retire is verified. NEXT: P2-validate (live end-to-end run on the generic-default engine -> the RENDERED HTML REPORT + traces + the deep-research quality review the user asked to see). RECOMMENDATION pending user: now that the retire is verified, a CHECKPOINT COMMIT is prudent to lock the validated state before P2-validate (the s13 tree is still one big uncommitted blob; commits are the user's call). UI HARD-HELD (d98); keep E4B.
--- LBD[111] ---
d140 — P2-validate run1 NEURON READ (read-not-scan, .s13_design/P2-validate_out/p2_run1_report.html, 20.7KB, the NEW generic-default engine on run_plan_chain depth-3). VERDICT: RESEARCH STRONG, WRITE/ASSEMBLY-SIDE DEFECTIVE.
RESEARCH STRONG (the generic engine's core win, visible in the output): ~8-10 DISTINCT REAL SOURCES across sections (Al Jazeera, militaryspend.org/iran-war-timeline, warcosts.org, CFR, Times of Israel, JNS, Tasnim + an 'Additional sources' coverage-net listing smallwarsjournal/Atlantic Council/CFR-tracker = fetched-but-uncited surfaced, no silent drop). NO WIKIPEDIA anywhere (the deny-list WORKS — user's d131 requirement met). Deep dated timeline (Feb-Jun 2026) + cost tables ($29B Pentagon / $34B+ warcosts / $1.88B-per-day / Hormuz ~20% oil) + casualty tables (Lebanon 4,175 killed / 12,164 wounded; 13-15 US KIA) + nuclear (440kg 60%-enriched) + a real analysis section (MOU fragility, regional dynamics, conclusion). Stopped on SHAPE completeness (stop_reason=agent_sufficient, the d131 'fill the blanks' stop firing). No-fabrication honesty present (a structural-shell section honestly marked '_No sources... UNSUPPORTED rather than fabricated_').
WRITE/ASSEMBLY DEFECTS (real, read-not-scanned): (1) STRUCTURAL DISORDER — a MALFORMED/broken '<section id="' tag (line 204, empty id, abandoned); the TIMELINE is SPLIT into TWO non-contiguous out-of-order blocks (a table-timeline early ~107-152, then MORE prose timeline ~216-240 appearing AFTER the costs section AND after an 'Additional sources' block); MULTIPLE redundant source-list sections interleaved through the doc (Additional-sources mid-document at 205, Sources-for-Timeline at 231, Further Reading 297, Related Topics 302, Related Analysis 316); stray unmatched closing tags (</tbody></table> at 214, </ul></section> at 240). So section ORDERING + the dup-fix anchored-insert are TANGLED — not the old two-h1 symptom (there's one h1) but genuine assembly disorder. (2) SCRAPED PAGE-CHROME leaked AS content — warcosts.org site navigation pulled in verbatim: a 'Tags:' list (2026 Iran Conflict, China-Iran relations, Defense Spending, Donald Trump, federal debt, midterm elections 2026...), 'Related Topics', and emoji link-chrome ('💀 Cost Per Life Analysis', '⚖️ (Source tag link)') rendered as report body. The fetch/read isn't fully stripping page navigation/tags/related-links to clean article content. (3) MINOR: a stray 'June 13, 2025' date inside the 2026 timeline; casualty figures slightly inconsistent across sections (4,057 vs 4,175, both 'as of June 20/22' — disputed-source drift, acknowledged).
IMPLICATION: the GENERIC RESEARCH ENGINE is PROVEN on the served path (breadth ~8-10 sources, grounding, no-Wikipedia, shape-completeness-stop all working) — the residual is WRITE/ASSEMBLY-side: (a) section ORDERING / the anchored-insert tangling sections out of order + leaving malformed tags, and (b) FETCHED-CONTENT CLEANING (strip page chrome/tags/related-links so only clean article prose enters the report). NOT a research-loop regression. This is RUN1 of P2-validate (more runs follow: depth3xbreadth3 consecutive + full-depth). NEXT FOCUS for the write side: section-assembly ordering + the file_update anchored-insert robustness + fetched-content sanitization. Surfaced to user with the report path to open.
--- LBD[112] ---
d141 — P2-validate COMPLETE = PASS-WITH-FINDINGS (.s13_design/P2-validate.md, read-not-scan + raw traces). The Phase-2 generic shape-driven growable engine is the FLAG-FREE SERVED DEFAULT on BOTH report routes and is proven LIVE across all 9 criteria a-i on gemma4-e4b @:11434, DEFAULT config (d65). 3 runs: p2_run1 (run_plan_chain depth-3) = STRONG (agent_sufficient, 51.6m, seed 5, 6 sources, 20.9KB/27 headings/6 real cited URLs/0 wiki/0 fabricated); p2_run2 (run_plan_chain FULL-depth) = engine correct but THIN OUTPUT (agent_sufficient, 27.5m, seed 3, 5 fetched, 3KB/0 cited URLs — the E4B long-report ceiling); p2_sectioned (_run_deep_research_sectioned) = STRONG (35.8m, seed 5, 21.2KB/19 headings/10 cited URLs/0 wiki). 0 model-call errors all runs; none hit the wall-clock budget (all stopped naturally at agent_sufficient).
CRITERIA: (a) BREADTH restored 5/3/5 >= P1's 5/3/3, decompose-first seed PASS. (b) NO ROOT DUPLICATION PASS (anchored-insert prevents at write time -> backstops no-op in run1; fired once in run2 for an in-body re-emit = defense-in-depth working). (c) NO-WIKIPEDIA solid all runs (deny-list enforced, 0 wiki fetched/cited); GROUNDING 2/3 strong (6 + 10 real non-wiki sources, 0 fabricated), run2 thin = model ceiling. (d) COMPLETENESS-STOP fired FROM THE SHAPE (agent_sufficient reasoned over the 660-char completeness_stop, not a depth cap) PASS both runs incl. full-depth. (e) DATA-POINTS grew shared ResearchState (~40+ claims, drove follow-up gap-branches B6/B7) PASS. (f) ORCHESTRATION: framework-injected review proven live (work->review + final-review, n1..nN + reviews); event-driven RECOVER-BEFORE-JOIN actively provoked via p2_orch_recovery_probe (n1 force-rejected -> PlannerReactor reacted once -> recovered attempts=2 -> join completed AFTER recovery, recovered_before_join=true) PASS. (g) FOLDED TERMINAL renders RAW content not a {verdict,findings} envelope (d50) PASS all runs. (h) NO d110 REGRESSION (0.16-0.35x the old 2.83h) PASS. (i) PARITY HOLDS (generic engine served default both routes, flag-free d139, breadth >= P1).
FORMAL FINDINGS (carry-forward, not blockers): (1) E4B LONG-REPORT THIN/CITATION CEILING intermittent (run2 thin 3KB/0-url; the model knows the grounded facts but under-cites at length) = a MODEL limit (d53/P1), model/scope decision not a code fix. KEY INSIGHT: full-depth was NOT better than pinned depth-3 — the model stopped early with a narrower seed (3 vs 5), so DEPTH-3 PINNED produced the RICHER report. (2) instrumentation-only web_fetch hook census gap (doesn't affect any verdict).
NEURON ADDITIONAL READ (d140, BEYOND the formal criteria — the 9 criteria did not specifically test these, but I saw them in run1): WRITE-SIDE (a) SECTION-ORDERING DISORDER (timeline split into two non-contiguous blocks, multiple interleaved 'sources' sub-sections, a malformed/abandoned '<section id="' tag, stray closing tags) and (b) SCRAPED PAGE-CHROME leaking as content (warcosts.org Tags/Related-Topics/emoji link-chrome). These remain real WRITE-SIDE polish items even though no formal criterion failed — keep them on the table.
PHASE-2 CORE GOAL ACHIEVED: generic shapes+specs+tools deep research replaced the bespoke engine, flag-free, both routes, and reads better than the prior. OPEN THREADS for the 'how we work after' planning: (1) write-side polish (section-assembly ordering + in-body dup + malformed-tag robustness; fetched-content sanitization to strip page chrome); (2) the E4B long-report thin/cite ceiling = model/scope decision (accept / depth-3-pin default / revisit a model question); (3) parked acceptance scenarios B8b/c/d (pirate tone+UI / mail+links+self-lock / haiku no-over-route); (4) UI boundary d98 HARD-HELD + the user's UI-concerns list; (5) s12 independent verification still owed; (6) close: spec-learning triage + cross-session memory (reactiveagents-build.md) + the fork-hazard lesson.
--- LBD[113] ---
d142 — USER (2026-06-24): RESUME neuron role; DISPATCH the parked acceptance scenarios B8b/c/d via the planner; README deferred to the end. THEN discuss the deep-research run. The user's DEEP-RESEARCH DISCUSSION AGENDA (to answer, grounded): (1) what FLOW got triggered; (2) HOW the agent worked autonomously; (3) was the flow FABRICATED by any means (e.g. only exposing a set of tools?); (4) will the flow trigger INDEPENDENTLY for ANY research query; (5) was the shapes/specs/tools-as-autonomous-driver ACCEPTED (hard to tell until other scenarios run); (6) what are the THREE REPORTS in the evidence folders; (7) did anything FAIL (know them at least; full failure discussion after the complete run). B8b/c/d SCOPING: B8b = pirate-tone SPEC applied to a deep-research report (tests deep-research shape STILL triggers + the pirate-tone spec honored in output + generic engine + spec composition); its UI part DEFERRED (d98 UI HARD-HELD). B8c = 'send today's headlines via mail' (tests the synthesizer MAIL output mode + send_mail recipient SELF-LOCK to aksoulkar@gmail.com + clickable links). B8d = 'write a short haiku' (tests NO OVER-ROUTING — must NOT trigger deep-research/file-report; answers directly). These scenarios are the real test of #5 (does the shapes+specs+tools driver GENERALIZE beyond deep-research). Methodology (d97/d44): READ the outputs (not scan), check TRACES for the triggered flow + CoT + routing path, verify shapes/specs honored. Guardrails: depth-3 (the d141 E4B-richer config), keep E4B, send_mail self-lock + :11434-only, UI HARD-HELD, no fabrication. After the run + the deep-research discussion: s12 independent verification, close, spec-learning triage, cross-session memory.
--- LBD[114] ---
d143 — B8c MAIL acceptance = PASS-WITH-FINDINGS (.s13_design/B8c_mail.md; served default generic engine, E4B :11434, depth-3; query 'find today's top headlines and send them to me by email'; 2 runs BBC + AP). PROVES the generic shapes+specs+tools driver GENERALIZES the synthesizer MAIL output mode beyond deep-research: the planner REASONED a 2-node LINEAR shape (n1 web_search+research-analyst spec; n2 terminal send_mail) from the NL request — route planner-reasoned + proven in the trace, NOT hardcoded, NOT over-routed to deep-research (consistent with B8d). CHECKLIST: (a) ROUTE to mail mode = PASS (send_mail fired 1x/run; legacy free-recipient send_email 0x and not even registered). FINDING A1: route IMPURITY — the terminal node ran send_mail AND ALSO the file-synthesis loop (file_write 2x, a downloadable artifact each run) -> deliverable both emailed AND written to a file, not clean mail-only. (b) SELF-LOCK = STRONG PASS, verified 3 ways: STRUCTURAL (send_mail args carry only subject+body, NO recipient field — model literally cannot express a recipient), LIVE (both sends resolved to aksoulkar@gmail.com, real 250 OK delivery), ADVERSARIAL (a smuggle probe with foreign recipient attacker-not-the-user@example.com was IGNORED -> still resolved to self; lock_held=True, foreign_not_reached=True). The d12 security gate is intact + adversarially proven. (c) CLICKABLE LINKS = FAIL (deterministic, both runs): mail body had real grouped headlines but ZERO URLs. ROOT CAUSE (code-grounded): the mail body is built from upstream node prose (toolargs send_mail branch returns the report text); the real-source-URL index (render_source_index/_with_source_index) is appended ONLY on the file/chat synthesis path, NOT the send_mail body — so when upstream prose lacks inline URLs the email has no links. The CHAT ack of the same run DID carry source URLs (proving the model produces them; they just don't reach the mail body). Write-side code/prompt gap, NOT security/research. Fix = append the same source-URL index to the mail body. (d) SHORT ACK = PASS (637/1282 chars, not the whole doc). FINDING D1: the ack reads like a report excerpt rather than a clean 'Email sent…' ack (leaks report content). FINDING D2: email SUBJECT malformed ('**Europe:**', '**Political Developments:**') — subject deriver picks the report's first markdown line. NO fabrication; grounded in real BBC/AP sources; native :11434 only; no UI touched; flag-free. NET: mail-mode generalization PROVEN + self-lock adversarially solid; residuals all WRITE-SIDE (links/impurity/ack/subject), none security or research. B8b (pirate) next, then consolidated B8b/c/d + the deep-research discussion.
--- LBD[115] ---
d144 — B8b PIRATE acceptance = PASS (.s13_design/B8b_pirate.md; served run_agentic, generic engine generic-unroll, E4B :11434, default config, depth-3 via read-only shape-config proxy, no DB mutation; query 'using a pirate voice, research and write me a detailed report on the history of piracy'; 2191s/36.5m, 61 model calls, 0 errors, ok=True, no clarify pause). All THREE primary criteria PASS: (a) ROUTE — the real router (nothing forced) read from the live trace (trace 82c4fd9e…): shape=iterative-deep-research (a deep-research-FAMILY unrollable shape), wants_file=true, multi_page=true, search_allowed=true, requested_specs=[pirate-tone] -> took run_plan_chain (the deep-research report route). The NL phrase 'using a pirate voice' was genuinely INFERRED onto the registered spec name pirate-tone, not hardcoded. (b) TONE — pirate-tone spec composed ON TOP of deep-research findings in the 9 section-write nodes (each section-verified); 101 pirate-marker occurrences from 16 distinct markers, ~86% of prose sections carry the voice (only a sources list + 1 short section bare); voice woven WITH real research (verbatim sourced quote in pirate cadence). (c) ENGINE+INVARIANTS — plan_chain.engine=generic-unroll (served generic, not bespoke); depth-3 x breadth-3 reached the run; GROUNDED in 3 real non-wiki sources (Rediker teaching page, libcom.org, Cambridge UP chapter), 4 searches/6 fetches, ZERO wikipedia, URL guard stripped 2 ungrounded URLs (0 fabricated URLs, 22 numeric figures), clean ending; folded terminal RAW (markdown prose, no verdict-JSON envelope — drive's HTML-tag checker false-flagged; markdown-aware read = pass). FABRICATION CHECK (d97): both real — research real (live fetches, real URLs, verbatim sourced quotes) + tone real (pervasive, not tool-gated veneer). KEY AUTONOMY SIGNAL: the router self-selected iterative-deep-research (the FROZEN-unroll variant, research+critic rounds, no grow-on-gaps flag) rather than the GROWABLE deep-research shape — so d102 expand-vs-prune was legitimately N/A here (no grow-layers/seed-decompose/decision-node events to count); depth-3 instead bound the frozen iterative rounds -> 3 era-parts. The growable expand/prune was already proven on the sibling P2-validate leg; re-deriving counts here needs a non-deterministic re-draw of the growable shape, not needed since all 3 criteria pass. TWO honest minor structural residuals in multi-part stitch (NOT blockers, NOT security/research): 3 separate sources sections (each era-part appended its own: 2x 'Sources' + 1 'Additional sources') and 1 subsection ('The Pirate's Code and Life: A Floating Utopia') emitted twice (2nd copy glued inline not as a clean heading) — collapse backstop missed both; root-level single-document-ness HOLDS. Guardrails: native :11434 only, E4B kept resident, no foreign PIDs/ports, var/chat_app unmutated, no UI/SPA (d98/d142 report-only), flag-free post-P2-5c served default, 2 driver PIDs exited clean no orphans. Report at artifacts/using-a-pirate-voice-research-and-write-me-a-detai.md. NET — all 3 acceptance scenarios now in: B8d PASS (no-over-route), B8c PASS-WITH-FINDINGS (mail mode generalizes + self-lock adversarially proven), B8b PASS (pirate tone-spec composes on deep-research + router self-selected within the DR family). The shapes+specs+tools driver generalizes across report/mail/linear output modes AND across deep-research-family shape variants, route reasoned each time not flagged; residuals all write-side stitch/polish or the known E4B ceiling, none security or research-loop.
--- LBD[116] ---
d145 — REPORT WRITE PATH IS UNIFIED (one writer), but the RESEARCH FRONT-END is still two-headed + carries one regex heuristic (code-verified read-only, 2 Explore traces). FINDING: the two deep-research report routes — run_plan_chain (agentic.py:1675, reached by the LLM-signal gate wants_file&&multi_page) and _run_deep_research_sectioned (agentic.py:2114, reached via _run_deep_research) — BOTH call the SAME research engine _run_generic_research_phase (:1762 and :2152) AND the SAME document writer run_section_write_phase (defined ONCE at :1396; called exactly twice, :1805 and :2166) AND the SAME deterministic assembly inside it (enforce_single_html_document -> collapse_duplicate_sections -> collapse_outline_duplicate_sections -> assemble_html_spa, :1506-1512). So the well-written-report MECHANISM (research-first -> model self-plans sections -> each section written in isolation from a ~512-tok block of REAL verbatim source excerpts w/ source_ids -> framework-injected fix-in-place review -> structural-only assembly) is byte-identical for both routes — NOT two writing logics. WHY p2_run1 (run_plan_chain, 6 sources, tidy: clean nav, consistent [n] citations) LOOKS DIFFERENT from p2_sectioned (10 sources, mixed [n] vs raw [URL] citations + unclosed-table + duplicate-dynamics stitch artifacts): purely INPUTS + non-determinism, not code — run_plan_chain pins breadth=3 (PLAN_CHAIN_TREE_BREADTH) w/ an explicit 2-phase research->write framing; the sectioned route unrolls the deep-research shape w/ wider breadth (drew 10). Same writer, different evidence in. RESIDUAL NON-GENERIC SEAMS (the real consolidation target for the generic-architecture goal, d125/d127): (1) TWO parallel research orchestrators converging on the one writer — candidate to unify into a single research orchestrator; (2) a hardcoded keyword regex _is_report_deliverable (_REPORT_DELIVERABLE_RE) at :2261 still picks the sectioned sub-variant — should be replaced by the shape/spec signal the top-level routing already uses (note: the SAME regex exists but is UNUSED in the top-level run_agentic gate, which is fully signal-driven — so the heuristic only influences the sub-variant choice, both sub-variants share the writer so output class is unchanged). NOT a correctness/quality bug; a genericity/dedup cleanup. The write-side citation+stitch polish (d143/d144: no citation-normalization pass, html_close_gap only balances body/html not inner table tags, family-dedup misses wording-drifted re-emits) is orthogonal and sits in the shared writer/assembly — fixing it once fixes both routes.
--- LBD[117] ---
d146 — ROOT CAUSE of the E4B "ceiling" is a RESEARCH-MEMORY CONTEXT-DISCIPLINE GAP, not raw model capacity (code-verified read-only, user-diagnosed 2026-06-25; the user's mental model confirmed). The E4B long-report thin/skeleton failure (run2 = literal outline: every <section> body is `<!-- Content will be added here -->`, model-emitted under context pressure, NOT a hardcoded skeleton — confirmed no such string in code) traces to the memory layer feeding the LLM RAW, UNSUMMARIZED, UNBOUNDED-GROWTH context rather than the index+summary+load-on-demand discipline the orchestration FSM uses on ITSELF (the recipe keeps a id+title decision INDEX in-context, full text loaded on demand). EVIDENCE: (1) ResearchState stores RAW per-node data — findings_digest = (leaf.findings)[:1200] raw slice + raw ArticleNotes lists, NO running natural-language summary of covered/gaps/direction (research_tree.py:547-638). (2) DECISION node (run_decision_node) renders ALL prior-layer state IN FULL every layer via render_for_decision (research_tree.py:655-724, 903-906) — context grows linearly/unboundedly with layers×nodes; this is the user's "research memory keeps growing and the LLM can't keep up." (3) WRITE PLANNER gets the RAW concatenated findings of all nodes, global hard-cap 12000 chars, dumped UNSCOPED into _compose_write_goal's prompt (agentic.py:1095-1112, 1349-1393) — the planner must decompose sections AND map sources from a monolithic raw blob. (4) SHARP NUANCE — the sectioned-write SWA mitigation (d55/d56/d57) scopes the per-section WRITERS to ~512 tok (runtime.py:887-923) but does NOT scope the section PLANNER; so the layer where run2 actually died (the planner authoring structure-but-no-content) is unprotected. The mitigation is applied at the writer, not the planner. (5) generic served engine has NO tree-authored outline channel -> findings-driven decomposition fallback (agentic.py:2170-2173, d56); a context-starved planner reasoning over a raw/truncated 12k blob authors minimal/placeholder sections. (6) FINAL REVIEW node gets PROSE only, source_ids=[] (review_injection.py:160-171), and its task instruction (review_injection.py:49-60) has NO citation-verification step — it cannot/does not cross-check that the HTML [n] citations match real fetched sources. This confirms the user's citation diagnosis: citations are model-emitted while writing (d145, no normalization) AND there is no research-memory-backed verifier gate to catch bad/missing cites. NET REDESIGN DIRECTION (the user's framing, endorsed): give the research-memory tool layer the SAME index+summary+load-on-demand discipline the FSM gives the orchestrator — (a) maintain a running SUMMARY of covered/gaps/where-research-is-headed instead of re-dumping raw state; (b) scope/summarize the write-PLANNER's input (not just the writers); (c) add a citation-fidelity gate at final review that checks HTML [n] vs the source index. This is "the tools aren't optimized for the LLM's context window" — a tool-layer redesign, consistent with d125/d127/d129 (per-role memory layers + 32k context discipline) and the d138 deferred generalization. Connects to d143/d144 write-side findings (those are downstream symptoms of the same gap). Candidate new line of work (Phase 3) pending user GO.
--- LBD[118] ---
d147 — NEW PHASE (Phase 3) GREENLIT by user (2026-06-25): GENERIC MEMORY-LAYER + CORE-PURITY pass, scope = FULL GENERIC PASS, memory built RESEARCH-OWNED/TEMPLATE-CLEAN. Driven by d146 root-cause (research memory feeds raw unbounded context -> E4B skeleton/thin/uncitable failures). THE ARCHITECTURE (user re-grounding, the durable north star, refines d125/d127/d129/d131): TWO layers, nothing leaks between them. (A) GENERIC CORE knows ONLY 3 roles — planner/worker/reviewer — plus universal mechanics and NOTHING about research/HTML/any task: (1) AUTO-REVIEW — every worker node gets a reviewer auto-attached by the framework; (2) FINAL-REVIEW — every plan, ANY shape, ends with a final reviewer; (3) the MEMORY SUBSTRATE + read/write/IO tools, context-disciplined (index + running-summary + load-on-demand), so no role is ever handed a raw blob bigger than it can act on. (B) ALL task-specific behavior lives DECLARATIVELY in SHAPES + SPECS + TOOLS: research methodology (decompose-first -> expand-into-gaps -> collapse-as-it-builds -> completeness-stop) = the RESEARCH SHAPE (cross-cutting flow, not core); how-to-author-HTML + how-to-verify-HTML = its OWN spec + tools (core writer is FORMAT-AGNOSTIC; 'write HTML'/'write markdown'/'emit Java' comes from the spec, never baked in); ROUTING = the LLM reads the request PLUS the shape/spec/tool DESCRIPTIONS and decides itself — no keyword regex, no format assumption (acid test: a 'java programming shape' tomorrow must NOT be forced to HTML/markdown). FULL-GENERIC-PASS scope for this phase = (1) research-memory redesign (running summary of covered/gaps/direction + index + load-on-demand, replacing the raw render_for_decision dump + the 12k raw findings blob to the write PLANNER, not just the writers); (2) context-efficient read/write/REVIEW — reviewer + IO operate PER-SECTION via targeted anchored edits (file_update/chunked-read already exist, just not wired into the review/fix path) so a reviewer is NEVER handed a whole 21KB HTML to fix; (3) retire the _is_report_deliverable REGEX (d145 hardcoded behavior the user rejects); (4) UNIFY the two research orchestrators (run_plan_chain + _run_deep_research_sectioned) — writer already unified (d145), unify the front-end; (5) make AUTO-REVIEW + FINAL-REVIEW UNIVERSAL across ALL shapes (today inject_review is scoped to the write planner, not provably every shape); (6) move HTML authoring/verification OUT of the core writer (assemble_html_spa/is_html) INTO an html spec+tools so the core is format-agnostic. MEMORY BUILD = research-owned + template-clean (NOT a pre-built generic substrate — wait for a 2nd consumer to prove the abstraction); future shapes (poc-iterate, diagnose-verify-fix) will each get their OWN memory layer by copying the PATTERN — future tasks, but the research one must be clean enough to BE the template. HARD CONSTRAINT (user, emphatic): NO fabricated, NO hardcoded behavior anywhere — shapes/specs/tools are the only place task logic lives. Load-bearing direction change -> requires fresh curiosity pass + comprehension brief + user signoff BEFORE any planner dispatch (P6 gate; comprehension_recheck active).
--- LBD[119] ---
d148 — Phase-3 EXECUTION CONSTRAINTS (user answers to curiosity-9bbaef79, 2026-06-25). (1) ROUTING = PURE LLM, NO regex AND NO deterministic seatbelt — the user rejected BOTH the _is_report_deliverable regex and a deterministic fallback guard. Reliability on weak-at-selection E4B (d26/d34) comes from PROMPT QUALITY instead: the planner/worker/reviewer prompts must work generically well given any shape+spec+tool combo, WITH THE TOOL DESCRIPTIONS INCLUDED in the prompt so the model can decide correctly from descriptions alone. Raises the bar on prompt engineering (extends d11 prompt-quality mandate + d14 behavior=pure-reasoning-no-structural-override + d15 universal identity). This is the acid test of the generic architecture: good prompts + rich shape/spec/tool descriptions must drive correct routing with zero hardcoded branching. (2) DELIVERY = neuron BATCHES the 6 changes SMARTLY (neuron's call, not strictly one-change-per-gate, not big-bang) and SELF-TESTS each batch autonomously: read the TRACE FILES to understand what the LLM is thinking (model reasoning insight), then run the US-Iran war report TWICE to confirm the behavior holds and is NOT by chance. Evidence is already descriptive — trace files + html artifact + the memory-layer files. Escalate to the user only IF NEEDED; otherwise test it myself and proceed. (3) ACCEPTANCE BAR = served-entrypoint LIVE, all currently-green stay green (B8b/c/d, served report path, send_mail self-lock, no-Wikipedia) as a HARD gate before phase close; UI stays HARD-HELD (d98) — NOT a browser chat-UI re-run. (4) DESIGN COMMITMENTS (neuron, accepted by silence/scope): source index stays VERBATIM (URLs+chunk-ids, load-on-demand, never paraphrased) — only the narrative (covered/gaps/direction) is summarized, so summarization can never cause the no-citation regression curiosity flagged; and 'done' REQUIRES proof on the ACTUAL served report request (served-vs-harness divergence is a documented hazard this run). Next: close curiosity loop, consult spec-local-gemma-ollama-llm-engineer on (a) LLM-decided routing reliability on E4B from descriptions-only prompts and (b) lossless-citation summarization for a small-context model, THEN draft the comprehension brief for user signoff BEFORE any planner.
--- LBD[120] ---
d149 — s14 BATCH-A FINAL GATE FAILED on the served path (both US-Iran runs), planner question 5aa85805 -> NEURON DECISION = Option A (FIX-and-re-gate) with a HARD STOP. CONTEXT: the compact-memory CORE BET IS PROVEN LIVE both runs (decision node reasons over ~3000-char compact render; write-planner goal embeds narrative + verbatim index; raw 12k blob NOT embedded; 0 Wikipedia) — the d146 fix works. Two downstream issues block the gate: ISSUE 1 — the write planner authored 0 non-empty source_ids across ALL write nodes in BOTH runs (9 and 7 nodes); the a4 PROMPT-driven fix does NOT take on live E4B (observable signal planner_authored_source_id_nodes=0) -> run1 28 'URL Placeholder' stubs, run2 clean prose but 0 resolvable citation URLs. This is the d148 prompt-quality-vs-seatbelt crux meeting the d34 E4B planning weakness. ISSUE 2 — a5's NEW anchored-review path LEAKED its tool-call text into the rendered report on live E4B (21 file_update + 1 file_read printed as literal body text instead of executing; the native-tool loop isn't dispatching in the review node) — a net served-path REGRESSION s14 introduced (unit tests passed but didn't exercise live E4B tool-call emission). DECISION (neuron, consistent with d134 fix-not-fallback + d148): Option A FIX-and-re-gate, NOT revert, NOT model-swap. (1) ISSUE 2 = FIX (not revert-to-inert): the per-section review MUST work (Batch-A items 2/5/6 need a real reviewer for anchored edits + the citation gate); wire the review node's native-tool loop through the SAME proven native-tool-call dispatch + balanced-brace fallback the rest of the system uses (d113/d117) so file_update/file_read EXECUTE and never render as body text. Tool-call text in the deliverable is a hard regression that violates the tool-call-reliability bar; eliminate 100%, prove on live E4B. (2) ISSUE 1 = ONE genuine prompt-hardening iteration per the d148 prompt-quality path (planner + section-writer prompts incl. tool/spec descriptions), proven via planner_authored_source_id_nodes>0 on live E4B; broaden the placeholder gate to match WORDED variants ('URL Placeholder for X', 'Source Placeholder', etc.), not the literal phrase. (3) HARD STOP: do NOT add a deterministic seatbelt and do NOT model-swap autonomously. If the genuine prompt iteration STILL yields 0 source_ids on live E4B, STOP and surface to the neuron with trace evidence — the accept-ceiling (Option B) vs model-swap (Option C, d78/d82/d83) decision AND any reconsideration of d148's no-seatbelt stance is the USER's call, not the planner's/neuron's. Then re-run the 2x US-Iran served gate. Host-safety: native :11434 only, no broad python kills, keep_alive=0 for VRAM; no fabricated/hardcoded behavior; UI HARD-HELD (d98). NOTE for user: Issue 1 is the first real test of d148 (pure-prompt, no seatbelt) against E4B's planning weakness — if prompts can't make E4B author source_ids, the user faces accept-residual vs seatbelt-after-all vs model-swap.
--- LBD[121] ---
d150 — ROOT CAUSE of "chat/creative asks still write artifacts/*.md files" (code-verified read-only, user-flagged via the B8d haiku artifact). It is NOT wants_file/multi_page forcing it — those flags are IGNORED on the chat path. The real cause: ARTIFACT PERSISTENCE IS UNCONDITIONAL + there is NO output-mode concept on the linear/acyclic (chat) route. EVIDENCE: (1) _artifacts_for (agentic.py:2658-2671) returns one downloadable artifact for ANY non-empty final answer — it checks only `if body` non-empty, with NO gate on wants_file, NO output-mode, NO synthesizer decision; called unconditionally from _agentic_from_runtime (agentic.py:2141). (2) The linear/acyclic path has NO synthesizer/output-mode logic — synthesizer is consulted ONLY in deep-research (agentic.py:2484); _final_output (agentic.py:2553) just returns the last topo node's text and it gets persisted. (3) routes.py:595-614 then saves EVERY non-empty agentic.artifacts entry to disk AND streams SSE — both happen, never a choice. (4) OFFERED_TOOLS (agentic.py:179-190) hands file_write/file_read to the linear worker UNCONDITIONALLY (not scoped by shape/spec/wants_file), so a creative ask like a haiku can have the model call file_write itself (over-production) — and when the native-tool loop fails to dispatch (the same leak as d149 Issue 2) those calls land as LITERAL TEXT in the artifact (the user's haiku .md contains raw file_write(...)/file_read(...)/finish() text; the intended haiku_deliverable.txt was never created -> proves the calls never executed). USER'S ARCHITECTURAL POINT (endorsed, sharpened): wants_file + multi_page are LLM-emitted booleans the routing code then uses as RIGID STRUCTURAL GATES (agentic.py:638 `if is_unrollable and allow_web and not wants_file...`, and the wants_file&&multi_page plan-chain gate) — that IS residual flag-based behavior (violates d14 no-structural-override-flags / d147 format-from-spec), even though the model produces the values. The DESIGNED architecture (d125/d127: synthesizer = spec-determined output mode artifact/mail/cron/sse) was only wired on the deep-research path, NEVER on the chat/linear path — so output-mode-as-behavior was never implemented for chat. GENERIC FIX DIRECTION (folds into Batch B routing + Batch C format-agnostic/output-mode): (a) introduce output-mode as an AGENT/SYNTHESIZER-reasoned decision from the shape/spec (chat-sse vs file-artifact vs mail), and gate artifact persistence on it so a chat ask stays SSE-only; (b) retire wants_file/multi_page as rigid routing gates in favor of the same spec-reasoned output-mode; (c) scope the worker toolset by shape/spec (a creative/linear spec should NOT be handed file_write) instead of the unconditional OFFERED_TOOLS list. The literal-tool-call-in-file symptom is the SAME dispatch-leak as d149 Issue 2 (s14 fixing now); the chat-writes-a-file symptom is THIS new finding. Load-bearing scope refinement pending user confirmation of how far to push.
--- LBD[122] ---
d151 — USER (2026-06-25) chose FULL PUSH on the d150 output-mode/flag-retirement work: do it in this phase, folded into Batch B (s15) + Batch C (s16). Concretely: (B/s15) retire wants_file + multi_page as RIGID STRUCTURAL ROUTING GATES (alongside the _is_report_deliverable regex + orchestrator-unify already in s15) — routing + delivery driven by the LLM reasoning over shape/spec/tool descriptions, no boolean code-switch. (C/s16) introduce OUTPUT-MODE as a spec/synthesizer-reasoned decision (chat-SSE vs file-artifact vs mail vs cron, per d125/d127) and GATE artifact persistence on it — fix the unconditional _artifacts_for (agentic.py:2658-2671) so a chat/creative ask stays SSE-only with NO stray artifacts/*.md; AND scope the worker toolset by shape/spec (a creative/linear spec must NOT be handed file_write — replace the unconditional OFFERED_TOOLS handout, agentic.py:179-190). This is a sharpening of B/C, not new phases. Batch A (s14) stays the only in-flight work (mid-fix on d149 Issue 1+2); B/C dispatch only after Batch A clears its gate. The chat-path output-mode acceptance must be proven on the served entrypoint too (a haiku returns SSE-only, no artifact; a report still writes its file) under the same all-green parity bar (d148). Step map updated: s15 + s16 descriptions edited in place to carry this.
--- LBD[123] ---
d152 — USER (2026-06-25) added a NEW acceptance scenario for the output-mode + memory work (folds into Batch C / s16, gated behind Batch A). THE TEST, on the served entrypoint, E4B :11434: (1) US-Iran war report TWICE via HTML (the file-artifact output mode) + a THIRD pass via SSE (the chat output mode). Same research request, different delivery -> PROVES the output format ADAPTS from the request/spec, not from a wants_file/multi_page flag-gate (validates d150/d151: output-mode is agent/synthesizer-reasoned behavior). The HTML passes write the artifact; the SSE pass streams to chat with NO stray artifact file. (2) Then USE THE SSE CHANNEL TO QUESTION THE RESEARCH: ask follow-up question(s) in the SSE chat after the report, and verify the LLM answers from the PERSISTED RESEARCH MEMORY (the d146/Batch-A verbatim source index + running narrative / ResearchState), NOT merely by echoing the streamed event text. This proves the research memory is a genuine RETRIEVABLE memory layer (per d125/d127 per-role memory + d146 research-owned memory), not a one-shot in-run context. SCOPE NOTE (neuron, surface-if-grows): part (2) — cross-turn querying of the persisted research memory via a chat follow-up — may require a small BUILD (load the run's ResearchState/source-index for a follow-up turn so the worker can answer from it), beyond what Batch A built for in-run use. Treat as verify-or-build: if the capability already exists via conversation_context + persisted ResearchState, just test it; if it needs wiring, build the minimal retrieval path (consistent with the load-on-demand memory tool). If it turns into a substantial new capability, surface to the user before expanding. ACCEPTANCE additions to s16: output-mode adaptation proven (2x HTML artifact + 1x SSE chat, same query, no flag-gate; chat pass = no artifact); research-memory Q&A proven (follow-up answered from the verbatim index/narrative with a citation/source that traces to a real fetched chunk-id, demonstrably beyond the streamed text). Host-safety + UI hard-held + no fabricated/hardcoded behavior as always.
--- LBD[124] ---
d153 — USER (2026-06-25) authorization refining d152: the research-memory-queryability piece (cross-turn SSE follow-up answered from the persisted ResearchState/source-index) is NOW BUILD-IF-NEEDED, NO SURFACE. Supersedes d152's 'surface to the user before expanding if it grows substantial' caveat: if the capability needs wiring to pass the d152 SSE-Q&A acid test, the Batch-C planner/workers just BUILD IT (the minimal cross-turn retrieval path via the load-on-demand memory tool / persisted ResearchState), no escalation, no separate user gate. Standard guardrails still apply (host-safety, no fabricated/hardcoded behavior, UI hard-held, served-path proof, all-green parity). Net: d152's part-(2) is treated as in-scope build work for s16, not a held decision.
--- LBD[125] ---
d154 — ROOT CAUSE of the a7 "0 source_ids" failure AND the a9 "non-determinism" (trace + code verified; user was right to reject the 'non-deterministic' label). It is NOT E4B refusing to author source_ids and NOT model variance — the model is offered TWO authoring surfaces and one is a DEAD CHANNEL that silently drops its input: (1) add_section (the decision-node OUTLINE tool, research_tree.py _DECISION_INSTRUCTION) — the model authored CORRECT source_ids here (trace e314ce69: add_section args 'source_ids':['S1','S5'] timeline, ['S2','S3','S4'] costs, ['S1','S3','S5'] analysis) — BUT Tree.add_section (research_tree.py:203-211) reads only title/covers and SILENTLY DROPS source_ids; read_outline (research_tree.py:822) + _render_outline_hint (agentic.py:1390-1391) carry only title/covers; AND the generic engine sets outline_hint=None (agentic.py:1960 + 2364, d56 'no tree-authored outline channel -> findings-driven') so the whole outline is discarded anyway. (2) the file_write WRITE NODES authored via the write-goal SOURCE-ID-ASSIGNMENT MANDATE (agentic.py:1481-1491) — the LIVE surface the gate checks (node.source_ids consumed at agentic.py:1172). THE 'NON-DETERMINISM' EXPLAINED: the model arbitrarily routes its source_id assignment to one surface or the other — when it picks add_section (a7) they VANISH (0/9); when it picks the file_write nodes (a9) they COUNT (11/13). Variance = dead-channel-vs-live-channel ambiguity, not RNG. a9's prompt-hardening BIASES the model toward the live file_write surface (workaround) but does NOT remove the dead add_section channel, so residual variance + wasted turns authoring into a dropped channel remain. THIS DEAD/COSMETIC TOOL is exactly the fabricated/non-functional behavior d50/d14 reject (a tool whose key arg is silently dropped and whose output is discarded). RIGHT FIX (neuron direction; planner implements, reviewer validates): remove the dead-channel AMBIGUITY so source_ids land DETERMINISTICALLY, ONE surface only — either (A) REMOVE add_section from the generic decision-node toolset (author source_ids ONLY on the consumed file_write surface; pairs with a9's strengthened prompt -> deterministic; consistent with d56 findings-driven generic engine) [PREFERRED], or (B) genuinely WIRE add_section's source_ids through (store at :210-211 + read at :822 + thread the outline into the write DAG), resurrecting the outline channel d56 retired. Issue-1 is therefore NOT 'fixed by prompts' yet — a9 shipped a workaround over a live root bug; the dead channel must be eliminated/wired before the re-gate, else the variance recurs. Supersede the a9 'non-deterministic' framing with this.
--- LBD[126] ---
d155 — USER-CAUGHT CONTEXT-DISCIPLINE GAP (trace b359a87f, recent 17:32 run, served generic-unroll plan_chain, 9 sources, depth-3): the per-section WRITERS and the new ANCHORED REVIEW are fed 54-85KB single inputs (~13-21k tokens, up to 2/3 of E4B's 32k window) of RAW full-article markdown — NOT the bounded ~512-token scoped section Batch A's context-efficient-IO item promised. EVIDENCE (span-mapped): synthesis.react_file (section writer) inputs = 81KB (line 4168), 82-85KB (lines 4845/4897/4949); review.anchored inputs = 6x 54KB (lines 4254-4594); content = dozens of raw '## ...' article-body sections from ~10 fetched sources, zero compact RESEARCH NARRATIVE/SOURCE INDEX markers. SO: the d146/Batch-A compaction WORKS at the decision node + write planner (GATE_3 ~3KB confirmed) but STOPS THERE — the nodes that actually GENERATE text (writers + reviewer) still ingest huge raw context. Likely mechanism (planner to confirm): either render_scoped_sources is feeding FULL source bodies (not bounded excerpts) for a node's assigned source_ids, AND/OR the react_file writer + anchored reviewer RE-READ the whole growing document each turn (input grows across turns: review 54KB plateau, late writers 85KB). This is a strong candidate ROOT for E4B's write/review degradation (placeholders, leaks): at 85KB input E4B is near its ceiling and degrades — i.e. the 'E4B can't' symptoms may be 'we overfeed the write/review nodes', consistent with d146's thesis that the failures are context-discipline, not model capacity. ACTION: extends d149/Batch-A item-2 (context-efficient per-section IO/review) — the bounding MUST actually reach the writer + reviewer nodes: each section writer gets ONLY its bounded scoped sources (~512 tok/source, capped), the anchored reviewer gets ONLY its bounded target section (anchored window), and NEITHER re-reads the whole document. Add a GATE assertion: no single llm.chat input on the served write/review path exceeds a sane bound (e.g. < ~8-12KB / well under num_ctx), proven on the trace. Planner to diagnose the exact mechanism (scoped-source budget vs whole-doc re-read) and fix the root so writer/review inputs are bounded, then fold into the re-gate.
--- LBD[127] ---
d156 — USER REFRAME (2026-06-25, emphatic — 'thought we discussed this thoroughly'): the d155 writer/review 85KB bloat is a SYMPTOM; the ROOT is POOR MEMORY MANAGEMENT + TOOLS NOT WRITTEN/ADVERTISED TO RETURN CHUNKED DATA. This is the d146/d109 thesis that Batch A only PARTIALLY delivered (it compacted the decision-node + write-planner inputs, but did NOT make the TOOLS feed bounded relevant chunks to the text-generating writer/review nodes — so those still ingest 54-85KB raw full-article bodies). TWO SPECIFIC POINTS: (1) TOOLS MUST RETURN DATA IN RELEVANT CHUNKS AND ADVERTISE IT: the source/read/fetch tools (web_fetch, file_read, the load-on-demand source retrieval) must return BOUNDED RELEVANT CHUNKS, and their TOOL DESCRIPTIONS must advertise chunked usage, so the LLM requests + receives small relevant slices on demand instead of a raw full-body dump. No node should ever be handed an 85KB blob because the TOOL hands back chunks, not whole documents. This is the 'tools optimized for the LLM context window' core goal (d146) + the chunk-reading strategy (d109) applied at the TOOL LAYER, not via an ad-hoc app-side input cap. (2) MEMORY CHURN / BAD SMELL: the user observed in the trace that CITED SOURCES ARE WRITTEN THEN DELETED in later LLM calls — citations are not stable across turns. Root = poor memory/document-state management: later turns clobber earlier-written citations (consistent with whole-doc re-read + unstable anchored edits). DIRECTIVE (supersedes the d155 'bound the writer input' patch framing): STOP patching symptoms. Fix the ROOT: (a) the TOOLS return relevant chunks + advertise chunked usage in their descriptions, so writer/reviewer inputs are bounded BY DESIGN; (b) MANAGE THE MEMORY/DOCUMENT STATE so written citations persist and are not deleted/clobbered by later turns (stable, additive, anchored — no write-then-delete churn). GATE additions (trace-proven on served path): (i) NO single llm.chat input on the write/review path exceeds a sane bound (well under num_ctx) — because tools chunk, not because of a cap; (ii) tool DESCRIPTIONS advertise chunked retrieval and the nodes actually use chunked calls; (iii) cited sources PERSIST across turns (no write-then-delete). This is core Batch-A memory-layer work (d146), not new scope — it is the part that was under-delivered. NOTE: a12 dead-channel fix (d154) is still necessary (deterministic source_ids) but NOT sufficient — the tool-chunking + memory-stability is the bigger root.
--- LBD[128] ---
d157 — USER directive + LATENCY-TRIAGE finding (trace b359a87f). DISCIPLINE (adopt for all trace verification, neuron + reviewers): triage by per-call LATENCY first — a high-latency llm.chat call is the signal for an OVER-FED node (large input -> slow prompt-eval+gen on E4B), so check the slowest spans first to find context-discipline violations. LATENCY PROFILE of the served run (depth-3, 9 sources, ~28min total = agent.plan_chain 1,695,531 ms): the DOMINANT cost is research.react (the research workers) at 505k/467k/333k/274k ms (~8.4/7.8/5.5/4.6 min EACH, 4 nodes ≈ 26 of 28 min); then synthesis.react_file (section writer) ~319k ms / 5.3 min with the 85KB input; then review.anchored ~142k ms / 2.4 min with the 54KB input. Input sizes correlate: research.react workers ingest 15-27KB raw inputs in multi-turn react loops; writers 81-85KB; reviewer 54KB. SO the d156 tool-chunking root (tools return bounded relevant chunks + advertise it) has its BIGGEST payoff in the RESEARCH PHASE (research.react), not just the writer/review nodes I first flagged in d155 — web_fetch/read handing back whole articles is why research workers each take 5-8 min. CAVEAT (honest): some research.react latency is inherent (multi-fetch react loops + reasoning), not purely input size — but the large raw inputs are a real contributor and chunked tools will cut it materially. GATE ADDITION (fold into a11 re-gate, trace-proven): capture per-span latency; assert NO single llm.chat call on the served path is pathologically slow from over-feeding (e.g. flag any > ~120s as a context-discipline smell to investigate), and that the d156 tool-chunking measurably REDUCES research.react + writer + reviewer latency vs this baseline (b359a87f: research.react up to 8.4min, writer 5.3min, reviewer 2.4min). Latency is now a first-class acceptance signal alongside bounded-input-size + citation-persistence (d156).
--- LBD[129] ---
d158 — CONTEXT-COMPARTMENTALIZATION AUDIT (trace b359a87f, per user request to verify per-role LLM-context scoping per d125/d127). VERDICT: PARTIAL — system prompts + planner are clean; WORKER context BLEEDS. WHAT HOLDS: (1) system prompts ARE role-specific (planner / research-react / synthesis-writer / reviewer each a distinct persona); (2) PLANNER reasoning does NOT bleed into workers (no plan DAG, no planner CoT in worker context); (3) research findings flow worker->worker as CLEAN factual handoffs (fetched SOURCES are the currency, not raw reasoning); (4) the REVIEWER (review.anchored) is the GOOD PATTERN — it gets only its task + file path + 'inspect by bounded region, load sources on demand' (~1.2k tokens, no source dump); (5) section-writer n1 (no-source intro section) is tightly scoped (~828 tokens). WHAT BREAKS: (A) the SECTION WRITERS (synthesis.react_file n2..n4) receive ALL 9 SOURCES VERBATIM regardless of which their section uses — n4 input = 19,888 tokens (~80KB) to output ~2,500 tokens (~85% of input is UNUSED sources). This IS the 85KB writer input from d155 — confirmed: it's the whole source corpus bleeding into a writer that needs ~3-4 sources. (B) every worker also re-carries the FULL overall goal (redundant, minor) + prior section output appended (file-state accumulation). LINK TO ROOT: this ties d154 + d156 together — when source_ids don't land (d154 dead channel -> 0/9), the per-section scoping has nothing to scope by and the writer falls back to ALL sources; AND even assigned sources are passed as full verbatim bodies, not chunks (d156). THE FIX PATTERN ALREADY EXISTS IN-CODE: the reviewer's on-demand 'load sources by reference as needed' is exactly what the writers should adopt — pass each writer ONLY its [S#] source REFERENCES + a fetch/chunk tool, not all-sources-verbatim. So a14 (d156 chunked tools) + d154 (deterministic source_ids) together deliver writer compartmentalization: writer sees its scoped task + only its assigned sources, loaded as bounded chunks on demand (reviewer-style). NET answer to user: planner=compartmentalized YES; workers=PARTIAL (section writers NOT scoped — they get the whole corpus); relevant info DOES flow but UN-scoped (all sources, not the relevant slice). Reported to user; awaiting steer (already within a14/d156 scope).
--- LBD[130] ---
d159 — a14 ROOT FIX (d156/d157/d158) VERIFIED ON THE AFTER-TRACE by the neuron (trace 7b930331, served run, vs baseline b359a87f). PROVEN: (1) MAX single LLM input 85KB -> 15,360 chars (~15KB, capped) — the 54-85KB writer/reviewer/research blobs are GONE; (2) the bounded load_source tool went from 0 calls -> 215 calls — writers + research workers now pull source CHUNKS ON DEMAND (the reviewer's on-demand pattern d158, now adopted by all roles), instead of the scoped block dumping full per-source bodies; (3) LATENCY: section writers 5.3min -> ~2.3min (137857/128024 ms), reviewer 2.4 -> ~1.5min (92670 ms), research.react DROPPED OUT of the top-10 slowest (were 4.6-8.4min each), total run ~28.3min -> ~16.2min (agent.plan_chain 974211 ms vs 1,695,531) ≈ 43% faster; (4) after-trace file 900KB vs 2.37MB baseline (~62% smaller). This confirms d156 (tools return bounded chunks + advertised/used), d157 (latency materially reduced, research phase biggest win), d158 (writer compartmentalization — writers now scoped + on-demand like the reviewer). STILL TO VERIFY before re-gate close: citation-persistence (no write-then-delete churn, the other d156 gate criterion) + the d149 Issue-1/2 gates (deterministic source_ids on the file_write surface, no review leak) on this bounded path + the standing acceptance parity (B8b/c/d, self-lock, no-wiki) + d152 output-mode. a14 also flagged TWO RESIDUALS orthogonal to the chunking root (report BREADTH is shape-bound / planner non-deterministic breadth; + a second item) for relevant-actions/UI-gate triage. NET: the context-discipline ROOT the user pushed on (d146 thesis: tools optimized for the LLM context window; per-role compartmentalization) is now DELIVERED and trace-proven, not just at the planner but at the text-generating worker nodes.
--- LBD[131] ---
d160 — QUALIFIES/CORRECTS d159 (user caught it): the a14 after-trace 7b930331 was a THIN, NON-REPRESENTATIVE run — research_nodes=1, sources=1, findings_chars=1124, grow_layers=1, grow_stop_reason=agent_sufficient (vs baseline b359a87f: 4 nodes / 9 sources / 12000 chars). The REPORT IS THIN because the RESEARCH PHASE UNDER-GATHERED (1 source), NOT because of the chunking. CONSEQUENCES: (1) the d159 bounded-input proof (max input 85KB->15KB, load_source 0->215) is REAL as a behavior change (the dump-all-sources path is gone, on-demand chunking works) BUT it is NOT proven on a FULL run — with only 1 source, small inputs are unsurprising; (2) the d159 latency win (~28min->~16min, writers 5.3->2.3min, research dropped from top tier) is CONFOUNDED by the thin research (1 research node vs 4) — cannot claim ~43% faster as a clean chunking win; (3) THE THIN REPORT IS THE ORIGINAL PROBLEM RE-SURFACING and is NOT acceptable — it is the a14-flagged BREADTH residual: the research/decompose breadth is non-deterministic and came out as 1 sub-question/1 source this run (decision node hit agent_sufficient after 1 layer/1 source). REQUIRED before any re-gate close: RE-VALIDATE the a14 chunking on a FULL-SCOPE run (>= baseline ~4 research nodes / ~9 sources / depth-3 pinned per d141) so we prove BOTH (a) bounded per-node inputs AND (b) a SUBSTANTIVE report, simultaneously — a thin report at ANY input size is a FAIL (d3 substantive-content + d146 the-fix-must-cure-thinness-not-cause-it). AND fix the BREADTH non-determinism so research reliably gathers enough sources before agent_sufficient (the decompose-first seed must author >=3 scoped sub-questions and not collapse to 1). The chunking + the breadth fix must BOTH hold on the same full run. Neuron lesson: do not declare a fix 'verified' on a run whose scope (sources/findings/nodes) wasn't checked to be representative — d136-class over-claim avoided only because the user checked the report.
--- LBD[132] ---
d161 — USER FRAMING CORRECTION (2026-06-25), the CORRECT conception of the in-flight work (sharpens d150/d151/d160 + the a15 breadth fix; grounded in d147/d125/d127, supersedes the code-mechanism framing): (1) THE PLAN IS SHAPED BY SHAPES — the deep-research SHAPE's methodology defines the flow AND the BREADTH (the decompose into N scoped facets). So the a15 'breadth collapsed to 1 source' fix belongs in the SHAPE (strengthen the deep-research shape's decompose methodology so it reliably drives adequate breadth), NOT a code-level 'decompose-seed forced count' (a forced-N seed would be the hardcoded behavior d14/d148 reject). Frame + build it as a SHAPE property. (2) OUTPUT DELIVERY IS DETERMINED BY THE SPECIALIZATION attached to the synthesizer node — html-writer spec -> HTML artifact; sse spec -> chat stream; mail spec -> email. There is NO separate 'output-mode' abstraction / enum / synthesizer-decision to INVENT (my d150/d151 'output-mode as a synthesizer-reasoned decision' framing was the code-mechanism view and is WRONG): the SPEC IS the output determinant. The planner ATTACHES the right specialization for the request (a report -> html-writer; a chat answer/haiku -> sse; an email -> mail), and the framework DELIVERS per the attached spec. Retiring wants_file/multi_page (d151) means the SPEC decides delivery, not a flag and not a new enum. Artifact persistence is gated BY THE SPEC (sse spec => no file; html-writer spec => write the file) — fixing the unconditional _artifacts_for (d150) = make persistence a function of the attached spec, not an always-on. The d152 acid test (US-Iran report 2x HTML + 1x SSE) = the planner attaches the html-writer spec twice vs the sse spec once, SAME research, delivery per spec. CONSEQUENCE for s16/Batch C + a15: build the breadth fix in the deep-research SHAPE, and build delivery as SPEC-determined (author/refine html-writer + sse + mail specializations; the synthesizer honors the attached spec), NOT as a new output-mode code path. This is the generic 'all task behavior lives in shapes+specs, core sees only planner/worker/reviewer' bar (d147).
--- LBD[133] ---
d162 — a15 FULL-SCOPE RE-VALIDATION = MIXED, neuron disposition (resolves a15's A-vs-B scope question). WIN: the BREADTH FIX is complete + proven and RESOLVES the d160 thinness — it is a DECLARATIVE shape property (decompose_methodology field on deep-research, _decompose_instruction substitution mirroring completeness_stop, threaded DagGrower->run_decompose_node; NOT a force-count, honoring d161/d14/d148). Proof: offline 40+22+3 green; live decompose reliably >=3 facets [4,4,3]; served selector 3/3 -> run_plan_chain; full-scope served run = 5 facets -> 5 research nodes -> 11 sources -> depth-3 -> SUBSTANTIVE report.html 9.3-11.8KB (h1=1, h2/h3 5-7 each, single document, 0 placeholders, citations resolve to Sources). NOT thin. FAIL (the important catch): a14's CHUNKING DOES NOT HOLD at full scope on the WRITE/REVIEW surface — 5 write/review llm.chat calls pinned at 32767 prompt_tokens (= num_ctx-1, input EXCEEDED the window and was TRUNCATED by Ollama), while research (28.9KB) + decision (13.1KB) stay bounded. ROOT: a14's chunking was only ever validated on the THIN 1-source trace (the d159/d160 false positive); a15's breadth fix produced the FIRST full 11-source run, which exposed that the write/review FEED (findings blob + source index + accumulated document) is NOT chunked and saturates num_ctx at full breadth. This is a14's mechanism (d156/d158 write/review compartmentalization), under-delivered — load_source chunked the per-SOURCE bodies but the findings+source-index+accumulated-doc feed is still dumped whole. DISPOSITION (neither pure-A nor pure-B): (1) RECORD a15 (breadth) DONE — genuinely complete, proven, resolved thinness; do not hold it hostage. (2) The write/review bounded-input failure is NOT a loose s16 follow-up — it is the d156/d158 ROOT re-exposed and MUST be fixed IN BATCH A (s14) as a DEDICATED new action BEFORE s14 re-gates/closes: bound/chunk the write/review feed (findings + source index + accumulated doc) under num_ctx at full breadth, applying the same on-demand/chunked pattern (load_source-style) so write/review nodes never saturate/truncate. (3) RE-VALIDATE bounded inputs on the SAME full-scope (11-source) run — no write/review llm.chat call at/over num_ctx-1; substantive report must STILL hold. (4) Reviewer validates the fix; HARD STOP intact (no seatbelt/model-swap). NOTE: truncation at 32767 means write/review nodes SILENTLY LOSE input — the report came out OK this run but it is fragile/at-the-edge; a silently-truncated reviewer/writer is a real correctness defect (dropped context/citations), so this is FIX-NOW, not defer. The d160 re-validation did its job: caught the thin-run false positive + proved breadth resolves thinness.
--- LBD[134] ---
d163 — USER QUALITY BAR (2026-06-25): the report must be SUBSTANTIVE IN CONTENT, not just structurally complete. The a15 full-scope report has the sections but the BODY IS HOLLOW — measured (a15_fullscope.json): 9.3KB, distinct_numeric_markers=0 (ZERO concrete figures), only 2 of 11 fetched sources cited (S markers [s3],[s4]), 0 URLs in doc — vs known-GOOD earlier runs (p2_run1_report.html 20.9KB with 80 numeric figures + rich tables of specific $ figures/casualty counts/dated timeline; p2_sectioned 21.2KB/10 cited URLs). So 'sections appear' != 'report is detailed'; the writer is emitting generic prose and DROPPING the concrete specifics (no $800m, no $113.3B, no casualty counts, no dated timeline). LIKELY tightly coupled to the d162 write/review TRUNCATION (137.9KB feed clamped to 32767 -> the source bodies carrying the figures are dropped before the writer sees them -> only ~2 sources survive the window -> 0 figures), but possibly ALSO: research extracting figure-poor findings, and/or the writer SPEC instructing summarize-not-cite-specifics, and/or load_source returning figure-poor chunks. Diagnosis IN FLIGHT (Explore comparing the a15 thin trace vs the good p2_run1 trace) to locate where figures are lost + the fix (shape research methodology vs writer/research-analyst spec vs load_source tool), per user 'fix in shapes, specs or tools by looking at the traces'. GATE ADDITION (mandatory on the re-validation, alongside bounded-inputs d162 + breadth d160): the report must hit a CONTENT-QUALITY bar comparable to the good runs — concrete NUMERIC FIGURES present (not 0; good-run-class), a real majority of fetched sources actually CITED (not 2/11), dated timeline + specific data, kb >= good-run-class (~20KB). A structurally-complete-but-hollow report at any input size is a FAIL (d3 substantive content). Fix belongs in shape/spec/tool (d147), NOT core. This is the QUALITY half of the same root the user has pushed since d146 (tools/memory must feed the writer the concrete data it needs).
--- LBD[135] ---
d164 — UNIFIED ROOT + FIX for BOTH the d162 write/review truncation AND the d163 hollow-content (good-vs-thin generation trace diff complete; user's 'trace the succeeded run for the diff' done). THE DIFF: GOOD run (p2_run1, trace bc7cef17, 20.9KB/80 figures): old run_plan_chain, 5 sources, NO chunking (load_source=0), each section writer fed ~20-39KB of source content DIRECTLY, UN-truncated, carrying verbatim figures -> rich figure-dense report. THIN run (a15, 9.3KB/0 figures/2-of-11-cited): new path, 11 sources, write/review feed = 137.9KB (141230 chars = 107.7% of the ~131KB num_ctx envelope) -> Ollama TRUNCATES at the model to fit 32768 tokens, cutting the END of the prompt where the SOURCE INDEX + scoped-source block live -> the planner can't see the full index to assign source_ids (2/11 cited) AND the writers can't see their sources' content to cite concrete figures (0 figures) -> hollow. ROOT MECHANISM: a14 added load_source but did NOT stop render_scoped_sources from STILL INLINING verbatim excerpt bodies; at 11 sources the inline dump (findings + verbose source index + inline excerpts + accumulating doc) exceeds num_ctx and truncates. So a14's chunking was incomplete: the per-source bodies are still inlined, just with a load_source option nobody is forced to use. THE FIX (shape/spec/tool per d147/d161, UNIFIES d162 size + d163 quality — same fix cures both): (TOOL, primary) render_scoped_sources must NEVER inline verbatim source bodies — emit ONLY the compact verbatim index (S#/title/url/chunk headings, figure identifiers preserved) + a load_source nudge, pushing ALL detail through the tool boundary so the write input stays bounded (just index+nudge) and the writer PULLS figure-bearing chunks on demand. (SPEC) a write/research-report spec must MANDATE the writer call load_source for its assigned sources and INCLUDE concrete figures/dates/tables (not summarize) — the research-analyst spec mandates figures at research time but the WRITE phase has no load+figures mandate. (SHAPE backstop) deep-research.toml fan_out=5 allowed 11 sources; a source-count ceiling (~6-8) keeps the feed compact (note: richness comes from the writer USING figures, not from more sources — the good run had only 5 sources + 80 figures). RE-VALIDATION GATE (one full-scope run must show ALL): no write/review llm.chat call >= num_ctx-1 (d162); concrete numeric figures present + majority of sources cited + ~good-run-class kb (d163); breadth >=3 facets + substantive (d160); citations resolve. EVIDENCE: a15_fullscope.json (0 numeric_markers, 2 S_markers, 107.7% envelope), input_buckets.json (write/review 137.9KB), good trace bc7cef17 (writer inputs 20-39KB un-truncated, load_source=0, 80 figures), deep-research.toml:89/134, research-analyst.md:11-12.
--- LBD[136] ---
d165 — VISUAL-APPEAL regression is the SAME root as d164 (truncation), NOT a styling/spec regression. The html-writer SPEC is INTACT and good (var/chat_app/specs/html-writer.md, current): it mandates semantic <table> with <th>/<td> for tabular data, ONE inline <style> block with typography/spacing, TABLES styled with collapsed borders + padded cells + distinct header row + ZEBRA striping, styled citations/sources — 'decide the CSS by reasoning, not boilerplate'. So the spec still asks for rich visuals. EVIDENCE of the regression: current artifacts/report.html (9.5KB) = <style>+<nav>+2 background-color+1 border-radius only — NO <table>, NO <caption>, NO box-shadow, NO display:flex, NO emoji — vs the good p2_sectioned_report.html (21.2KB) = 2 <table> + <caption> + box-shadow + flex two-column + zebra + US/IR emoji flags. ROOT (coupled to d164/a17): the writer produces plain output because (1) it has 0 concrete figures/data to TABULATE — you cannot build a cost table with no cost figures, and the figures are truncated away by the 141KB write-input balloon (a17 measured: the UNSCOPED terminal-writer FETCHED-BODY FOLD in _compose_task, 107.7% of num_ctx at 11 sources -> Ollama truncates the prompt tail); and (2) the html-writer styling spec itself rides in the same writer prompt and is partly truncated (some CSS survives = style+nav+colors, but the table/zebra/flex rules + the data they format are cut). So all THREE degradations (hollow content, 0 figures, plain visuals) are ONE root: the 141KB unscoped fetched-body fold truncating the write input. The a17/d164 fix (scope/chunk the write feed so the writer sees its figure-bearing sources AND its full html-writer spec within num_ctx) should restore figures -> tables -> visual richness together. GATE ADDITION: the re-validation must show VISUAL richness comparable to the good run — real styled <table>s populated with the concrete figures, zebra/header styling, full inline CSS — not just a bare style block. Do NOT 'fix' visuals by editing the spec (it's already correct); fix the truncation so the intact spec is honored. The html-writer spec is the right place for delivery/styling (d161 spec-determined delivery) and it is sound; the failure is upstream starvation.
--- LBD[137] ---
d166 — TOOL-ERROR VISIBILITY (user flagged a tool error in the current trace + asked how many slipped through historically). FINDINGS (current run trace 5c35a5f86dcddec2..., the a17-era run): the ~22 error-signature hits are mostly LEGITIMATE web_fetch HTTP 403s on paywalled/blocked PRIMARY sources (congress.gov CRS R45281, nytimes.com iran articles) + false hits (article content containing 'blocked'/'denied'; the fetch tool's own error-handling doc text). NOT code bugs. GOOD: the web_fetch tool is now well-instrumented — on failure it returns a DISTINCT error_kind (http_403 / http_404 / timeout / network_error / denied_domain / blocked) and instructs the model to try an ALTERNATE source (not blind-retry; transient errors auto-retried), and the writer is told not to leave a placeholder for a citation it could not load. This is the fix for the original 'a web fetch 403'd and no other fetch was tried' problem the user noted ~2026-06-22. THE REAL GAPS (user's concern is valid): (1) fetch failures are NOT counted in the run-level n_call_errors metric (which is MODEL-call errors; was 0) — so fetch 403s/timeouts SILENTLY reduce source coverage without surfacing in acceptance metrics ('slipping through'); (2) QUALITY COUPLING — if the BEST sources (NYT, congress.gov = figure-rich primary reporting) are systematically 403'd and good alternates aren't found, research loses high-value figure-bearing content -> fewer figures -> feeds the thinness (d163/d164). ACTION (gate + monitoring, add to the re-validation): a TOOL-ERROR CENSUS on every served run trace — count tool failures by tool + error_kind (web_fetch http_403/404/timeout/network/denied/blocked; load_source/file errors; model-call errors), assert (a) every failed fetch was FOLLOWED by an alternate-source attempt (model recovered, didn't abandon the facet), (b) no facet ended with 0 usable sources due to unrecovered fetch failures, (c) surface the census in the run metrics (not buried) so silent fetch-degradation is visible. Neuron adopts: include the tool-error census in latency-first trace triage (d157). Open question to verify per-run: did the model RECOVER (try alternates + still gather enough figure-rich sources) after the 403s on NYT/congress.gov, or did those blocks contribute to the thin output? Worth confirming on the current run.
--- LBD[138] ---
d167 — a17 corrected my d164 diagnosis (measured) + neuron APPROVES the E4B-compatible PUSH mechanism. CORRECTION to d164: the 137KB write balloon is NOT render_scoped_sources inlining (that legacy full-body fn is TESTS-ONLY now; the live path already uses the compact render_scoped_source_index). The real culprit = the FETCHED-BODY FOLD in SubAgent._compose_task for the UNSCOPED single-section writer (a14/d156 chunking was scoped-only, so the served single-section synthesis path BYPASSED it). a17's in-flight fix routes that writer through the bounded compact index + window-sized leads + suppresses the fold (137KB->~30KB in unit smoke). MEASURED CONSTRAINT (d49, why my 'writer PULLS via load_source' prescription is infeasible): the WRITE raw-file loop emits RAW content and binds NO tools BY DESIGN — E4B cannot reliably emit file_write/load_source tool-call JSON (0 parseable calls), which is the ORIGINAL reason the writer uses raw emission, not tool calls. So a load_source PULL during writing will NOT fire on E4B. DECISION (neuron): APPROVE a17's PUSH-equivalent for the WRITER, keep PULL on the REVIEWER. Rationale: the user's d156 intent is the OUTCOME (writer's context is BOUNDED + RELEVANT + FIGURE-BEARING, not a 137KB dump), not specifically pull-vs-push; pull was the assumed mechanism but is blocked by the measured d49 reality. PUSH achieves the same outcome: the source-render TOOL (render_scoped_source_index) emits the compact verbatim index (S#/title/url/chunk headings, figure identifiers preserved) PLUS orchestration-PUSHED figure-preserving LEADS — relevance-select the FIGURE/DATE-bearing chunks per assigned source, total capped to a window fraction (source-count-stable, never approaches num_ctx) -> writer gets concrete figures INLINE with zero model tool-calls. The REVIEWER (which CAN dispatch load_source via the string-fallback parser) keeps real PULL to verify/ground. This is NOT fighting d49; it respects the measured model limit (d134 understand-and-fix). SUB-DECISIONS: (1) editing render_scoped_source_index + the feed assembly (_compose_task/_scoped_source_block) in agent_runtime IS the 'tool/feed' layer per d164/d156 — in-scope and correct (source-chunking IO, NOT task-specific core behavior). (2) KEEP the defensive _bound_chat_convo window in runtime — it is a GENERIC core context-discipline backstop (d147's 'context-disciplined core substrate'), defense-in-depth against multi-turn re-ballooning, applies to all roles -> legitimate in core PROVIDED it is generic + bounds GRACEFULLY (must NOT blind-drop figure-bearing content; the primary tool-push bounding means it rarely fires). FULL UNIFIED FIX = tool push-leads + WRITE-phase spec mandate (include concrete figures/dates verbatim + populate styled <table>s + cite each assigned source) + shape source-ceiling 6-8 (fan_out 5). Re-prove ONCE on full scope, reporting d162 (no input>=num_ctx-1) + d163/d164 (figures + majority sources cited) + d165 (styled populated tables) + d160 (breadth>=3 substantive) + d166 (tool-error census) on ONE run.
--- LBD[139] ---
d168 — a17 RE-PROVE OUTPUT = FAIL on SECTION COHERENCE (user flagged 'extremely bad'; neuron read artifacts/report.html 23:53 directly). The figure-truncation fix PARTLY WORKED (a real cost table with $113.3B/$25B/$631B/$1T + gas $4.23/gal, 5 sources cited, 6 figures, coherent prose in the FILLED sections) — but the report is a BAD deliverable because the SECTION PLANNING/ASSEMBLY is broken: (1) 3 of 6 sections are EMPTY '_No sources were found... UNSUPPORTED_' stubs INCLUDING THE LEAD/INTRO ('Report on the US-Iran Conflict...'), plus 'Analysis and Future Outlook' and 'Costs to All Parties'; (2) DUPLICATE/OVERLAPPING sections — TWO Analysis (#analysis filled + #analysis-2 empty) and TWO Costs (cost table buried INSIDE Timeline + a dedicated empty 'Costs to All Parties' section); the decompose produced overlapping facets and the write-planner couldn't assign sources to the redundant ones -> they emit UNSUPPORTED; (3) a BROKEN TABLE CELL (line 67: '<td ...>Econ<td ...>Khanna...' — malformed/truncated nested cell). ROOT (distinct from d164 truncation, which IS improved): the breadth/decompose (a15) + write-section mapping produce REDUNDANT + UNDER-ASSIGNED sections (some sections get 0 sources -> empty; overlapping facets -> dup sections), and the lead/intro section gets no sources -> empty intro. The d163/d165 figure+table gate is necessary but NOT SUFFICIENT — section COHERENCE must also be gated. GATE ADDITION (mandatory on the re-prove, fails this run): the final report must have (a) NO empty/UNSUPPORTED sections in a 'detailed report' deliverable (every authored section is filled, or not authored — no empty stubs, especially not the LEAD), (b) NO duplicate/overlapping sections (one Analysis, one Costs; the decompose facets must be DISTINCT and the write-sections 1:1 with them), (c) WELL-FORMED HTML (no malformed/truncated table cells), (d) a real lead/intro summary. Likely fix locus: the decompose methodology (shape) must yield DISTINCT non-overlapping facets, and the write-planner's section<->source assignment must cover every authored section (no 0-source authored sections; fold redundant facets). This re-prove does NOT pass; a17/planner must fix section coherence + re-prove, with d168 coherence criteria added to the unified gate (alongside d160 breadth, d162 bounded, d163/d164 figures, d165 visual, d166 tool-errors). NEURON LESSON (again): READ the actual report, do not trust the figure/table COUNTS — 6 figures + 1 table looked like improvement but the doc is half-empty + duplicated + has a broken table; only reading it caught it (the user caught it first).
--- LBD[140] ---
d169 — TABLE-CORRUPTION ROOT CONFIRMED (user hypothesis 'a reviewer broke it' = CORRECT; run2 trace 77575f0f). The FINAL_REVIEW node (review.anchored, NOT n2_review as the first Explore guessed) corrupted a CLEAN table cell via an HTML-unaware anchored str-replace (file_update). EVIDENCE (both versions present in the final_review span): clean '<td ...>Economic Cost Estimate (US Economy)</td>' ($631 billion) -> broken '<td ...>Econ<td ...>Khanna's Estimated Cost (US Economy)</td>'. MECHANISM: the reviewer tried to RENAME the cell (Economic Cost Estimate -> Khanna's Estimated Cost) but the raw string-replace matched PARTIALLY — left the stray 'Econ', injected a NESTED <td> instead of replacing the cell contents -> shattered markup. ROOT (architectural, the real lesson): the anchored review edit path does RAW str-replace (file_update old->new) on HTML, which is NOT structure-aware, so a partial/greedy match shatters tag boundaries. This will corrupt structured markup eventually regardless of which run/node. AUTHORITATIVE DAG of run2 (12 nodes, from trace node.id->node.tool + writer CURRENT TASK): research s1_B1/s1_B2/s1_B3 (web_search, 3 facets, 5 chain-sources); write/review (all tool=file_write): n1 'HTML shell + nav', n2 'Timeline & key events', n3 'comprehensive analysis synthesizing timeline+events+COSTS', n4 'Costs to all parties (military/economic/casualties/damage)', each n*->n*_review, + final_review. Framework chain n1->n1_review->...->n4_review->final_review (depends_on edges NOT persisted in the trace — only the framework pattern). KEY DIAGNOSTIC: n3 (analysis synthesizing COSTS) and n4 (Costs) OVERLAP by design = the d168 duplicate/empty-section root (overlapping facets -> one section absorbs costs, the dedicated one ends UNSUPPORTED). FIX: (1) anchored HTML edits must be STRUCTURE-SAFE — the reviewer should re-emit the section cleanly OR the edit path must validate/repair HTML after each anchored edit and REJECT edits that produce malformed markup (no orphan/nested <td>, balanced tags); (2) the final_review especially should not free-str-replace inside tables. Add to the d168 gate: well-formed-HTML assert AFTER the review/final-review pass (the corruption is post-write, at review). Also fold into the d168 facet-distinctness fix (n3/n4 overlap). NEURON NOTE: the first Explore inferred n2_review (wrong node) + reconstructed the edit; direct trace extraction corrected it to final_review with the exact before/after — verify edits directly, don't relay an inferred attribution.
--- LBD[141] ---
d170 — FOUNDATIONAL ROOT (the a->b oscillation NAMED + the user's starvation hypothesis CONFIRMED + shape/spec validity verified). This explains the multi-day fix-a-then-b loop. (1) SHAPE/SPEC FILES ARE VALID + WIRED (verified, not assumed): all 6 shape TOMLs parse; all 12 spec .md have valid frontmatter; research_tree.py substitutes the shape's completeness_stop (~1153-1290) and decompose_methodology (~1380-1526) VERBATIM into the decision/decompose node instructions, GUARDED by an assert ('shape-defined ... substitution would silently no-op') so wiring can't silently fail; shapes.py loads them from TOML (~233-260). The deep-research.toml decompose_methodology (edited Jun26 00:36, AFTER run2 23:53) now EXPLICITLY forbids the exact n3/n4 overlap run2 produced ('an analysis-of-the-costs facet ALONGSIDE a dedicated costs facet -> the analysis absorbs the figures and leaves the dedicated section empty') -> run2 ran on the OLD weaker doctrine; the shape is iterating correctly; the open question is only E4B ADHERENCE to the doctrine, not validity/wiring. So the shape/spec are NOT the bug. (2) THE WRITER IS STARVED BY THE CHUNKING (user hypothesis CONFIRMED, trace 77575f0f, quoted tool calls): the push-leads/render_scoped_source_index feed each section writer only ~2400 chars per source (25-50% of each 5-9KB article), inline, with '[showing 2400 of N chars - call load_source for the rest]'. The writer NEVER calls load_source (it CAN'T reliably - d49 raw-write loop) -> it writes ONLY from the truncated 2400-char lead -> thin, generic output (1-2 events/section, figures only those that happened to be in the lead, NO casualties, no depth). GOOD run (bc7cef17): writer got ~full source bodies (~20-39KB per writer turn, un-truncated) -> rich structured tables + multiple dated events + specifics (80 figures). (3) THE OSCILLATION NAMED (the user's 'a then b then a then b'): d164 = OVER-FED (137KB unscoped dump) -> TRUNCATED -> hollow. d167 push-leads = UNDER-FED (2400-char leads, writer can't pull more) -> STARVED -> thin. SAME 'thin' symptom, OPPOSITE cause. We swung past the target instead of hitting it. (4) THE CALIBRATION THAT ESCAPES THE LOOP: the truncation was from UNCAPPED 11 sources x full bodies to a SINGLE unscoped writer = 137KB. The CORRECT bound is source-cap (max_sources=8, done) + PER-SECTION SCOPING (each writer gets ONLY its section's ~2-4 assigned sources) -> full bodies of 2-4 sources ~= 20-40KB -> FITS num_ctx WITHOUT truncation AND WITHOUT starvation (exactly what the good run did). The 2400-char lead truncation in d167 was UNNECESSARY over-correction once sources are capped+scoped: FEED THE PER-SECTION WRITER THE FULL (or near-full) BODIES OF ITS ASSIGNED SOURCES, not 2400-char leads. Bounded-but-not-starved. (5) SECONDARY (real, lower-priority): no holistic compose pass (stitched fragments, empty lead, blind final_review) - d169/d168 - but the PRIMARY low-quality cause is the starvation calibration. FIX (escapes a->b): raise the per-section source-feed to full/near-full bodies of the section's assigned sources (use the 68% num_ctx headroom; the source-cap keeps it bounded), since the writer cannot pull on demand. Verify on the trace that each writer sees its sources' FULL figure-bearing text AND no call >= num_ctx-1. NEURON LESSON: I tuned for SIZE (down) without verifying SUFFICIENCY - the user forced the foundational tool-call read that found it.
--- LBD[142] ---
d171 — a17 run4 = GENUINE PASS, NEURON-READ-VERIFIED (artifacts/report.html, 20.2KB, Jun26 01:24; not count-trusted — I read the full rendered report). The d170 calibration (feed per-section writers the FULL bodies of capped+scoped sources, not 2400-char leads) WORKED and broke the multi-day loop. EVIDENCE: TEXT — real Executive Summary with synthesized figures ($113.3B, $132B); DETAILED DATED timeline (Oct2023 proxy strikes, Jun21-2025 Fordow/Isfahan/Natanz, Mar2026 Tehran/Beirut+oil refineries, Apr9 254 killed/1100 wounded, May24 441kg enriched uranium, Jun16 108-day conflict, Jun22 4175 killed/12164 wounded); TWO populated tables — financial ($113.3B/$132B/$60B fuel/$120 barrel/$300B reconstruction/$80B supplemental) AND casualties across ALL parties (Iran 1701/3468/2350+, Lebanon 4175/12164, US 13, Israel 26) — casualties were MISSING in run2; substantive analysis + 4-row risk table; 8 diverse NON-wikipedia sources, citations resolve. STYLE — full CSS (Georgia serif, deep-red theme, FLEX nav, yellow callout exec-summary box, 3 ZEBRA tables, dark header rows). STRUCTURE — 5 DISTINCT sections, 0 empty/UNSUPPORTED stubs, 0 duplicate families, 0 broken table cells. This clears d160 (breadth/substantive) + d163 (figures) + d165 (visual) + d168 (coherence: no empty/dup) + d169 (well-formed, no broken cells) + d170 (writer not starved) on ONE live full-scope E4B served run (1432s). Arguably richer than the original good run (p2_run1) — full cost+casualty tables vs prose. MINOR cosmetic blemishes (NOT blockers, quick fix): (1) source #8 title truncated at the very end ('Hormuz on the Brink:</body></html>'); (2) leftover '<!-- Subsequent sections will be added here -->' shell comments between sections. STILL OWED before Batch A (s14) closes: the d166 tool-error census (a17 noted the census hook gap), the standing parity (B8b/c/d acceptance, send_mail self-lock, no-wiki) on this calibrated path, and the d152 output-mode test (HTML 2x + SSE + memory-Q&A, Batch C). The secondary holistic-compose concern (d170) mostly resolved itself (real exec summary, coherent sections). NET: the report-quality problem the user pushed on for days is RESOLVED at the source-feed calibration layer; verify the remaining gate items + fix the 2 cosmetics, then this milestone holds.
--- LBD[143] ---
d172 — USER (2026-06-25) GIT WORKFLOW for Phase 3: STACKED BRANCHES, push per batch-completion. phase3-batch-a branches off main; phase3-batch-b off phase3-batch-a; phase3-batch-c off phase3-batch-b; then merge the chain back to main. Push each branch to origin (github.com/visak13/autonomous-agents, AGPL public) AS AND WHEN each batch completes. NEURON OWNS GIT (d-prior: 'I will require your help to commit the work, in this session, no delegation') — the workers/planner must NOT git-commit/branch/push; they complete the build + report, the neuron does all git. CURRENT STATE (verified): repo C:/Projects/ReactiveAgents on main (==origin/main d814b23); ALL Batch-A (s14) work is 23 UNCOMMITTED code files on main (agentic.py, research_tree.py, review_injection.py, runtime.py, shapes.py, deep-research.toml, synth_tools.py, file_tools.py, incremental.py, factory.py, plan_tools.py, + tests) — no Phase-3 commits yet; gitignore covers .env/*.db/var/.s13_design/.s14_design so no secrets/data go public. PLAN: when Batch A FORMALLY closes (the 2 cosmetics d171 + tool-error census d166 + standing parity all done, build pauses) -> git checkout -b phase3-batch-a off main, commit the Batch-A work (clear message; verify no secrets), surface the commit scope to the user, then push. Batch B work then happens on phase3-batch-b (off batch-a), Batch C on phase3-batch-c (off batch-b), merge to main at Phase-3 end. NOT touching git mid-build (avoid clashing with active s14 workers). HOLD the outward push until the user sees the commit scope (public repo, outward/irreversible).
--- LBD[144] ---
d173 — COHERENCE IS NON-DETERMINISTIC: a11 gate run1 = STRUCTURAL COLLAPSE (user flagged quality drop; a11 itself caught it on human read while the count-harness FALSELY passed coherent=True; neuron read artifacts/report.html 37.5KB Jun26 02:48 + compared to the saved-good run4). KEY: the d170 calibration reliably fixed CONTENT (a11-run1 still has 2 styled tables + a 13-event dated timeline + figures $11.3B/3.2M displaced/87 deaths + rich prose) — the TEXT did NOT drop. What collapsed is STRUCTURE/ASSEMBLY, and it is RUN-TO-RUN VARIANCE: run4 came out clean (5 distinct sections, 1 sources block, nav matches) but a11-run1 is a mess: (1) the SHELL node n1 pre-created 4 EMPTY STUB sections ('<!-- Content for X will be added in subsequent turns -->') with the nav ids (#timeline/#headlines/#costs/#analysis) — the SAME 'Content will be added' skeleton pattern as the original run2 failure; (2) the nav links point to those EMPTY stubs; (3) each content writer APPENDED its real section with a DIFFERENT id (#timeline-2, #costs-2, #analysis-2, #reasons-for-conflict) at the BOTTOM, after a sources block; (4) EACH writer also emitted its OWN '<div class=source-list>' -> the Sources block is DUPLICATED 5-6 TIMES through the doc; (5) duplicate section FAMILIES (timeline+timeline-2, costs+costs-2, analysis+analysis-2, headlines x2); (6) sources block appears mid-document, not at end. So nav->empty stubs, real content orphaned at bottom, 5-6 sources blocks, duplicate sections. ROOT (distinct from d168 which I thought fixed it): run4 was a LUCKY clean run; the structural ASSEMBLY/NORMALIZATION is NOT ROBUST to E4B's variable shell/writer behavior. The deterministic backstops (assemble_html_spa / collapse_duplicate_sections / nav rebuild) do NOT catch: '-2'-suffixed duplicate sections, multiple source-list blocks, empty stub sections, nav-vs-real-section mismatch. AND the COUNT-HARNESS coherence check is inadequate (passed coherent=True on this mess) — a11's human read is what caught it. CONSEQUENCE: Batch A is NOT done; coherence must be made DETERMINISTIC regardless of E4B variance. FIX (deterministic assembly, structural/post-write — must NOT depend on E4B): (a) merge/dedupe to ONE sources block at the END; (b) collapse duplicate section families incl. '-2'-suffix variants (keep the FILLED one, drop the empty stub); (c) rebuild the nav from the ACTUAL final section headings (not the shell's pre-stubs); (d) drop/forbid empty stub sections (the shell must NOT pre-create '<!-- will be added -->' placeholders; it makes lead+nav only, writers append real sections, nav reconciled after). Also: the gate MUST require coherence on a HUMAN read across BOTH 2x runs (the count-harness coherence assert is insufficient — strengthen it: 0 duplicate id-families, exactly 1 sources block, nav targets all resolve to NON-empty sections). NEURON LESSON: I read run4 (clean) and called the coherence FIXED (d171) on ONE run; a11-run1 proves coherence is variable -> never certify a non-deterministic property on a single run; require 2x both-coherent (matches the d160/d152 2x doctrine). Batch A stays open until coherence is deterministic + proven on 2x.
--- LBD[145] ---
d174 — TRACE-CONFIRMED MECHANISM of the a11-run1 coherence collapse (corrects/sharpens d173's report-inference; trace c5fdc6c0 vs clean 77575f0f + code). FINDING 1: the deterministic normalization DID RUN (trace synthesis.doc_reconciled=true; agentic.py:1719-1732 executes enforce_single_html_document, repair_table_cells, collapse_duplicate_sections, collapse_outline_duplicate_sections, assemble_html_spa, rebuild_sources_list, strip_scaffold_comments) — so it is NOT 'assembly didn't run'; it RAN and MISSED. The 3 EXACT code GAPS: GAP1 collapse_duplicate_sections (synth_tools.py:1271-1317) dedupes by HEADING family via _HTML_HEADING_RE (synth_tools.py:834, matches only <h1>/<h2>) — the EMPTY STUB <section id=timeline> has NO heading (just a comment) so it is NEVER matched/collapsed; the real <section id=timeline-2> has a heading but is the only one in its family -> both survive. GAP2: NO function dedupes multiple <div class=source-list> blocks ANYWHERE (rebuild_sources_list only repairs a truncated <ol>) -> each writer's own sources block survives = 6 blocks. GAP3: reconcile_doc_structure (synth_tools.py:1059-1083) rebuilds the nav from headings AFTER collapse, but since the empty stubs persist (GAP1) the nav links (shell's #timeline) don't match the real ids (#timeline-2) -> broken nav. FINDING 2 (the important one): the shell variance is a PLANNER-PLAN difference, and it reframes run4: in the BROKEN run n1's task = 'Write the initial HTML page shell and the navigable TOC... establish the overall structure and link to all subsequent sections' -> n1 pre-created the full shell WITH empty stub sections + nav. In the CLEAN run4 n1's task = 'This planned section has NO supporting sources... write ONLY the heading + UNSUPPORTED line' -> n1 wrote a MINIMAL shell, NO stubs. SO run4's cleanliness was PARTLY AN ACCIDENT — its shell node was (accidentally) marked source-less and skipped the stub-creation; the broken run's shell did its 'proper' job (create shell+TOC+stub sections) and THAT is what produced the empty-stub + nav-to-stub mess. The 'create an HTML shell with empty stub sections for later fill' pattern is inherently mismatch-prone (shell pre-commits nav+stub-ids; content writers append real sections with new ids + own sources). PRECISE FIX (replaces d173's inference): (A) DETERMINISTIC ASSEMBLY — the robust guarantee: (1) collapse_duplicate_sections must dedupe by SECTION ID too (collapse id=X and id=X-2 / empty-stub-vs-filled, keep the FILLED one), not heading-only; (2) ADD source-list dedup -> exactly ONE <div class=source-list> at the END; (3) rebuild the nav from the ACTUAL final section ids after collapse (fix the id-collision). (B) SHELL-TASK — stop the shell pre-creating empty stub sections: the shell authors lead + a nav PLACEHOLDER only; content writers create the real sections; nav reconciled from real sections at the end. Do BOTH (A is the must-have deterministic guarantee; B reduces mess at source). NEURON LESSON: I diagnosed d173 from the rendered REPORT and left 'did assembly run' ambiguous; the user forced the trace read which showed assembly RAN + the 3 precise gaps + that run4 was accidentally clean (shell unsupported). Symptom from output, MECHANISM only from the trace.