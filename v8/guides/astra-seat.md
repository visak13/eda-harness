# astra-seat — GPT-6 Astra as a 1:1 fleet seat (epic-6a8a6020fd · s-e1260012b9)

Owner words: "use GPT 6 Astra with any harness out there we can to add Claude Code like monitor and cron tools … behave exactly the same". Design: board doc design-97ca02e989 (this guide carries its survey, terms and control cards verbatim so the record is readable without the board) plus the spike evidence. Parity reference: `guides/harness-parity.md`.

## 1. The fidelity requirement, decomposed (what "exactly the same" needs from a harness)
| need | Claude Code behaviour | harness primitive required |
|---|---|---|
| N1 tool set | `Monitor{command|ws,description,persistent,timeout_ms}`, `TaskStop`, `CronCreate{cron,prompt,recurring,durable}`, `CronList`, `CronDelete` with fixed description/result strings | client-registered tools, in-process, long-lived background work allowed |
| N2 idle wake | a Monitor line or a due cron becomes a new user turn only when the REPL is idle | idle signal + "deliver when idle" primitive |
| N3 mid-turn attach | a Monitor line during a turn is attached to the model's NEXT tool result (any tool) | a hook that can mutate/append to any tool result |
| N4 cron rules | 5-field local cron, recurring/one-shot, deterministic jitter, 7-day expiry, session-only, fires only when idle | harness-owned scheduler + N2 |
| N5 boot | role card as first input, edp8 MCP (streamable HTTP + `X-Participant/X-Token` headers), resume | system/developer prompt injection, MCP client with headers, session resume |
| N6 fleet | spawn/alive/kill/pid/last-output from the pool; `EDP_HARNESS` park semantics | headless start, machine-readable events, stop |

## 2. Survey — 19 harnesses, scored 0–3 on the needs above (full per-harness notes in the research record; A = Astra + auth path, B = in-process tools with background push, C = mutate next tool result, D = idle signal, E = prompt injection, F = MCP HTTP + headers, G = resume, H = hooks, I = licence, J = maturity)
| Harness | A | B | C | D | E | F | G | H | I | J | Σ | one-line verdict |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **Pi** 0.85.1 (MIT) | 3 | 3 | 3 | 3 | 3 | 0 | 3 | 3 | 3 | 3 | 27 | every primitive first-class: `registerTool`, `sendMessage(deliverAs:"followUp"|"steer", triggerTurn)` ("followUp… delivered only when idle") **[D]**, `tool_result` content patch **[D]**, `isIdle()`/`agent_settled` **[D]**; Astra on API key and Codex subscription since 0.85.1 **[D]**; NO MCP client (extension needed) **[D]** |
| **OpenCode** 1.18.30 (MIT) | 3 | 2 | 3 | 3 | 3 | 3 | 3 | 3 | 3 | 3 | 29 | `tool.execute.after` mutates `output.output` **[D]**, `session.idle` **[D]**, MCP `remote` with `headers` **[D]**, Astra prompt added 1.18.30 **[D]**; busy session throws `BusyError` (lock, not queue) → in-flight path must buffer **[D]**; pool backend already exists |
| **Codex app-server** 0.154 (Apache-2) | 3 | 3 | 3 | 3 | 3 | 3 | 3 | 2 | 3 | 3 | 29 | `dynamicTools` + client-owned `item/tool/call` results, `turn/steer`, `thread/status/changed` **[D]**; native tool results (shell, patch) are NOT client-mutable — only `PostToolUse additionalContext` as developer context **[D]** → N3 partial; experimental flag |
| **OpenClaw** 2026.9.4 (MIT) | 3 | 3 | 2 | 3 | 3 | 3 | 3 | 3 | 3 | 3 | 29 | ships all of it (Codex harness, idle-deferring heartbeat cron, `tool_result_persist`) **[D]** but is a large gateway platform; native-tool results immutable under the Codex harness |
| **Hermes** 0.21.2 (MIT) | 3 | 3 | 3 | 2 | 3 | 3 | 3 | 3 | 3 | 3 | 29 | Python tools, `transform_tool_result` **[D]**, `inject_message` **[D]**; cron runs in isolated sessions (not N2) **[D]** |
| Kilo 7.6.2 | 2 | 3 | 3 | 2 | 2 | 3 | 3 | 3 | 3 | 2 | 26 | OpenCode lineage, Codex catalog lags Astra **[D]** |
| DeepSeek Harness 0.1.5-rc (MIT) | 2 | 3 | 3 | 3 | 2 | 3 | 2 | 3 | 3 | 1 | 25 | most exact native Monitor/Cron semantics (`jobs`: busy → inject next step, idle → follow-up turn) **[D]**, but "OAuth… such as Codex, not supported yet" **[D]**, daily breaking rc |
| Claude Code (reference) | 0 | 3 | 3 | 3 | 3 | 3 | 3 | 3 | 0 | 3 | 24 | the tools themselves; proprietary; non-Claude routing "not supported" **[D]** |
| Cline CLI/SDK | 3 | 2 | 0 | 2 | 3 | 3 | 2 | 2 | 3 | 2 | 22 | `tool_call_after` observe-only **[D]** |
| Amp (proprietary) | 2 | 3 | 3 | 3 | 1 | 3 | 2 | 2 | 0 | 2 | 21 | primitives exist, inference proxied through Amp, closed |
| Copilot CLI/SDK | 1 | 2 | 3 | 2 | 2 | 2 | 2 | 3 | 0 | 3 | 20 | GitHub-routed models, no ChatGPT OAuth |
| Mistral Vibe | 1 | 0 | 2 | 1 | 2 | 3 | 2 | 2 | 3 | 2 | 18 | API-key only |
| Goose 1.50 | 2 | 0 | 0 | 0 | 2 | 3 | 2 | 2 | 3 | 3 | 17 | MCP-only tools, hooks "don't alter results" **[D]**, scheduler = new sessions |
| Zed/ACP (+codex-acp) | 3 | 1 | 0 | 2 | 1 | 2 | 2 | 0 | 3 | 2 | 16 | transport only: no client tools, no idle push, no steer **[D]** |
| Droid / Cursor CLI | 1/0 | 0 | 1 | 2 | 2 | 3 | 2 | 2/3 | 0 | 2 | 15 | proprietary, no Astra/BYOK path that fits |
| Crush 0.94 (FSL) | 3 | 0 | 0 | 0 | 1 | 3 | 2 | 1 | 2 | 2 | 14 | MCP-only, only PreToolUse |
| Warp/Oz, Aider, Gemini CLI | ≤1 | 0 | ≤2 | ≤1 | ≤2 | ≤3 | ≤2 | ≤3 | 3 | ≤3 | ≤20 | out (no Astra path or no tools/hooks) |
Equal-weighted sums are shown; on the needs that decide this epic (B, C, D, A) **Pi** is the only harness where every primitive is a first-class documented API, with **OpenCode** and **Codex app-server** next.

## 3. Terms — per route, primary sources (research record has the quotes and URLs)
Anthropic: Claude Code is proprietary under the Commercial Terms even when no Anthropic model is called **[D]**; "Anthropic doesn't support routing Claude Code to non-Claude models through any gateway" (llm-gateway page) **[D]** — unsupported, not prohibited **[I]**; every Anthropic credential/harness restriction (OAuth "exclusively for purchasers", third-party tools "misrepresent their identity… prohibited") concerns Claude credentials, which route A never sends **[D]**; residual risk = future client attestation **[I]**. Copying Claude's tool description texts into another harness is an IP question Commercial Terms D.4 ("duplicate the Services") does not settle either way **[Astra]** — the owner owns that call.
OpenAI: API use = permitted under the Services Agreement **[D]**; consumer ToU forbids "automatically or programmatically extract data or Output", circumventing limits, credential sharing **[D]**; the Codex auth doc says "Use API key authentication for programmatic Codex CLI workflows, such as CI/CD jobs" **[D]**; "Codex for OSS" names Codex, OpenCode, Cline, pi, OpenClaw approvingly — an endorsement, not a contractual grant **[D]/[I]**; ChatGPT Pro guidance forbids "reselling access or using ChatGPT to power third-party services" (an owner-only agent is not that, but an always-on seat is "programmatic") **[D]/[I]**.
| route | Anthropic | OpenAI | licence | risk note |
|---|---|---|---|---|
| A1 Claude Code binary → local proxy → OpenAI **API key** | unsupported, not prohibited | permitted | CC proprietary; proxies MIT | fast mode / WebFetch domain check / Remote Control call api.anthropic.com directly and break **[D]**; cleanest A route |
| A2 same with **ChatGPT OAuth** | as A1 | unclear/tolerated at best; auth doc steers programmatic use to keys | as A1 | double-unofficial; raine README: "Unofficial clients may carry account risk" **[D]** — highest account exposure |
| B1 OSS harness (Pi/OpenCode/Hermes/…) + **API key** | n/a | permitted | MIT/Apache; Crush FSL; Amp proprietary | Astra on API: $10/$50 per M tokens, 1M context **[D]** |
| B2 OSS harness + **ChatGPT OAuth** | n/a | tolerated/publicly endorsed for Pi, OpenCode, Cline, OpenClaw; not contractual; unattended use → keys per auth doc | plugin READMEs: "personal development use… for production… use the Platform API" **[D]** | unnamed harnesses (Goose, Crush, Hermes, Amp, DSH) have no endorsement |
| C native Codex (CLI/app-server/SDK) + ChatGPT login | n/a | permitted for interactive use; unattended → API key or Enterprise "Codex access tokens" **[D]** | Apache-2 | lowest OpenAI risk of the subscription routes |
| D-key in-house on Responses API | n/a | permitted | ours | cost only |
| D-oauth in-house reusing `~/.codex/auth.json` | n/a | unclear, most exposed | ours | no endorsement umbrella at all |
Count: four routes have a clear OpenAI basis (A1, B1, C, D-key); the three OAuth routes (A2, B2, D-oauth) are tolerated-to-unclear; none is categorically prohibited by topology **[Astra]** **[I]**. Anthropic only governs the A routes (unsupported) plus the text-copying question for every other route.

Primary sources (fetched 2026-09-14 by the terms research pass; quotes in the design's research record):
- Anthropic, "LLM gateway configuration" — "Anthropic doesn't support routing Claude Code to non-Claude models through any gateway": https://code.claude.com/docs/en/llm-gateway
- Anthropic Commercial Terms of Service (§D.4 "duplicate the Services", competing products): https://www.anthropic.com/legal/commercial-terms
- Claude Code repository licence (points to the Commercial Terms): https://github.com/anthropics/claude-code/blob/main/LICENSE.md
- OpenAI Services Agreement (API; §2.2 integrate into Customer Applications, §3.3 restrictions): https://openai.com/policies/services-agreement/
- OpenAI Terms of Use (consumer; "automatically or programmatically extract data or Output", credential sharing): https://openai.com/policies/row-terms-of-use/
- OpenAI Codex authentication ("Use API key authentication for programmatic Codex CLI workflows, such as CI/CD jobs"): https://developers.openai.com/codex/auth (308 → https://learn.chatgpt.com/docs/auth)
- OpenAI "Codex for OSS" (names Codex, OpenCode, Cline, pi, OpenClaw): https://developers.openai.com/community/codex-for-oss
- OpenAI "What is ChatGPT Pro" ("reselling access or using ChatGPT to power third-party services"): https://help.openai.com/en/articles/9793128-what-is-chatgpt-pro
- OpenAI GPT-6 Astra model page (API availability, context, pricing): https://developers.openai.com/api/docs/models/gpt-6-astra
- raine/claude-code-proxy README ("Unofficial clients may carry account risk"): https://github.com/raine/claude-code-proxy
- Pi 0.85.1 release ("GPT-6 Astra … available through OpenAI API keys and OpenAI Codex subscriptions"): https://pi.dev/news/releases/0.85.1

## 4. Remote targeting — control-surface cards (how the Python pool drives each shortlisted harness)
| harness | transport / start | boot + prompt | client tools | events | idle | steer | resume | stop | Python client |
|---|---|---|---|---|---|---|---|---|---|
| **Pi** | `pi --mode rpc --session <file> --model openai-codex/gpt-6-astra -e edp8.ts --append-system-prompt …` — strict JSONL on stdio, LF-only, optional `id` [D] | `{"type":"prompt","message":"<role card>"}` → `{"type":"response","command":"prompt",…}` | TS extension: `pi.registerTool({name,description,parameters,execute})`; background push `pi.sendMessage(msg,{deliverAs:"followUp"|"steer"|"nextTurn",triggerTurn})` [D] | `agent_start`, `turn_start/end`, `tool_execution_*`, `tool_result`, `queue_update`, `extension_ui_request` [D] | `agent_settled` ("no automatic retry, compaction retry, or queued continuation remains") + `get_state → isStreaming` [D] | `steer`, `follow_up` [D] | `--session`, `switch_session`, `fork` [D] | `clear_queue` then `abort`, then kill [D] | none needed — raw JSONL over `subprocess` |
| **OpenCode** | `opencode serve --port N --hostname 127.0.0.1` (`OPENCODE_SERVER_PASSWORD`), HTTP + SSE [D] | `POST /session` → `POST /session/:id/prompt_async {parts:[{type:"text",text}],model,agent,system}` [D] | plugin `.opencode/plugins/*.ts`: `tool({description,args,execute})`; hooks `tool.execute.before/after`, `session.idle`, `session.status`, `chat.message` [D] | `GET /event` SSE (`message.part.updated`, `permission.updated`, `session.status`) [D] | `session.idle` event; `GET /session/status → idle|busy` [D] | none; busy → `BusyError`, buffer in plugin [D] | `-s <id>`, `/session/:id/fork` [D] | `POST /session/:id/abort` [D] | `httpx` + SSE (pool already has `OpencodeSpawner`) |
| **Codex app-server** | `codex.exe app-server` stdio NDJSON JSON-RPC (or `--listen ws://127.0.0.1:PORT`); daemon/remote-control reported broken on Windows (#42507) [D] | `initialize{capabilities.experimentalApi:true}` → `initialized` → `thread/start{cwd,sandbox,dynamicTools,developer_instructions,config}` → `turn/start{threadId,input}` [D] | `dynamicTools` on `thread/start`; model call = server request `item/tool/call`, answered by JSON-RPC id with `{contentItems,success}` [D]; MCP `url` + `http_headers`/`env_http_headers` [D] | `turn/started|completed`, `item/*`, `thread/tokenUsage/updated`, `account/rateLimits/updated` [D] | `thread/status/changed{status:{type:idle|active(activeFlags)}}` [D] | `turn/steer{expectedTurnId}` [D] | `thread/resume` (dynamic tools restored) [D] | `turn/interrupt` + kill | `pip install openai-codex` (dynamic-tool callbacks unconfirmed) or raw JSON-RPC |
| **Claude Code** (reference / route A) | as today: interactive CLI under ConPTY/console from the pool; `ANTHROPIC_BASE_URL/AUTH_TOKEN/MODEL` env selects the proxy [D] | role card typed as activation (existing) | its own tools — Monitor/Cron are interactive-CLI tools, NOT in the Agent SDK / `-p` mode [D] | PTY drain log | n/a (the pool never infers idle) | n/a | `--resume` (existing) | existing | n/a |
| **ACP** (any adapter) | `initialize` → `session/new{cwd,mcpServers}` → `session/prompt` → `session/update` → `session/cancel`; `pip install agent-client-protocol` [D] | `session/prompt` | **none** — the client can only pass MCP servers [D] | `session/update` (`tool_call`, `agent_message_chunk`, …) | none pushed; the turn ends when `session/prompt` returns `stopReason` [D] | none | `session/load` / `resume` (capability) | cancel + kill | official |
ACP is a transport, not a harness for this epic (no client tools, no idle push, no steer) [D]; it stays useful later as one client for the Claude/Codex/OpenCode/Goose/Kilo/Pi adapters.
Fleet wiring is identical for every non-Claude path: a third `Spawner` in edp-pool mirroring `opencode_launcher.py` (`launch/alive/kill/knows/pid/last_output_ts` + `exit_code/viewport_died/session_token`), `CompositeSpawner` route by `EDP_<HARNESS>_ROLES`, `EDP_HARNESS=<name>` for the park hint, env from `build_env` minus claude-only keys with `EDP8_TOKEN` merged after the secret strip; the seat identifies to the board by header exactly as `.mcp.json` does.

## 5. Recommendation and decision
**Primary: Pi.** The only harness whose documented API covers N1–N4 without adaptation: `registerTool` (N1), `sendMessage(deliverAs:"followUp")` = "delivered only when idle" (N2, N4), `tool_result` content patch on any tool (N3), `agent_settled`/`isIdle()` (N2), JSONL RPC from Python (N6), Astra on both auth paths since 0.85.1, MIT, an OpenAI-named harness. Its one gap is N5's MCP client: Pi has none, so the edp8 extension bridges the board's MCP over streamable HTTP with headers (moderate, spike-verified). [Astra: Pi first]
**Secondary: OpenCode.** Native MCP + headers, pool backend exists, `tool.execute.after` (N3), `session.idle` (N2); weaker on N2/N3 because a busy session rejects prompts (buffer-then-flush in the plugin) and there is no steer.
**Fallback: Codex app-server** — permitted and native, but N3 only for its own dynamic tools; native shell/patch results can only receive `PostToolUse additionalContext` developer text, so "attached to the next tool result" is not literally met. [Astra]
**Conditional experiment: route A1** (Claude Code + local proxy + OpenAI API key). Literal parity by construction (N1–N4 are Claude's own code), Anthropic-unsupported but not prohibited, OpenAI-permitted. A half-day spike, because translation fidelity (tool schemas/ids/streaming/thinking/compaction) and feature leakage to api.anthropic.com are unproven, and because the owner must accept "unsupported" as a production posture. [Astra: conditional]
**Auth for a resident seat: OpenAI API key, not ChatGPT OAuth.** OpenAI's auth doc routes programmatic/unattended workflows to keys; an always-on seat with 30-min heartbeats is that. ChatGPT OAuth is acceptable for spikes and the human-present drill only, with the owner's explicit acceptance. Cost at Astra API pricing is an owner decision (G1).
**Parity is proven, not asserted:** `guides/harness-parity.md` captures Claude's tool schemas/texts/envelopes and MEASURES the unstated rules from a live seat's session JSONL (the model-input boundary); `scripts/parity_oracle.py` replays one scripted sequence through a Claude seat and the Astra seat and diffs at that boundary, under a controlled clock/seed for timing. [Astra]
**Text-copying exposure:** byte-equal tool descriptions in a non-Anthropic harness are an IP question the terms do not settle; the owner accepts it at G1 or the descriptions are paraphrased (then "exactly the same" narrows to behaviour + schemas).

## 6. Spike results (SP · Pi) — 2026-09-14, architect seat, session 7edf0320

Pinned: Pi `@earendil-works/pi-coding-agent@0.85.1` — installed durably at `edp-pool/.pi-harness` (`package.json` + `package-lock.json` committed, `npm install --ignore-scripts`; `node_modules` ignored) and found by `edp8.pi_seat.driver.find_pi` / edp-pool `pi_bin_argv` without any env (override: `EDP_PI_HARNESS` or `EDP_PI_BIN`), Node v25.1.0, Claude Code 2.1.270 as the reference. Extension: `v8/.pi/extensions/edp8.ts`. Driver: `v8/src/edp8/pi_seat/driver.py`. Harness: `v8/tests/pi_ext/fake_pi_harness.ts` (run `node --experimental-strip-types tests/pi_ext/fake_pi_harness.ts` from `v8` with `node_modules/typebox` junctioned to Pi's bundled typebox 1.3.7).

| check | result | evidence |
|---|---|---|
| Pi lists Astra | PASS — model ids `openai/gpt-6-astra`, `openai/gpt-6-astra-fast`, `openai/gpt-6-astra-pro` (+`:batch`) in the 0.85.1 bundle; `get_available_models` is empty until a credential exists | grep of `dist/bundle/chunks/chunk-JVUZSMYM.js`; RPC `get_available_models` → `{"models":[]}` |
| Auth path | OPEN — Pi's store `~/.pi/agent/auth.json` is separate from `~/.codex/auth.json`; Pi offers OpenAI API key (`OPENAI_API_KEY`) or its own ChatGPT/Codex device-code login (`/login`). Owner asked 15:35Z (m-7fe3c34fac); no model turn until answered | `OPENAI_CODEX_DEVICE_CODE_LOGIN_METHOD`, `DEVICE_VERIFICATION_URI=…/codex/device` in the bundle |
| `pi --mode rpc` from Python | PASS — `PiSeat.start/get_state/stop` (spawn `node …/dist/cli.js --mode rpc -e … --no-session`, JSONL, `clear_queue`→`abort`→stdin close, exit 0) | driver smoke 15:46Z |
| Extension loads, registers the five tools by Claude's names + schemas + verbatim descriptions | PASS | `get_commands` shows `edp8`; harness "tools registered" |
| MCP bridge to the board (streamable HTTP, `X-Participant`/`X-Session`/`X-Token`, role-scoped URL) | PASS — `initialize` + `tools/list` → 37 edp8 tools registered as Pi tools, `tools/call` forwarded | extension log `mcp bridge: 37 tools from http://127.0.0.1:9402/mcp/architect`; harness "mcp bridge registered whoami" |
| Monitor result text, standalone envelope + `<system-reminder>` preamble, 200 ms batching, exit≠0 / exit 0 / timeout envelopes, TaskStop text + silence | PASS (byte-equal to parity §3/§4) | harness 21/21 |
| Monitor line during a busy turn attaches to the NEXT tool result (any tool) via `tool_result`; remainder flushed standalone at `agent_settled` | PASS in the fake harness (`tool_result` returns patched content); live Pi confirmation needs a model turn | harness "attach to next tool_result", "settled flushes pending standalone" |
| 500-char truncation, rate-limit suppression text | PASS (rate limit is a token-bucket approximation of the measured 2-delivered/6-suppressed pattern) | harness |
| CronCreate/CronList/CronDelete texts; idle-only fire; due-while-busy → deferred once, fired at `agent_settled`; bare prompt as user message | PASS in the fake harness; jitter = `fnv1a(jobId) % 900 s` (hypothesis fitted to the measured 629 s) | harness "cron due while busy: not fired", "cron deferred fire at settle, bare prompt" |
| Parallel tool-call ordering, cancellation with pending attach, resume with tools re-registered, provider-bound input capture | NOT RUN — need a model turn | — |
| S8 shared inference admission lane | PASS — `src/edp8/admission.py` (mkdir-atomic `lane.lock` + holder.json TTL 15 min, FIFO+priority tickets in `lane.queue`, bounded wait); consult() acquires per run (`lane_busy` error, never a hang), the Pi extension acquires per provider request and releases at `after_provider_response`/`agent_end`; a 429 writes consult.py's `quota.json` and posts `record_status(blocked)` | `tests/test_admission_lane.py` 5/5 (incl. two OS processes), `tests/test_consult_lane.py` 6/6, harness lane checks 7/7 (Python↔Node contention both ways) |
| S2 seat runner + pool spawner | PASS to the credential boundary — `python -m edp8.pi_seat.run` (role card as first prompt, session-file resume, resident loop) and edp-pool `PiSpawner` armed by `EDP_PI_ROLES` through `CompositeSpawner`; env overlay = pool contract minus claude keys, `EDP_HARNESS=pi`, token never on argv | `edp-pool/tests/test_pi_launcher.py` 3/3; runner smoke: Pi answers `No API key found for openai` |
| S3 parity oracle | Claude side + diff engine done — `scripts/parity_oracle.py --capture-claude` extracts tool_use/tool_result/standalone/attached/cron_fire events from a session JSONL (95 events from 7edf0320), normalises ids/paths/times, `--diff` = unified diff at the projected boundary, `--cases` = the 10 scripted cases; `--capture-pi` reads the driver's RPC mirror (schema to pin on the first live run); `--both` exits 2 until both seats have credentials | `tests/test_parity_oracle.py` 4/4 |
| Reachable tool surface of the seat | Pi built-ins (`bash`, `read`, `edit`, `write`, `grep`, `find`, `ls`) + the five parity tools + 37 edp8 tools (role-scoped by the proxy). No browser / computer-use / unreal MCP servers (Pi has no MCP client; only what the extension bridges) | `get_commands`, extension log |

### 6.1 Live results — Astra seat under Pi, 2026-09-14 (runs 3–6, oracle 0 diffs)

Auth: owner chose Pi's own ChatGPT/Codex device-code login (`~/.pi/agent/auth.json`, separate from `~/.codex/auth.json`; nothing copied); model `openai-codex/gpt-6-astra`, thinking medium (`models.json` seat `astra`, `roles_openai` column maps every role to it as an option beside `roles`). Owner rulings this session: the GPT shell opens as a visible console like the Claude shells (`PiSpawner` mode `monitor` = Pi TUI in `CREATE_NEW_CONSOLE`, `headless` = RPC runner); the role→model map is a second column, not a replacement.

| check | result | evidence |
|---|---|---|
| Live seat boots, `whoami` over the bridge, role card as first prompt | PASS | run 1 smoke (`live_smoke.py`), visible window pid 39084 |
| `scripts/parity_oracle.py --diff` on the 9 live cases (10th = accelerated expiry, fake harness only) | **PASS — 0 diffs, 37 events per side** | `tests/pi_ext/oracle_traces/{claude,pi}_trace_final.json`; re-run `python scripts/parity_oracle.py --diff tests/pi_ext/oracle_traces/claude_trace_final.json tests/pi_ext/oracle_traces/pi_trace_final.json` |
| Trace provenance | Claude: clean driver pass 17:25:26–17:37:17Z (cron 0ff23b29 one case per wake) with cases 3/4/5/7 re-run 17:41Z and case 4 again 17:5xZ after the case pin; Pi: run 4 (all 9) with cases 3/4/5/7 from run 5 and 4/7 from run 6 (same pins). Every splice replaced the same case on BOTH sides; window markers in the seat scratchpad (`oracle_claude_*`, `pi/work/run*_start.txt`) | `pi/work/cases4.out`, `cases5.out`, `cases6.out` all `ok` |
| Gaps found by the oracle and closed in `edp8.ts` | (1) idle-queued notifications → ONE user turn (§5 row "queued notifications at idle"); (2) Monitor result held `MONITOR_START_GRACE_MS=250` so early events attach to the Monitor's own result (§5 "Monitor result latency"); (3) one-shot CronCreate result humanises the spec (`(Every minute)`); (4) lane release on assistant `message_end` (WebSocket path has no `after_provider_response`); (5) `sendFollowUp` retry over the agent-start race | fake harness 30/30; parity §5 rows |
| Case pins ruled by the architect | case 4 uses a builtin-only 700-char echo (a `head\|tr` pipeline raced the ~270 ms window: attached on Claude, standalone on Pi — a shell-speed artefact, not a semantic gap); case 7 prescribes `* * * * *` one-shot (model-chosen specs differed) | `parity_oracle.py` CASES |
| Claude-side anomaly, NOT mirrored | a one-shot whose slot passes while the seat is idle may never fire (af5e71f3) or fires at the next idle minute after another job took the slot (26a68cfa); Pi fires at the first settle after due. Recorded as a deviation for G1 (parity §5 "one-shot due while IDLE") | CronList 22:53 IST; fire 17:42Z |
| Boot-time tool list | deviation (unchanged): Claude defers the five tools until `ToolSearch`; Pi registers them at boot; built-in tool names differ (`Bash` vs `bash`) | G1 |
| qa adversary round (report-fb5ff85cd9, m-527660cbce) | 13 reproduced defects fixed and pinned in the fake harness (47/47): A2 admission fails CLOSED (quota block / lane busy → abort + error), A3 dead queue tickets skipped (ticket liveness by touch, 10 s) + aging (120 s) against consult starvation, A4 unique lease ids + consult TTL = its own timeout, A5 bounded cron fields, A6 oracle (empty trace = failure, CronList errors kept, `[error]` flag, receiving tool on attached notifications), A7 `--both` runs the live Pi seat against the checked-in Claude reference + `EDP_PARITY_CLOCK_SCALE`/`EDP_PARITY_SEED` + accelerated 7-day expiry check, A8 failed sends re-queue, A9 fire committed before the send, A10 reschedule from the slot, A12 `ws` source, A13 spawn error → failed envelope, #16 idle deliveries coalesce (20 ms). Not fixed, recorded: A1 (proxy role endpoint — owner finding), A11 rate-limit pattern [H], A14 parallel-attach/abort (S6 live probe), A15 containment posture (= a Claude seat's) | `tests/pi_ext/fake_pi_harness.ts`, `tests/test_admission_lane.py` (9), `tests/test_parity_oracle.py` (6), parity §5 rows |
| Monitor variant | Claude 2.1.270 serves two Monitor tools per seat (parity §5 "Monitor tool VARIANTS"); the seat mirrors `EDP_MONITOR_VARIANT=persistent` (default, this session's reference) or `expiry` (qa's seat) — G1 line 7 | edp8.ts `MONITOR_VARIANT` |
| Suspended-seat resume | Claude replayed ~80 missed heartbeat fires as one turn after a 3-day suspend; Pi fires each due job once at settle — deviation, G1 line 8 | parity §5 "missed periods while SUSPENDED" |
| `parity_oracle.py --both` | runs `scripts/parity_live_pi.py` (one Pi RPC session, seeded ids, the 9 live CASES) and diffs against `tests/pi_ext/oracle_traces/claude_trace_final.json`; exit 0/1/2 (2 = the seat cannot start: no install or no login) | run 7 output in `.pi/parity/both-run7.out` (see §6.2) |

### 6.2 `parity_oracle.py --both` — live runs 2026-09-17 (Pi 0.85.1 at edp-pool/.pi-harness, Astra via the Pi login)

| run | result | note |
|---|---|---|
| 7 (12:25Z) | all 9 cases ran; diff = `<output-file>` path only (relative tasks dir not normalised) → fixed in the normaliser + runner | `.pi/parity/both-run7.out`; re-diff from its mirror: 0 diffs |
| 8 (12:31Z) | 1 hunk: case 1 first line attached to the Monitor's own result on Pi (first line +40 ms → batch flush +240 ms < grace 250) — a race Claude does not have | fix: `MONITOR_START_GRACE_MS = BATCH_MS = 200` (parity §5 row) |
| 9 (12:37Z) | **0 diffs, exit 0** — 37 events per side, 9 cases | `.pi/parity/both-run9.out` |

Deviation noted for the oracle: Claude's five tools are *deferred* (name-only until `ToolSearch`); Pi registers them fully at boot, so the boot-time tool list differs. Claude's `Bash` vs Pi's `bash` (and read/edit/write names) also differ — out of the words' scope (monitor + cron) but visible at the model-input boundary; recorded for G1.

## 7. Spike SA (Claude Code + local proxy + OpenAI API key) — NOT STARTED, blocked at G1
Blocked on an `OPENAI_API_KEY` (the ChatGPT login the owner chose does not serve route A1): needs `OPENAI_API_KEY` (route A1 is API-key by design §7). Plan: claudex or raine/claude-code-proxy on 127.0.0.1, `ANTHROPIC_BASE_URL`/`ANTHROPIC_AUTH_TOKEN`/`ANTHROPIC_MODEL=gpt-6-astra`, a normal v8 seat under the pool, the same check list at the JSONL boundary, plus the list of features that still call api.anthropic.com.
