# S5 adherence read

Read the named implementation/test files against this approved contract (design-f716d0d138 v7 §4.9 + S3 drafts). Goal: open-tab Windows question/approval notifications; explicit permission, generic contents, authenticated server recipient authorization (not client heuristic), no initial backlog storm, bounded atomic participant/request dedup across tabs/replay/restart, minimal worker with no fetch/push/cache or secrets/body in stored data/URLs, same-origin eligible participant tab reuse + live identity revalidation, canonical event/source routing + page reauthorization, dirty draft/pending-upload safety, no duplicate tabs on focus refusal, truthful denied/insecure/offline/error fallback. No closed-browser delivery promise. Real native matrix is QA-owned after implementation, not an engineer fabricated pass.

Craft: keep existing single authenticated feed, envelope API, auth headers, tab-local drafts, typed React+CSS modules, narrow path edits. No dependency additions. Historical no-consult/four-theme/migration strategy restrictions superseded by current epic design. No shared-service restart/live DB/test credentials or paid provider prompts.

Engineering evidence: targeted Python selectors/API + harness/serve smoke, RTL permission/dirty/identity/error/replay tests, worker VM routing tests, one isolated Chromium spec with actual worker/IDB/authenticated feed and cross-tab replays. Chromium test mocks native display/focus and synthetic notificationclick explicitly; it is NOT native evidence. `NATIVE-MATRIX.md` provides 20 each foreground/background/minimized/multitab + failure procedure; 0/80 executed, new owner consent needed. Native harness serves production SPA/API on owned temp board/home loopback, never launches a browser.

Inspect correctness/security/lifecycle races and unsupported claims, especially startup baseline vs feed, revocation/identity change, IDB transaction claim/failed display, service worker lifecycle/control, component async cleanup and router/draft behavior. Report concrete defects and smallest safe fixes; do not edit files or require final native QA before implementation handoff. Diff is uncommitted scoped S5 additions plus small integrations; unrelated scratch files are not ours.
