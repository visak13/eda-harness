# S1 → S2 integration contract

## Authority / reference disposition
Approved design-f716d0d138 v7; exact owner approval m-64a0762cce `[design_signoff] Approve design-f716d0d138 v7` and gate verification note-5dcb6c2d3f. Owner feedback m-4976b4d372 and m-09b402580a rejected design-dominant/garden direction in favor of conversation-first/subtle retro. Clean reference review packet m-692e2a0afa / note-1fa814d4dc precedes approval. This records v7 approval, NOT an invented specific approval of our new family sheet. Sheet reaction requested m-a47e7b2a95, art-9a7798dabb; pending.

## Focused reference specifications
- Epic: revision3-clean-epic.png conveys heading + Status, Owner/requester, Assigned seat/person, needs-action/blocker; links Design vN / Files & evidence / History / Work; conversation takes the main column, not oversized cards. Complete composer with recipient/type/mentions/replies/attachments/image previews/Expand/help/shortcuts and draft safety. Neutral sample work, no garden artwork. Do not invent Pause/Retry.
- Review: revision3-clean-review.png conveys pinned source/title/version/actions, readable body 16/24 and metadata >=14/20, feedback next to the reviewed doc/version. Request changes stays in viewer and posts to source; dedicated tab carries identical context, normal comments remain distinct. Dirty source and feedback drafts remain independent. Approve only actual current design/version/gate. No decorative body type.
- History: contextual viewer categories Conversation, Decisions, Status & assignments, Documents/evidence, Activity. Explicit direct-object versus epic-subtree scope. Plain actor/time/outcome; technical IDs secondary. Files categories Design, References, Evidence, Deliverables, Other; retain unmatched/shared records. Find/Browse all records preserves global archive; no primary Library duplicate. There is no approved history PNG: this written spec is the reference, not a fabricated render.
- Usage: revision3-clean-usage.png conveys small bounded widget immediately above Find, not new page/tab or scrim. Claude 5h/weekly/Fable (subordinate, unavailable okay), Codex 5h/weekly; label timestamps/freshness/errors. No invented real telemetry; specimen values synthetic.
- Narrow: 320/390 CSS-px column; compact labelled navigation, wrap metadata/actions/context links; composer stacks without fixed keyboard overlap. Viewer fills bounded viewport, header wraps, feedback stacks below readable document. Usage width <= viewport minus 24px, internal scroll and focus return, no permanent spacer lane. Preferences body portal width min(320px,vw-24), clamped 12px and internally scrollable. Local table/code scrolling only; no body overflow hiding. Actual 200% zoom and landscape checks belong to runtime integration.
- Themes: all eight themes remain; Obsidian page exactly #000000. Functional icons currentColor at meaningful control/focus contrast >=3:1; text >=4.5 (HC >=7). Character belongs in tiny motifs, not shading; bold clear functional boundaries. Specimen light/dark/HC colors are isolated examples, NOT proof for theme token pairs.

## Assets
`runtime-mapping.json` is exhaustive: 35 functional names + 10 status names; 11 bot identities. `icon-paths.ts` is typed reusable geometry; `icons/*.svg` are safe standalone originals. Generate deterministically with build_assets.py. No scripts, resources, CSS imports, filters, gradients or raster fragments. Each functional SVG uses a 24 grid and 2px rounded currentColor stroke. Adaptations deliberately remove generated texture, repair collapse to inward arrows, add owner's key-like antenna, preserve simple flat silhouettes. No claim that SVGs were image_gen outputs: actual image_gen supplied the source PNG; vectors are manual adaptations.

## Runtime obligations
1. Replace Icon's Record<string,string>/string union with finite IconName. No unknown fallback to Decisions. Validate dynamic backend names at the boundary and display explicit neutral unknown/omit decorative glyph rather than false semantics. Status mapping keyed against actual TicketStatus with exhaustive TypeScript check (not arbitrary string Record).
2. `status-partial` has an extra filled semicircle. Use ICON_FILLS together with ICON_PATHS (or copy both paths from SVG); stroke path alone is insufficient. Keep short visible label and accessible name; never infer statuses from counts/presence. SVG is decorative inside a labelled button/status; icon-only controls require purpose-specific aria-label (Close versus Remove attachment). Standard <svg> element is decorative aria-hidden, focusable=false; principal targets >=44px, glyph 16/18/24 independent.
3. Bot SVG templates are shape data for avatars.py, not a replacement identity adapter. Keep human branches, IDs, picker preference/fallback and model/role textual semantics untouched. Preserve consultant OR model contains gpt dispatch; no provider logos or new model guesses. System and unknown differ. Copy vector body into existing _svg wrapper and retain size/decorative/accessibility behavior. Text role/model label remains authoritative. Reviewer lens and other small accents can become subtle at 16px; full role text must remain visible. Avatar external boundary/focus ring from theme tokens must remain >=3:1 against dark surfaces (do not rely on dark illustration outline on dark panel).
4. Avatar bytes continue via authenticated fetch → object URL; never raw unauthenticated endpoint img; revoke owned URLs. Do not inline arbitrary uploaded SVG. Repository-owned Icon paths are trusted authored geometry, separate from uploaded content.
5. Preserve backwards route/library glyph compatibility and text-native select semantics. Use inventory.md for existing Unicode control replacements; never globally replace arrows in prose/history/document content. Reuse close/check glyphs with contextual labels rather than adding operations. No new runtime dependencies.

## QA handoff
S1 delivers source/assets/specimen only, S2 integrates into built app and captures actual surfaces/theme comparisons. QA independently checks all statuses, typed invalid-name behavior, identity/model preservation and 16/18/24 light/dark/HC specimens plus actual application. Neither static reference nor generated contact sheet proves runtime behavior/accessibility/contrast. One consolidated QA at epic acceptance; no self-verdict here.
