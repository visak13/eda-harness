# S6 adherence read (one independent source review)

Story s-c38a764934, design-f716d0d138 v7 §4.10. Review own commits 5487cdf + cd53663, not sibling work. Default-off private subscription Usage widget above Find (304px, max560/viewport, nonmodal AnchoredPanel, no route/tab/scrim), Claude5h/weekly/Fable + Codex5h/weekly, genuine utilization not tokens/spend. Duration classification and ordinary absent→present→absent mandatory. Fable/absent Codex5h accepted; no invented values/observations. Current legitimate sources have no observation timestamp: valid receipt numbers deliberately stale/unknown, receipt separately labelled, expired reset clears values.

Inherited applicable craft: single envelope client and actor/token auth, scoped TanStack query/no draft churn, eight theme tokens contrast/no invented theme values, keyboard/outside dismissal/focus restore, bounded reflow, safe subprocess ownership/cleanup/no secrets, typed structured errors/no raw payload exposure, cold targeted tests. Old migration geometry/four-theme/no-consult rules superseded by current v7. One consult required by engineer protocol; no per-story QA self-verdict.

Paths: src/edp8/{usage.py,usage_sources.py,api_usage.py,service.py registration}; tests/test_usage.py; web/src/api/usage.ts; web/src/components/{UsageWidget.tsx,UsageWidget.module.css,UsageWidget.test.tsx,AppShell.tsx slot,AnchoredPanel.tsx optional height}; web/e2e/s6-usage.spec.ts; docs/s6-usage-activation.md. Existing AnchoredPanel preference behavior otherwise unchanged. Source only; screenshots synthetic and not a provider proof.

Implemented boundary: server config explicit canonical participant→provider binding ID + absolute local snapshot, read-only private file projection/cache/debounce/backoff. No HTTP write/config path, no default fleet telemetry. Config re-read each request for immediate ACL revocation. Collector opt-in separately: Claude session-only statusline projects finite numeric fields; Codex owned stdio initialize/account/read/rateLimits/read and updated events, validates private hash of supported account email (installed 0.153.4 has type/email/planType, NOT id). Never persist/export email/hash to board. Existing CLI handles its own login; no agent credential-file reads/login/model prompts.

Verification so far: 26 new Python tests + service regression (41 passed1skip combined before final rerun), 21 widget/shell RTL tests + existing contrast284, TypeScript and build green, 4 isolated browser specs eight themes ×1440/320/844 sizes with axe/draft/focus/bounds. Full web e2e not run. Native Firefox/200% browser zoom independent QA, not claimed.

Operator permission obtained via owner m-597c52eaa1/architect m-db20600e19: bounded private setup/read-only probe and NEW owner-started Claude session, no shared rollout/restart/permanent telemetry. Private config/settings prepared; owner notified command. New genuine Codex receipt14:26:54Z weekly79% reset1790325485,5h absent; process reaped. Original provider observation unknown. Initial probe safely refused absent account id; corrected to actual supported email pin and reran. Fresh Claude owner session receipt pending. Do not inspect private setup, tokens, raw account payloads or unrelated untracked scratch files.

Please independently identify concrete implementation/ruleset adherence defects (security/binding, stale/null/expiry, lifecycle/refresh, tests/keyboard/reflow), with paths and fixes. Do not edit files, start providers/browsers/services, or broaden scope. Distinguish proven defects from acceptance limitations. We will await and act on result before handoff.
