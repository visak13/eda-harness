# S6 Usage engineering handoff — 2026-09-18

Story `s-c38a764934`, design `design-f716d0d138` v7 §4.10, plan `note-80a3c96731`. Engineering evidence only; **no QA verdict, live deployment or owner acceptance**.

## Delivered
- Usage trigger immediately above Find. Independent 304px/max560px/viewport-bounded nonmodal AnchoredPanel, no new route/tab/iframe/scrim or main-page dimming. One optional height parameter added to shared panel; preference defaults unchanged. Source draft/composer/feed/NotificationCenter untouched.
- Claude 5h/weekly/Fable and Codex 5h/weekly; numbers are used-percent (zero valid), local absolute reset with timezone, status and unknown observation shown. Expandable source receipt timing explicitly not observation. Fable remains unavailable under Claude, Codex absent short-window remains unavailable.
- Authenticated `/v1/me/usage` uses canonical actor ID, private/no-store response and explicit operator mapping only. No role/default-host account disclosure or client-provided paths/participant selector. Source snapshot cache >=30s; missing/auth/error backoff to300s, bounded256 actor/provider entries; ACL revocation re-read every request. UI only queries while open, debounces manual refresh and polls >=60s with backoff. No provider process/network/model prompt on UI opening.
- Separate default-off opt-in collectors: session-only Claude allowlisted stdin capture; owned Codex stdio App Server reads/updates with supported account fingerprint check. No credential-file reads/login/config mutations. Private binding IDs, paths, hashes/email/raw payload/errors never exported to board. Account changes fail closed; API-key accounts not treated as subscription. Every ordinary quota snapshot replaces prior windows, duration drives classification.
- Both real sources lack observation timestamp: valid numbers intentionally `stale`/observation unknown, receipt separate; old receipt labelled, expired reset suppresses utilization rather than inventing zero. CLI projection and API normalization reject invalid/nonfinite/oversized numbers.

## Source validation and consent
- Prior S0 historical proof remains in `docs/s0-capability-proof/SOURCE-EVIDENCE.md`; fixtures replay historical Claude0/15%, Codex92% at historical time, never current values.
- Owner `m-597c52eaa1`, architect `m-db20600e19` authorized bounded private setup/read-only collection and new **owner-started** Claude session only. Shared deployment/restart/permanent telemetry/global/wrapper edits/login/agent prompts remain unauthorized.
- New genuine Codex receipt **14:26:54.571353Z**: weekly **79%**, reset **1790325485**, 5h absent. `docs/s6-codex-source-proof.json` retains projected source and exact normalized percentage/reset comparison, unknown observation, mapped/unmapped comparison. No account fingerprint or identity retained. Supported installed account/read shape is type/email/planType (not account id); initial id-based pin safely refused, schema-key-only inspection corrected to private normalized-email hash. Owned child was reaped on both failure and success. This proves real collector/normalizer, **not a production browser deployment**.
- New genuine Claude receipt **14:35:33.942309Z**: 5h **2%**, reset **1789756200**; weekly **17%**, reset **1790251200**; Fable unavailable. `docs/s6-claude-source-proof.json` retains allowlisted source and exact normalized percentage/reset comparison. Architect `m-d624acc465` independently verified owner screenshot shows Claude Max/ordinary response and capture status; numeric values were read from receipt, NOT inferred from screenshot. Owner started the dedicated session via `m-cb39fc3dcc`; no engineer session/login/prompt. Claude identity remains operator-attested and original observation time unknown.

## Verification receipts
Commands use existing venv/package lock; no uv resync/dependency changes/full web e2e. QA resource seat `qa.s-e1260012b9` confirmed no competing browser/consult; checks serialized, workers1, isolated temp boards/DBs/owned processes only.

- `.venv/Scripts/python.exe -m pytest tests/test_usage.py tests/test_service.py tests/test_api_views.py -q --tb=short --color=no`: **66 passed,1 skipped** after consultant fixes (29 Usage tests). Numeric/malformed/oversize/null/zero, durations/pool precedence, absent→present→absent cache/event updates, expiry, unknown observation, ACL/revocation/identity switch, backoff, endpoint tokens/no-selector/no-private-output, atomic capture, fingerprint mismatch, source failure, RPC interleaving and owned-process cleanup.
- `npm --prefix web run test -- --run --maxWorkers=1 src/components/UsageWidget.test.tsx src/components/AppShell.test.tsx src/theme/contrast.test.ts`: **305 passed**,3 files. Includes authenticated envelope request, closed widget no fetch, zero vs missing/Fable grouping, error hides cached numbers,401 identity panel, ordinary source updates, reset expiry, no URL/draft change, Escape/Close/outside focus. Existing eight-theme contrast tests use exact same panel/rail/ink/muted/strongline/accentink pairs; no new palette values.
- `node web/node_modules/typescript/bin/tsc --noEmit -p web/tsconfig.json`: PASS.
- `npm --prefix web run build`: PASS; pre-existing >500KB bundle advisory remains (about566KB JS/174KB gzip).
- `EDP8_BOARD_CMD='C:\Projects\Learning\eda-base3\v8\.venv\Scripts\edp8-board.exe' npm --prefix web run e2e -- e2e/s6-usage.spec.ts --workers=1 --max-failures=1`: **4 passed** on installed Chromium1234. Default-unlinked real endpoint, above-Find/no navigation, synthetic state rendering; eight themes ×1440x900/320x568/844x390; bounded <=304px/viewport and scroll-reachable refresh, no root inert or page horizontal overflow; axe WCAG2A/AA serious/critical zero; dismissal/focus/source draft.
- Same command with `--config playwright.s6-firefox.config.ts --workers=1 --max-failures=1`: **4 passed** on installed automation Firefox1538. Same scoped fixture checks; **not** owner's stock Firefox156/native toast/200% browser zoom proof. No browser downloads/profile interference.
- Optional real-source spec: set `EDP8_E2E_USAGE_CONFIG` to the approved private mapping and run `... npm --prefix web run e2e -- e2e/s6-source-validation.spec.ts --workers=1 --max-failures=1`: **1 passed**. Reads actual retained Claude/Codex receipts through an isolated board API and built widget, compares every reported percentage/reset, confirms unmapped actor gets no account, retains `s6-usage-source-real{,-codex}.png`. No provider launched by test; screenshots are genuine capture values, not synthetic fixture data and not a production deployment. Cold suites skip this optional spec absent explicit private mapping. E2E fixture now strips inherited fleet EDP8_USAGE_CONFIG; only deliberate EDP8_E2E_USAGE_CONFIG can enable test mapping.
- Final browser reruns also assert native Tab enters source timing summary and source caret selection survives dismissal, in both engines.
- `git diff --check`: clean (Windows LF/CRLF notices are not errors).

Initial test-only failures: oversized pytest parameter ID exceeded Windows environment limit (added short ids); token test missed suite's explicit EDP8_TOKENS override (fixed isolated fixture). All rerun green. Source implementation defect found by real schema inspection noted above, not concealed as initial success.

## One awaited independent adherence review
Consult `20260918T142911Z-9dc290e2`, brief `docs/s6-adherence-brief.md`, completed/read before handoff. Manifest provider_model unavailable; no model identity invented. Two proven P2s fixed with cold regressions:
1. RPC calls consumed/dropped quota notifications during identity checks. Deferred ordered notifications now survive calls, latest snapshot replaces prior values; quota read response supersedes earlier notifications and post-read identity updates override it. Once-mode test interleaves notifications inside both identity/quota RPCs and proves absent short-window wins.
2. `math.isfinite` could overflow on a huge JSON integer before range validation. Bounds now precede float conversion in collector/normalizer, outer safe boundaries catch overflow, huge-integer tests cover both Claude/Codex and cache. No raw exceptions/old apparently-fresh receipt escape.
No second consult. Later test/report/proof changes not separately independently reviewed; consolidated QA still owns acceptance.

## Criterion evidence map
- **c-a759c1af61 (command):** test_usage.py + real S6 Codex proof JSON + S0 source document/historical replay + activation contract. Cold automated tests require no credentials. Source values/observation distinctions and accepted Codex5h/Fable absence are explicit. Fresh S6 Claude capture and isolated real-source API/widget comparison now completed as above. Shared deployment/account-revocation and independent acceptance remain QA/operator work, not a fabricated pass.
- **c-17bb736d6e (look):** screenshots `web/e2e/evidence/s6-usage-{1440,320,844}-{folio,obsidian,folio-hc}.png`, matching `-bottom` images; Firefox variants append `-firefox`. These are **synthetic fixtures** (future reset2100 deliberately not real sample); representative light/dark/narrow/short images inspected by engineer. All eight themes checked automatically for axe/bounds, not eight-theme human visual approval. Demo `art-b2928c5ee9` attached on epic; no owner visual reaction recorded yet. Additional `s6-usage-source-real{,-codex}.png` shows genuine captured values through isolated API/widget, separately from synthetic geometry screenshots. QA must independently judge final appearance/reflow/keyboard and source comparisons.

## Operational handoff / remaining gaps
Exact activation/rollback contract: `docs/s6-usage-activation.md`. Private validation directory `C:/Users/aksou/AppData/Local/Temp/edp8-s6-usage` was newly created, inheritance removed, current operator only full-control ACL. Contains temporary usage.json/session settings and genuine allowlisted receipts. **No env/config in the live board was edited.** Settings command resolves module using existing venv even outside repo. Do not delete while owner's dedicated Claude session may write; architect/operator coordinates session end, receipt retention/redaction, then scoped removal. No Codex collector left running; no auto-start installed.

Actual browser200% zoom, native Firefox/virtual keyboard, full populated-app acceptance matrix and deployed live telemetry remain independent QA/operator checks. Reduced viewport does not substitute for zoom. Dark implementation and private bounded validation do not authorize permanent collection or shared service restart. Rollback scoped code/slot/router commits; remove approved mapping immediately to revoke, stop only owned collector/end dedicated Claude session; no DB migration.
