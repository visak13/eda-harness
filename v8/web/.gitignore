node_modules/
test-results/
playwright-report/
.playwright/
coverage/
*.local

# The root .gitignore ignores *.png; re-include the committed reference plates and
# win32 Playwright baselines so evidence lives in the repo (strategy_ll §9).
!e2e/design-reference/*.png
!e2e/**/*-snapshots/**/*.png
