{
  "mcpServers": {
    "edp8": {
      "type": "http",
      "url": "${EDP8_MCP_URL:-http://127.0.0.1:9402}/mcp/${EDP_ROLE:-owner}",
      "headers": {
        "X-Participant": "${EDP_HANDLE:-owner}",
        "X-Session": "${EDP_SPAWN_SESSION_ID:-}"
      }
    }
  }
}
